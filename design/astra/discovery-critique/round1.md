**Part 1 — my earlier problems**

1. **Final confirm buried on the phone — Fixed.** `phone-6b` shows a dark bar pinned along the bottom ("Ready? Go to Finish Discovery →") while I'm halfway down the scope document, and in `phone-6` it sits right above the confirm card — so the checkboxes are always reachable now (it looks like a jump, not the confirm itself; it also doesn't say how many of the two boxes are still unticked).

2. **Turns, fuel and dollars — Partly fixed.** The usage card now opens with a plain sentence ("Your next reply is free. 10 free replies left today. Free replies never use your money."), sits below the brief, "Buy fuel" only appears when I'm actually blocked, and `desktop-3` states "You cannot send another reply right now. Your draft is saved." — but the "Beta free turns used · 16% used · Within allowance · 1 reply = 1 turn" bar still explains nothing, and the four-numbers-together rule blocks the disclosure fix, so the card has to carry that weight in copy.

3. **Dead button loudest on arrival — Partly fixed.** The panel now says "Start below: answer the AI's first question…" and the disabled pill is visibly lighter than the dark one at 100% (`desktop-1` vs `desktop-5`), but on `phone-1` it's still a wide, near-full-width pill above the chat and the current question still isn't at the top of the phone screen; the fixed rule keeps the button visible, so restyling is the only option and it's better than before.

4. **"Finish" twice / navigation disagreeing — Partly fixed.** The review breadcrumb now reads "Discovery · last step" (`desktop-6`) instead of "Scope", which removes the contradiction while still honouring the rule that Discovery stays current until I confirm; but at 100% I see two live dark "Finish Discovery →" buttons (`desktop-5`, progress panel and chat card) and the confirm card still opens with a green check before I've ticked anything (`desktop-6`, `phone-6`).

5. **Brief read-only / buried on phone — Partly fixed.** Pencils now say "Edit" (`desktop-2`), the brief comes before the usage card on my phone (`phone-1`, `phone-2`), and there's a "View brief" jump near the top; but "Revision 2" is still jargon, I can't see any highlight on the field that just changed, and I still can't tell what tapping Edit opens.

6. **Checkbox promise — Partly fixed.** The card now adds "In practice your NGO decides who can see volunteer details, and the tool stores only what 'Information handled' lists." and keeps the "does not promise delivery or a price" note (`desktop-6`, `phone-6`), but the checkbox itself still says my NGO "takes responsibility for data access".

7. **Which answer went where — Not fixed.** In `desktop-4`/`desktop-5` my three answers are still one unlabelled bubble with no "Filed: …" line (only the brief shows the mapping), and "Answer 3 independent questions together" still sits under a single visible question in `desktop-1`/`desktop-2`/`phone-1`/`phone-2` — though I can't tell from the fresh shots whether the other two questions were about to appear.

8. **Small noise — Partly fixed.** Receipts now say "Free reply · no charge" and the "N topics left" line is gone (`desktop-2`, `desktop-5`), but "Reference files · 0 shared with AI" is on every screen and "Suggested cause labels", "The draft needs a rewrite" and "Ask AI a question first" are still there (`desktop-1`, `desktop-6`, `phone-6`).

**Part 2 — new problems, most severe first**

1. **The blocked card contradicts the free turns I can see — Blocker? No, Major.** `desktop-3`: the same card says "You cannot send another reply right now" and "Wait for the daily reset or buy fuel to continue" while showing "Beta free turns 42 of 50 left", "$0.00" paid fuel and a "Buy fuel" button. I can't tell why 42 free turns don't help, so my only options look like waiting or spending NGO money — exactly the money worry the new copy was fixing. Fix: one plain line naming the limit next to the block message ("Today's free replies are used; beta turns aren't used in Discovery"), keeping all four required rows but muting the ones that don't apply. Severity: Major.

2. **The data explanation arrives after the promise — Minor.** `desktop-6`, `phone-6`: "In practice your NGO decides who can see volunteer details…" sits below both checkboxes and the grey confirm button, so I tick the data promise before I read what it means. Fix: move that sentence directly under the second checkbox, above the button. Severity: Minor.

3. **"View brief" is my first tappable thing on the phone — Minor.** `phone-1`, `phone-2`: an outlined "View brief" button sits under the title, before the stepper, progress card and chat, and before I've answered anything it takes me to six "To be discussed" rows far down the page. Fix: turn it into a small progress chip ("Your brief · 1 decided") near the progress card, or move it into the header row. Severity: Minor.

4. **On desktop I finish reading, then have to go back up to confirm — Minor.** `desktop-6`: the checkboxes and confirm button live at the top of the right rail while the document runs well past the first screen; the phone got a persistent action, the desktop didn't. Fix: make the confirm card sticky in the rail, or repeat the two checkboxes at the end of the document. Severity: Minor.

5. **"Still needed" uses names the chat never uses — Minor.** `desktop-1`, `phone-1`: the panel lists "main priority, who books shifts, maintenance owner, success measure, booking rules, information handled" while the chat asks "Who should book volunteers into shifts?" and "What information will the tool hold?"; I can't match the remaining list to the questions I'll actually be asked. Fix: reuse the chat's wording. Severity: Minor.

Verdict: not ready
