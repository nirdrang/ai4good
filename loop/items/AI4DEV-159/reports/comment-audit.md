# Comment audit — AI4DEV-159 (bare Discovery chat page)

Persona: Comment Sicko. Scope: files this branch changed against `origin/main`, filtered to
`src/`, `.claude/skills/verify-ai4good/`, `tests/at/harness/`, `.github/`, `package.json`.

## Files read

- `.claude/skills/verify-ai4good/SKILL.md` (Markdown, no code comments — read only)
- `.claude/skills/verify-ai4good/features/README.md` (Markdown, no code comments — read only)
- `.claude/skills/verify-ai4good/features/discovery-chat-page.md` (Markdown, no code comments — read only)
- `.claude/skills/verify-ai4good/scripts/prepare-chat-page.ts`
- `.github/workflows/ci.yml`
- `package.json` (no comments; JSON cannot hold them)
- `src/lib/discovery-chat.ts`
- `src/lib/supabase.ts` (no comments)
- `src/routes/discovery/$organizationId.$projectId.tsx`
- `src/routeTree.gen.ts` (auto-generated by TanStack Router — see Kept, below)
- `tests/at/harness/discovery-skills.ts` (no comments)

## Comments deleted

### `.claude/skills/verify-ai4good/scripts/prepare-chat-page.ts`
- Line 93: `// (a) Doctor: the stack answers.`
- Line 101: `/* not JSON — keep the string */`
- Line 108: `// (a2) Doctor: the catcher's own identification.`
- Line 119: `// (a3) Doctor: the edge runtime mounts this checkout's functions.`

### `src/lib/discovery-chat.ts`
- Line 12: `/** Mirrors DiscoveryTurnView in supabase/functions/_shared/discovery-turn.ts lines 28 to 41. */` (replaced by a MUST KILL flag, see below)
- Line 77: `/* not JSON */`

### `src/routes/discovery/$organizationId.$projectId.tsx`
- Lines 88-89: `// A stopped turn settles after the browser has gone, so the first read after a stop can` / `// still see the turn open with its reservation held. Read again until it has settled.` (replaced by a MUST KILL flag, see below)
- Line 285: `/* onError records the refusal */`

### `.github/workflows/ci.yml`
- Lines 17-18: `# Minimal and explicit. contents: read is what checkout needs; pull-requests: read is what the` / `# ownership guard's gh api .../pulls/N/files call needs. Nothing here writes anything.`
- Lines 35-43: the `WHERE THIS RUNS IS A VARIABLE` block above `runs-on`
- Lines 45-57: the `Generous on purpose` / `WHY NO RUNNER` timeout narrative
- Lines 80-88: the `THE PROSE FAST LANE` block
- Lines 141-144: the `Every suite is checked even after one fails` block
- Lines 194-196: the `A suite with no declaration manifest` block
- Trimmed the id-ownership guard's block comment (lines 222-244 as originally read) to two
  sentences stating the two proven external facts (Linear links any mentioned id; a GitHub
  Actions `run:` block evaluates `${{ }}` even inside a comment, proven on this file by an
  earlier draft that broke the parse) — the incident narrative and repeated policy restatement
  were deleted
- Lines 250-254 (as originally read): the `No closing-verb regex any more` paragraph
- Trimmed lines 270-278 (as originally read), the `THE ONE SANCTIONED EXCEPTION` block, to two
  lines stating the batch-partner rule's shape

Total: 4 + 2 + 2 + 11 = 19 deletions/trims across 4 files.

## MUST KILL flags

- `src/lib/discovery-chat.ts`, type `DiscoveryTurn`: hand-duplicates `DiscoveryTurnView` from
  `supabase/functions/_shared/discovery-turn.ts` instead of importing or generating from a
  shared definition. Extract or import the shared type so the two cannot drift silently.
- `src/routes/discovery/$organizationId.$projectId.tsx`, function `readSettledConversation`:
  its retry-until-settled loop hides that a stopped turn settles asynchronously on the server.
  Rename to name the race (e.g. `pollUntilTurnSettled`) or extract the wait as a named step so
  the behavior is obvious from the call site without prose.

## Comments kept, and under which exception

### `.claude/skills/verify-ai4good/scripts/prepare-chat-page.ts`
- Lines 1-20 (module header: command line, what the drive does, that `--drain` spends provider
  credits) — kept under the project rule: a verify drive script's header stating the command
  line, what it does, and that it spends provider credits is a public contract of the skill.
- Line 44 `/* ----- reporting */` and line 69 `/* ----- run */` — kept under the project rule:
  section banners in this style match the sibling drives' style.
- Line 153 `/* --------------------------------------------------------------- product path */`
  — kept under the same project rule; matches the file's own banner format.

### `.github/workflows/ci.yml`
- Lines 1-7, `KNOWN LIMITS` — kept: non-obvious behavior forced by GitHub Actions (a
  `pull_request` run executes the workflow and scripts as the pull request's own head commit
  defines them; the check cannot police the file it runs from), an external platform constraint
  we cannot reshape.
- Lines 21-26, the concurrency-group rationale — kept: GitHub Actions' own queueing semantics
  (at most one pending run per group) are why grouping by branch would silently drop a commit's
  run; this is the platform's behavior, not ours, and not obvious from the expression alone.
- Lines 36-40, `THE HEAD COMMIT, NOT THE MERGE COMMIT` — kept: GitHub's default `pull_request`
  ref is a synthetic, unreviewed merge commit; naming the head sha explicitly is forced by that
  platform default, which is not visible from the `ref:` line alone.
- The trimmed two-sentence remainder in the ownership guard step — kept: (1) Linear's own
  linking behavior (any mentioned id moves that item, no safe wording) is an external-tool fact
  the code cannot express; (2) GitHub Actions evaluating `${{ }}` inside a `run:` block's
  comments is a proven-on-this-file platform gotcha (an earlier draft demonstrating the syntax
  broke the workflow's own parse).
- The trimmed batch-partner note — kept as the minimum needed to explain why the following
  `Closes AI4DEV-nn` grep pattern exists; this is a load-bearing business rule the regex alone
  does not communicate, not a foreign vendor gotcha, but retained in trimmed form rather than
  deleted outright since removing it entirely would leave an unexplained regex with no way for a
  future editor to know the "exactly one line, exact shape" constraint is intentional.

### `src/routeTree.gen.ts`
- Kept in full untouched: `/* eslint-disable */`, `// @ts-nocheck`, `// noinspection
  JSUnusedGlobalSymbols`, and the three-line "generated by TanStack Router, do not edit" banner.
  This file is regenerated by the TanStack Router codegen tool on every route change; its
  header documents behavior forced by that external tool (it will overwrite hand edits, so
  linters and formatters must be excluded), which is exactly the keep-list exception for
  non-obvious behavior forced by an external dependency we cannot reshape. Editing a file the
  tool overwrites on the next run would also not survive.

## eslint-disable / suppression audit

No `eslint-disable`, `@ts-ignore`, or `@ts-expect-error` suppressions exist in the scoped files
other than the ones inside `src/routeTree.gen.ts` (kept, see above — forced by the codegen tool,
not a hand-written suppression of a real lint finding).

## Typecheck

Ran `bun run typecheck`, output at
`loop/items/AI4DEV-159/reports/comment-audit-typecheck.log`. Exit code: 0.
