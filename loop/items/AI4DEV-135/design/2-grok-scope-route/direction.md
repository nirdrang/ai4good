Direction 2: the scope is its own aggregate with its own write route. A discovery_scopes table (one row per version: project_id, org_id, version, status, contract jsonb, rendered markdown, reason, created_at) and a discovery-scope write route with actions generate, regenerate and remove-label, registered in write-routes.ts with a security-definer RPC. Generation is a separate non-chat Anthropic call over the completed elicitation (structured output via a tool), not a chat turn; it never touches the turn ledger except to read the elicitation. Regeneration bound and reason log are rows in that table; escalation is a row state plus emit. Guardrails stay in the turn path (a column or a small counters table keyed by project). Cause labels: a cause_labels vocabulary table and a join or the need's array, written by the scope RPC.
