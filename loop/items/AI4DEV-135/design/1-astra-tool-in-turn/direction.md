Direction 1: the scope is a tool call inside the chat turn. Add a second strict tool, record_scope, beside record_elicitation in discovery-prompt.ts. The model calls it when the elicitation is complete (or when the ceiling forces wrap-up). The scope is stored as a sibling jsonb column on discovery_turns on the settling turn, added to the immutable allow-list; a scope version is a turn, so the versioned read is 'the latest settled turn whose scope is not null' and regeneration is a new zero-cost turn with a regenerate reason carried in request_settings or a new column. Cause labels are written into need_intakes.cause_labels inside discovery_turn_settle in the same transaction. Guardrail counters live on the turn row (an off_topic boolean the settle records from a deterministic marker in the tool output or reply). Keep the turn ledger the single aggregate for everything Discovery produces.
