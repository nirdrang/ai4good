Direction 4: an append-only event log per project. One table discovery_events (project_id, org_id, seq, kind, payload jsonb, created_at) with kinds such as scope_generated, scope_regenerated, label_removed, off_topic_declined, ceiling_reached, escalated. Every read (current scope version, regeneration count, off-topic strikes, founder flag, escalation state) is a fold over the log in TypeScript, exposed through one read function; the versioned scope read is the fold at a given seq. Writes append through one security-definer RPC and one write route (discovery-scope) whose actions are the event kinds the NGO may cause. Generation is triggered by the turn module when it settles a complete elicitation (it appends scope_generated) and by the regenerate action. The vocabulary is a cause_labels table; label reuse is a fold over label events plus the table. Make the fold the single source of truth; nothing is denormalised onto the need except cause_labels, which the fold writes through for the consumers that already read it.
