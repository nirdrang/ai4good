Direction 3: the need is the aggregate. need_intakes gains scope jsonb, scope_version integer, scope_markdown text, scope_regenerations jsonb (or a small scope_regenerations table), off_topic_count integer and founder_flagged_at; the vocabulary is a cause_labels table. One pure module supabase/functions/_shared/scope.ts (the file NOTIFICATION_COMPONENTS already names) owns the contract type, the parser, the renderer, the label normaliser, the guardrail counter and the regeneration bound as pure functions; the shell (routes and the settle RPC) is thin. Generation runs automatically when the settle that carries elicitation.complete=true lands (the settle RPC or the turn module chains the scope call), and on the regenerate action of the existing project-need write route (new actions regenerate and remove_label). Extend project-need rather than add routes.
