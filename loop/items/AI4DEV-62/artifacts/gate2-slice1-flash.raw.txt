# CODE REVIEW — AI4DEV-62 slice 1 (`610ead7...c2a7b6b`): per-organisation roles

**Method note (read-only, no git available in this cage):** the tree I hold is the branch head, which contains the later slice-2 commits (AT-001.17/.32, migration B) as context. I could not execute `git diff`, so the subject's declarations were verified against the current files plus each file's own landing narrative (`_pending.ts`'s removed-label comments, the pending ledger, the 24→21→19 header count, which are all consistent with this commit flipping exactly AT-001.16/.36/.37 and removing `D3_L1`). The slice-2 material was treated as context per the contract. The measured answers in `loop/items/AI4DEV-62/artifacts/verify-first-answers.md` were read and used as recorded measurements.

My severity scale: **high** = a body can go green for the wrong reason, or shipped code grants/denies authority wrongly on a reachable input; **medium** = a defect no current body drives, where the next natural test would fail or misread; **low** = a real, located divergence that is currently inert.

---

## Findings

**[1] severity: low — `tests/at/suites/req-001/_fixture.ts:1084-1086` vs `supabase/migrations/20260811120000_...sql:43-89`**
- claim: the fixture's operator grant checks organisation existence *before* the grantee-type rule, while the database's BEFORE trigger runs before the org foreign key is consulted, so the two surfaces answer differently for one input.
- why it matters: for a grant into a **nonexistent organisation with a non-NGO grantee**, the fixture returns `refused` ("no organisation … exists") at loop tier, but the database raises the trigger's `not-an-ngo-account` 42501 (BEFORE ROW triggers fire before FK checks — the same mechanism measured in verify-first answer (b) for the account FK; the fixture's own comment at 1078-1081 claims "the order is the database's order" and gets the trigger-vs-index pair right, but the org-existence pre-check at the top is an order the database does not have). No current body drives a grant into a nonexistent org, so no green is at risk today — but this is exactly the direction-2 class: a loop tier grading refusal semantics the database does not have. A future "grant into a deleted organisation" negative would pass at loop with `refused` and fail at integration with `not-an-ngo-account`.
- unverified-runtime-claim: no — the fixture half is code reading; the trigger-before-FK ordering is Postgres semantics already measured on this stack (answer (b), for the sibling FK on account_id; the same mechanism applies to the org_id FK).

**[2] severity: low — `tests/at/suites/req-001/_fixture.ts:1035-1036` vs `supabase/functions/update-organization/index.ts:97-112`**
- claim: the fixture's rename answers `refused` ("no organisation … exists") for a nonexistent organisation, while the deployed function answers 403 `not-a-member` for a well-formed-UUID nonexistent organisation, and the RPC backstop would answer "no such organisation" — three surfaces, three answers, and the fixture matches none of the two live ones.
- why it matters: the deployed path's membership read returns 200 with an empty array for a nonexistent well-formed UUID → `absent` → `not-a-member` (the deliberate no-existence-oracle shape, which direction 5 confirms is not a leak). At loop tier the same input is `refused`. No body drives it today (all bodies rename real orgs), but the next natural negative test — "rename of an org the caller never heard of refuses with not-a-member" — would pass at integration and fail at loop, i.e. the direction-2 target failure in its *other* direction (loop red against semantics the live stack has). Note the nuance: for a **malformed** (non-UUID) id the REST read answers 400 → the edge returns 502 `refused`, which *does* match the fixture — the divergence is specific to well-formed nonexistent UUIDs.
- unverified-runtime-claim: yes — "the deployed function answers 403 not-a-member for a random well-formed UUID" is a runtime behaviour claim. What would settle it: call the deployed `update-organization` with a valid session and a random UUID, and observe the status and `kind` (expect 403 `not-a-member`, not 502 `refused`).

---

## Directions checked clean

1. **Two refusal kinds distinct on every path** — traced end to end: `orgAdminActionAllowed` (member → `not-an-admin`, null → `not-a-member`), the edge's `absent`/`found` mapping, the RPC's two distinct backstop sentences, the fixture's use of the same shared module, and the live adapter's whitelist (`_live.ts:590`). No collapse anywhere; the C arm and the B arm remain different tests.
2. **Fixture-vs-database divergence** — besides the two findings above, the *exercised* paths match the migration exactly: trigger-before-index ordering for a non-NGO grantee into an already-seated org (measured, answer (d); mirrored at `_fixture.ts:1098` before `1111`), release-to-null allowed, and the `not-an-ngo-account` / `org-already-seated` kinds align with SQLSTATEs 42501/23505.
3. **Refusal-writes-nothing read-backs** — every refusal is followed by reads through independent surfaces (`organization(id)`, `organizationsNamed(attempted)`, `membershipsOf`, `membership(org, account)`), at both tiers, and the fixture mutates state only on the success line (`_fixture.ts:1047`).
4. **AT-001.16 Given construction** — exactly as D2/amended ruling 1: A via the product completion, B operator-created unseated then granted the single `member` seat, C unseated; `membershipsOf` length-2 plus role-sort `['admin','member']` plus the null C read proves two distinct rows with distinct roles, at both tiers.
5. **Definer RPC** — `search_path = ''` pinned with every name schema-qualified; `p_name` null/whitespace refused (22023); membership **and** role re-checked inside the definer; nonexistent-org answers `23503` — reachable only by service-role key holders (execute revoked from public, granted to service_role only), and via the product path a nonexistent org is indistinguishable from a non-member (403 `not-a-member`), so no client-reachable existence oracle.
6. **Edge function** — `resolveCaller` pattern matches `create-organization`; the membership row read is keyed on the token-derived `caller.id` (the caller influences only the target org, which is the design); each refusal kind maps distinctly onto the wire; `validateOrganizationName` is imported, not restated; name is validated after authorization, mirroring the sibling function.
7. **NGO-only trigger** — binds `INSERT OR UPDATE`; refuses `platform_admin` (via `<> 'ngo'`); both sentences are exactly the ones the live adapter classifies on (`/NGO accounts only/i`; the 23503 no-account sentence with its own distinct wording). The trigger's UPDATE half closes the re-pointing path.
8. **Privileges (D13)** — exactly one new table grant (`select on org_memberships` to `service_role`); execute revoked from public and granted only to `service_role` on `update_organization`; execute revoked from public on the trigger function; `notify pgrst, 'reload schema'` present.
9. **Declarations** — .16/.36/.37 green at both tiers with every other id keeping its declared kind; the D3_L1 label is gone from `LEAF` and the header's 24→21→19 narrative matches; the ledger's 19 ids biject against the manifest's `verify:` lines and the expected file's red set.
10. **SQLSTATE classification** — `databaseRefusal` reads `errno`/`errcode`/`code` with the 5-char shape guard plus sentence fallbacks. Network/timeout errors cannot match a refusal branch: their codes (`ECONNREFUSED`, `ETIMEDOUT`, `EPIPE`…) either fail the `^[0-9A-Z]{5}$` shape or are not 42501/23505, and their messages contain none of the three sentences, so they land in `refused` — never in a meaningful kind. A cancelled-query 57014 likewise lands `refused`. The direction-10 false-green cannot occur.

No findings outside the subject worth reporting from the later commits I read as context.

`CODE REVIEW: 2 FINDINGS`