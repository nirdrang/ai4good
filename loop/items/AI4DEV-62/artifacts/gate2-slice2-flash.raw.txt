All subject files are read, the plan and rulings are the yardstick, and the measured answers (a)–(f) were checked against the code that claims them. Verdicts on the ten directed questions, then findings.

**Directed answers, in brief**
1. The one-seat index is exactly `unique (org_id)` alone (`20260811130000...sql:41-42`); it binds every path, operator included. Clean replay: the index is created at the end of the four-migration chain on empty tables; the only membership writers (`complete_signup` in both migrations, `create_organization`) create a *new* organisation per call and write exactly one admin row, so nothing trips it. No finding.
2. The guard binds UPDATE only (`...:101-104`), which is safe: the seat is a single column, so INSERT cannot express a second developer; `INSERT … ON CONFLICT DO UPDATE` is still an UPDATE and fires it. Release-to-null and same-account re-write are both allowed in SQL and in the fixture, and the same-account control is asserted by both AT-001.32 bodies. No finding.
3. `revoke all … from anon, authenticated, service_role` present (`...:123`), RLS on with zero policies (`...:110`), FKs exactly as D6, no GRANT statement anywhere in the migration; function execute revoked from public. Verified-first (f) is recorded and agrees. No finding.
4. `_source-scan.ts` resolves from `import.meta.url` (cwd-independent, both tiers), throws on a missing/empty `src/routes/`, and a missing `routeTree.gen.ts` propagates as a loud error — no vacuous pass. The net is applied to file names and to route-tree literals (which carry the paths). No finding.
5. The 401 control from measured (e) is **not** asserted — the control asserts only `.not.toBe(404)` (`_integration.ts:921-929`), which still discriminates the failure mode it exists for (a router answering 404 to everything fails the control; a router answering non-404 to everything fails the probe). The Data-API arm asserts only `status >= 400` — see finding 1.
6. The loop-tier absence arm is a real assertion against `Object.keys(sut)` (`c-membership...test.ts:300-301`), not a comment. No finding.
7. AT-001.32's arms are all present and ordered: Given → refusal → writes-nothing read-back → same-account control, with the operator-provisioned Given stated in both bodies' evidence. No finding.
8. Fixture-vs-SQL comparison — see finding 2. On every *asserted* path the fixture's refusal kinds and their order match the migration exactly (including the measured trigger-before-index order).
9. SQLSTATE classification reads `errno`/`errcode`/`code` with a SQLSTATE-shaped pick, distinct kinds for 42501 (trigger) vs 23505 (index), sentence fallbacks matching the migration's own messages; no 42501/23505 source other than the intended ones exists on either operator statement. No misread path found.
10. `req-001.json` shows exactly .17/.32 green at both tiers (loop 18/19, integration 13/24 — both match the plan's counts); `_pending.ts` lost D3_L1 and D3_L2, header 24→21→19; the ledger's 19 ids match the manifest's `verify:` lines in both directions (including D2.LW adding no id). One stale count inside `_pending.ts` — finding 3.

**Findings**

[1] severity: LOW (an oracle can pass while the claimed fact is false on a plausible input)    `tests/at/suites/req-001/_integration.ts:931-935`
    claim: AT-001.17's Data-API arm asserts only `status >= 400`, so it cannot distinguish privilege denial from table absence or a down PostgREST; the plan's stated mechanism ("the membership table is unreachable through the Data API (F4's privilege layer)") is not what the assertion proves.
    why it matters: a 502/503 from a PostgREST-only outage, or a 404 from a dropped table, satisfies the arm while the "privilege layer denies client reads" fact is false. The residual is small (a PostgREST outage would also fail the at00116/.36 rename bodies in the same run, since `update-organization` reads memberships through PostgREST), but the arm itself, read alone, would pass those states. Asserting 403 specifically — the shape the anon key genuinely gets on an un-granted table — would close it.
    unverified-runtime-claim: no (plain from the assertion; what the anon key answers on the slot is recorded in measured answer (c) for the service role and in F1's no-grant fact for anon).

[2] severity: LOW (latent fixture-vs-database divergence on paths no body drives — invisible to the integration tier)    `tests/at/suites/req-001/_fixture.ts:1084-1120, 1161-1179, 1035-1036`
    claim: the fixture's refusal ORDER differs from the database's on three operator paths, producing different kinds than the live tier would: (a) a grant into a nonexistent organisation with a non-NGO account — fixture answers `refused` (org check first), the database fires the BEFORE trigger first and answers `not-an-ngo-account` (42501); (b) an occupied project seat re-pointed at a nonexistent account — fixture answers `refused` (account check first), the database fires the guard trigger first and answers `seat-occupied`; (c) renaming a nonexistent organisation — fixture answers `refused`, the deployed function's REST read returns an empty set and answers `not-a-member`.
    why it matters: the plan's stated safety net ("A modeling divergence between fixture and database is exactly what the integration tier exists to catch") cannot catch these, because no current body drives the divergent paths — both tiers would stay green over the disagreement, and a future body that does drive one (asserting a kind) would find loop and integration disagreeing. The database order on (a) and (b) is settled by measured answers (b) and (d), so this is a stated divergence, not a guess.
    unverified-runtime-claim: no (both orders are readable statically; the DB trigger-before-FK/index order is recorded in measured answers (b) and (d)).

[3] severity: LOW (declaration-file consistency the plan's own F9 rule treats as load-bearing)    `tests/at/suites/req-001/_pending.ts:10`
    claim: the header sentence "Thirteen are written" contradicts the enumeration in the same paragraph, which names 18 written ids (7 + 2 + 4 + 3 + 2), followed by "The other 19" — 18 + 19 = 37, so the enumeration is the true state and "Thirteen" is a stale pre-slice-1 count.
    why it matters: the plan's F9 discipline makes this file's header count a moving, checked fact ("the header count moves with it", 24 → 21 → 19), and the first line of the same header correctly says 19. A reader cross-checking the header against the ledger or the manifest finds one sentence inside the file disagreeing with both the file's own count line and its own list of ids — the exact drift the rule exists to prevent.
    unverified-runtime-claim: no.

Nothing outside the subject (migration A, `memberships.ts`, `update-organization/index.ts`) presented a defect worth reporting; the slice-1 pieces the slice-2 bodies lean on are consistent with what the bodies assert.

CODE REVIEW: 3 FINDINGS
