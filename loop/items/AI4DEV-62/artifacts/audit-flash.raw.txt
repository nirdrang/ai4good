All checklist lines are now traced against the tree. I have everything I need to write the audit.

---

## AUDIT — AI4DEV-62 (per-org roles and membership isolation) + partner AI4DEV-63

### Method disclosure (required before any box)

**This cage cannot execute git.** I have only read/glob/grep — no shell tool — so I could not run `git diff ea4f345...f5de217 -- src supabase tests .github package.json bun.lockb tsconfig.json vitest.config.ts`. I also attempted to read the worktree's gitdir pointer (`C:\Users\nirdr\Downloads\ai4good\.git\worktrees\agent-aede7117cc1a2b492\HEAD`) to at least confirm the checked-out commit; that path is outside the cage's permitted directories and was refused by the permission layer. The commit pins could therefore not be confirmed from git metadata.

**Fallback used:** every one of the fifteen claimed files was read directly at head, at or around the cited line numbers, and every claim was graded on the head-state content plus the recorded measurements (`artifacts/verify-first-answers.md`, `artifacts/gate2-verify-answers.md`, `artifacts/goal-runs.md`), which I cite and never re-run. Delta-dependent claims (what *changed* versus base `ea4f345`) are graded COULD-NOT-VERIFY where the head state alone cannot settle them; content claims are graded on what I read. This is stated openly so the orchestrator knows which boxes rest on direct reads and which on the missing diff.

### Box 1 — every adopted ruling is implemented as ruled

- **[C1] PASS.** `tests/at/suites/req-001/c-membership-and-acknowledgment.test.ts:53-131` (default body): Given = A via `completeSignup` (admin asserted at :87-89), B operator-created and granted `member` (:78-82), C operator-created unseated (:83); rename A succeeds (:106-108), rename B refuses `not-an-admin` + read-backs prove nothing written (:111-116), rename C refuses `not-a-member` + read-backs (:121-126). Integration `_integration.ts:693-744` (`at00116`): same three-target structure via `twoMembershipGiven` (:646-675), kinds asserted at :727 and :737. Writes-nothing read-backs at both tiers (:115-116, :125-126; :728-729, :738-739).
- **[C2] PASS.** `_contract.ts:585` declaration; `_fixture.ts:1064-1072` — `state.organizations.set` only, no membership insert; `_live.ts:606-612` — single `insert into public.organizations`, no membership insert; `_live.ts:122` in `backedSutMethods.accounts`.
- **[C3] PASS.** `_source-scan.ts`: `routeFileNames()` (:53-65) walks `src/routes/` recursively; `routeTreeLiterals()` (:68-71) reads string literals from `src/routeTree.gen.ts`; the regex `/invite|add[-_ ]?member|addmember|add[-_ ]?user|adduser/i` (:48); assertion `toEqual([])` in the default body (`c-membership-and-acknowledgment.test.ts:309-312`) AND in `at00117` (`_integration.ts:922-925`) — identical at both tiers.
- **[C4] PASS.** `supabase/migrations/20260811130000_single_seat_org_and_single_developer_projects.sql:123` — `revoke all on table public.projects from anon, authenticated, service_role;` verbatim, with the measured rationale comment (:112-122) citing verify-first answer (f).
- **[C5] PASS.** `_fixture.ts:1093-1132`: account absent → `refused` (:1098-1102); account not NGO → `not-an-ngo-account` (:1104-1111); organisation absent → `refused` (:1115-1117); seat held → `org-already-seated` (:1123-1132). Order matches the migration's BEFORE-trigger-before-FK-before-index semantics.
- **[C6] PASS.** `_fixture.ts:1222-1236`: project absent → `refused` (:1223-1224); seat held by a different account → `seat-occupied` (:1225-1233); account absent → `refused` (:1234-1236). The same-account re-write stays allowed (:1225's `!== accountId` condition), matching the guard's `new <> old` condition.
- **[C7] PASS.** `_fixture.ts:1037-1055` — `updateOrganization` contains no organisation-existence pre-check: the only guards are the dead-session check and the membership lookup feeding `orgAdminActionAllowed(membership?.role ?? null)`, so an unknown organisation answers `not-a-member`. The removal condition is cited from `artifacts/gate2-verify-answers.md` v1 (:28-51): the deployed function answered HTTP 403 kind `not-a-member` for the random uuid, with its 200 control — recorded as measured before the removal; I do not re-run it.
- **[C8] PASS.** `_live.ts:210-222` `databaseRefusal` reads `errno`/`errcode`/`code` in order, keeps the first shaped `/^[0-9A-Z]{5}$/`, defaults `code = ''`. Grant catch (:648-660): `/NGO accounts only/i` AND ('' or `42501`) → `not-an-ngo-account`; `/org_memberships_one_seat_per_org_idx/i` AND ('' or `23505`) → `org-already-seated` (the generic duplicate-key half is gone); else `refused`. Seat-assignment catch (:758-765): `/single developer seat/i` AND ('' or `42501`) → `seat-occupied`; else `refused`. The repoint catch (:697-703) applies the same rule.
- **[C9] PASS.** Migration `20260811120000...sql`: `btrim(p_name, E' \t\r\n\f')` at line 137 (emptiness check) and line 141 (`v_name` assignment). Before/after pair cited from `artifacts/gate2-verify-answers.md` v3 (:83-122): before — tab-only `p_name` succeeded (HTTP 200, name `"\t"`); after — SQLSTATE `22023` with the row keeping its prior name. I do not re-run it.
- **[C10] PASS.** `_contract.ts:616` with `RepointMembershipOutcome` (:252-254) = ok / `not-an-ngo-account` / `refused`; `_fixture.ts:1152-1179` in the database's order (no row → `refused`; account absent → `refused`; not NGO → `not-an-ngo-account`; else re-keyed keeping `seated.role`); `_live.ts:679-704` as a single `update public.org_memberships set account_id … where org_id … returning` (zero rows → `refused` :686-688; catch classifies per C8 rule :697-703); name at `_live.ts:124` in `backedSutMethods.accounts`. The AT-001.37 arm exists at BOTH tiers after the control seats the NGO account: default `c-membership-and-acknowledgment.test.ts:273-291` (control grant :273-274, repoint → `not-an-ngo-account` :279-282, read-backs :285-291); integration `_integration.ts:876-893` (same shape).
- **[C11] PASS.** `_integration.ts:950-953` — the known-function control asserts `expect(present.status, ...).toBe(401)`.
- **[C12] PASS.** `_integration.ts:967-970` — Data-API arm asserts `toBe(401)` AND `toMatch(/permission denied/i)`. Cited from `artifacts/gate2-verify-answers.md` v2 (:53-81): the measured answer was HTTP 401 with SQLSTATE 42501 and the permission-denied body — the measured shape, per the ruling's disagreement rule; I do not re-run it.
- **[C13] PASS.** `_pending.ts:10` states "Eighteen are written" and names exactly eighteen ids: .01–.07 (7), .09, .10 (2), .38, .12, .13, .14 (4), .16, .36, .37 (3), .17, .32 (2) = 18; line 13 "The other 19"; header line 2 "There are 19 of them". 18 + 19 = 37.

**Rejected-ruling residuals (nothing to grade, confirmed as recorded):** R1's ordering is present in code — migration `20260811120000...sql:143-146` (existence, 23503) precedes the membership read (:151-154), and execute is revoked from public / granted to `service_role` alone (:197-198). The platform_admin half of R5: neither .37 body drives a `platform_admin` account (verified both bodies use only volunteer and NGO-control accounts), and the migration's own comment (:33-37) is the pin.

### Box 2 — the diff stays inside the declared scope

**Positive half: PASS, by direct read.** All fifteen claimed files exist at head and their content matches the claims: `src/routeTree.gen.ts`, `supabase/config.toml`, `supabase/functions/_shared/memberships.ts`, `supabase/functions/update-organization/index.ts`, both migrations, `tests/at/expected/req-001.json`, `_contract.ts`, `_fixture.ts`, `_integration.ts`, `_live.ts`, `_pending.ts`, `_source-scan.ts`, `c-membership-and-acknowledgment.test.ts`, `f-lifecycle-and-audit.test.ts`. The two migrations are the newest files in `supabase/migrations/` (exactly four migrations total, matching the records' "4 expected, 4 applied"). The manifest declares exactly the five claimed ids green at both tiers (loop 18 green/19 red; integration 13 green/24 red — `req-001.json:4-89`), matching plan step 8 and the recorded goal runs.

**Negative half ("nothing else in the source-only diff"): COULD-NOT-VERIFY.** Without git I cannot enumerate the diff, so I cannot prove no other file under `src supabase tests .github` was touched. What I can state: no new edge function beyond `update-organization` exists in `supabase/functions/` (only `complete-signup`, `create-organization`, `update-organization` + `_shared`), no invite/add-member naming exists anywhere in `src/`, and the record's own gate-2 slices (`610ead7...c2a7b6b`, `c2a7b6b...d0444da`) plus goal-runs.md's statement that `src/routeTree.gen.ts` was the one generated file committed with the record are consistent with the fifteen-file claim. This box needs one `git diff --name-only` run to close.

**Unchanged-file claims:** `.github/workflows/ci.yml`, `package.json`, `tsconfig.json` exist at head. `bun.lockb` and root `vitest.config.ts` do **not** exist anywhere in the tree (the repo carries `bun.lock`; the only `vitest.config.ts` is `tests/at/vitest.config.ts`) — those two claims are vacuously true (an absent file cannot change) but their "unchanged" wording names files that are not in the tree; the same names come from the orchestrator's own instrument, so this is a naming carry-over, not this item's code. Base-comparison of the other three is COULD-NOT-VERIFY without git.

### Box 3 — every stated fact about the code is true

- **[F1] PASS (head state).** `src/routeTree.gen.ts:63-68` is exactly a type-only `declare module '@tanstack/react-start'` block (`interface Register` with type members, no runtime code). The route tree carries only `/` and `__root__` (:20-37); no invite or add-member string appears in the file. The "build-regenerated" provenance is recorded in `artifacts/goal-runs.md` (:9-13) and is delta-dependent; the type-only and no-route facts are verified from content.
- **[F2] PASS (head state).** `supabase/config.toml:480-481` — `[functions.update-organization]` with `verify_jwt = true`; the surrounding comment at :470 says "REQ-001's three edge functions" with exactly three blocks (:474-481). The "adds one block / updated the count" delta is COULD-NOT-VERIFY without git; the head state is true.
- **[F3] PASS.** Migration `20260811120000...sql:197-198` (revoke from public, grant to `service_role`); :143-146 raises 23503 "no such organisation" before the membership read at :151-154 — existence before caller membership, the service-role-only path the rulings record.
- **[F4] PASS.** `org_memberships_grantee_must_be_ngo` trigger at migration `20260811120000...sql:86-89` binds `before insert or update on public.org_memberships`. The claim's mechanism is structurally sound: re-pointing is an UPDATE that changes no row count, so the unique index on `org_id` (insert-only) never sees it — the UPDATE half is the only guard, and the trigger function reads `new.account_id` (:52-54), which is the new account on an update.
- **[F5] PASS.** `org_memberships_one_seat_per_org_idx` on `(org_id)` alone (migration `20260811130000...sql:41-42`); guard trigger `projects_single_developer_seat` at :101-104 raising "its single developer seat is held by account" (:89-92), matching the `/single developer seat/i` classification.
- **[F6] PASS.** `public.projects.assigned_volunteer_id` is one nullable `uuid` column (migration `20260811130000...sql:61`). No product path creates or assigns a project: grep for `projects` across `supabase/functions/` returns nothing, and `src/` contains no project/invite/member references; the only writers anywhere are the operator methods `createProjectAsOperator`/`assignVolunteerAsOperator` at both tiers, and both AT-001.32 bodies (default `f-lifecycle-and-audit.test.ts:78-105`, integration `_integration.ts:1057-1083`) reach the Given exclusively through them.

### Notes outside findings (scope or record matters, mentioned once)

- The `bun.lockb`/root-`vitest.config.ts` naming in the unchanged-files claim does not match the tree's actual files (`bun.lock`; `tests/at/vitest.config.ts`) — pre-existing instrument naming, not this item's code; the claims remain vacuously true.
- The recorded goal runs (`artifacts/goal-runs.md`) carry the execution evidence for the runtime-verified claims (both-tier exit 0 on slot 2, 18/19 and 13/24 counts); I cite them and did not re-run anything.

### Findings

None. Every checklist line graded PASS on the evidence I could gather at head; the only non-PASS verdicts are the delta-dependent halves (Box 2 negative, F1/F2 provenance) which my cage cannot settle without git — method limitation, not defect, and stated with exactly the command that would settle them (`git diff ea4f345...f5de217 --name-only` restricted to the code territory).

AUDIT: CLEAN