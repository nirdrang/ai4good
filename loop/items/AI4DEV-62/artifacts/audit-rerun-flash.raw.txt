All checklist lines have been traced. Writing the audit.

---

# AUDIT — re-run at `4235a2e`, full checklist (far-reaching fix; nothing carried forward)

**Instrument note.** My launch cage gives me no shell, so I cannot execute `git diff 390042c...4235a2e -- src supabase tests .github package.json bun.lock tsconfig.json` (nor `bun run build`, nor `git status`). Every verdict below rests on character-level reading of the tree at the pinned head; every claim whose substance is a diff or a runtime measurement is graded COULD-NOT-VERIFY with the exact command that settles it. No `bun.lockb` and no root `vitest.config.ts` exist (`bun.lock` and `tests/at/vitest.config.ts` are present), so nothing names them.

## Findings

None. Every claim on the checklist that is testable from a snapshot traced true, character for character; the three boxes I could not settle are graded COULD-NOT-VERIFY below, not PASS.

## Box 1 — every adopted ruling is implemented as ruled

- **[C1] PASS.** Loop body `c-membership-and-acknowledgment.test.ts:87-165`: A via product completion (admin), B operator-created then granted `member` (112-116), C operator-created unseated; Given asserted at 121-136 (exactly two memberships, roles `admin`+`member`, none in C). Three rename targets: A succeeds (140-142), B refuses kind `not-an-admin` and writes nothing (145-150), C refuses kind `not-a-member` and writes nothing (155-160), plus no membership written (164). Integration `_integration.ts:713-764` identical shape via `twoMembershipGiven` (666-689: A product, B operator+member grant, C unseated), three targets at 739-763.
- **[C2] PASS.** `createOrganizationAsOperator(name)` at `_contract.ts:602`, `_fixture.ts:1078-1086` (writes organisation row only, no membership insert), `_live.ts:606-612` (single `insert into public.organizations … returning id, name`, no membership insert), and listed in `backedSutMethods.accounts` at `_live.ts:122`.
- **[C3] PASS.** `_source-scan.ts:53-65` enumerates `src/routes/` recursively via `readdirSync`; `routeTreeLiterals` (68-71) reads `src/routeTree.gen.ts` and extracts quoted literals; `INVITE_OR_ADD_MEMBER` regex at 48; hits returned 95-102; both bodies assert `toEqual([])` (`c-membership-and-acknowledgment.test.ts:343-346`, `_integration.ts:942-945`). I ran the regex by hand over the tree's route names (`__root.tsx`, `index.tsx`, `README.md`) and the route-tree literals (`'/'`, `'__root__'`, …) — no match. The arm READS the file; the "no longer CHANGES it" conjunct is a diff claim (see C17).
- **[C4] PASS.** `20260811130000_single_seat_org_and_single_developer_projects.sql:123` — exactly `revoke all on table public.projects from anon, authenticated, service_role;`.
- **[C5] PASS.** `_fixture.ts:1107-1151` in the claimed order: account absent → `refused` (1112-1117); not NGO → `not-an-ngo-account` (1118-1126); organisation absent → `refused` (1129-1131); already seated → `org-already-seated` (1137-1146).
- **[C6] PASS.** `_fixture.ts:1236-1254` in the claimed order: project absent → `refused` (1238); seat held by a different account → `seat-occupied` (1239-1247); account absent → `refused` (1248-1250).
- **[C7] PASS.** `_fixture.ts:1051-1069` contains no organisation-existence pre-check — caller → membership-row lookup → `orgAdminActionAllowed` → name validation → write. The removal is corroborated by the file's own comment (1043-1050) naming gate-2 R2c and the v1 measurement; per instruction I cite that measurement rather than re-run it.
- **[C8] PASS.** Both catch sites match the ruling exactly: `_live.ts:650` `/NGO accounts only/i` AND (no code OR `42501`); `:656` `/org_memberships_one_seat_per_org_idx/i` (the index's own name) AND (no code OR `23505`); `:762` `/single developer seat/i` AND (no code OR `42501`); everything else `refused` (`:659`, `:765`). Sites: grant 648-660, seat assignment 758-766.
- **[C9] PASS.** `20260811125000…sql:146` (`length(btrim(p_name, E' \t\r\n\f')) = 0`) and `:150` (`v_name := btrim(p_name, E' \t\r\n\f')`) — explicit whitespace set in both places; errcode `22023` at 148. The before/after measurement is artifact-cited, not re-run.
- **[C10] PASS.** Contract union `ok`/`refused`/`not-an-ngo-account` at `_contract.ts:269-271`; fixture re-key keeps role (`role: seated.role`, `_fixture.ts:1190`); live is a single `update … returning` (`_live.ts:682-684`), zero rows → `refused` (686-688), catch classifies per C8 (697-703); `backedSutMethods.accounts` lists it (`:124`). AT-001.37's re-point arm exists at BOTH tiers: loop `c-membership-and-acknowledgment.test.ts:313-325` (refuses `not-an-ngo-account`, control row still holds account+role at 319-322), integration `_integration.ts:903-913` (same).
- **[C11] PASS.** `_integration.ts:970-973` — the create-organization control asserts `.toBe(401)`, with the R6 comment at 966-969.
- **[C12] PASS.** `_integration.ts:987-990` — status `.toBe(401)` AND body `.toMatch(/permission denied/i)`.
- **[C13] PASS.** `_pending.ts` line 2 ("There are 16 of them"), line 11 ("Twenty-one are written"), line 10 ("all 37 of them"); the enumeration at 11-15 names exactly 21 ids (7+2+4+3+2+3); 21+16=37; LEAF map (45-67) holds D3_L3, D5_L1, D5_L2, D6_L1, D6_L2, D6_L3 — neither D3_L1, D3_L2 nor D4_L1; line 46 says "SIX LABELS ARE GONE" and lists six (D1.L2, D2.L1, D2.L2, D3.L1, D3.L2, D4.L1).
- **[C14] PASS on the verifiable conjuncts.** The branch's migration is `20260811125000_org_membership_ngo_only_and_organization_rename.sql` (its own header, lines 3-10, tells the stamp-collision story). Five `.sql` migrations exist with five distinct stamps (20260808120000, 20260809090000, 20260811120000, 20260811125000, 20260811130000). The conjunct "main's 20260811120000… is UNCHANGED by this branch" is a diff claim I cannot execute; the file's own prose references only its own item's rulings, so there is no contradicting internal evidence either.
- **[C15] PASS.** Every completion in the declared files that must SUCCEED carries `...SIGNER` (shipped-constant idiom: `authorityAttestation: ACKNOWLEDGMENT_IDENTITY_COPY.authorityStatement` — `c-membership-and-acknowledgment.test.ts:76-80`, `_integration.ts:51-54`, `f-lifecycle-and-audit.test.ts:38-42`), at all loop/integration call sites I traced (loop: 102, 185, 250, 302, 360, 369, 419, 432, 578; integration: 675, 845, 891, 997, 1006, 1050, 1060, 1070, 1130, 1221; lifecycle: 65, 75, 85). The two volunteer-with-organisation completions that must be REFUSED omit it, at exactly the named lines: `c-membership-and-acknowledgment.test.ts:271-275` and `_integration.ts:862-866`. The refusal is `accounts.ts:235` ("a volunteer signup carries no organisation name…"), which precedes the identity checks at 256-290; `accounts.ts:200-206` states that ordering is load-bearing.
- **[C16] PASS.** Header lines 5-9: "SEVEN OF THIS FILE'S EIGHT IDS ARE NOW WRITTEN" — .16, .36, .37, .17, .19, .39, .20 — and "AT-001.18 is the ONE id this file still declares: it belongs to `LEAF.D3_L3`"; confirmed by the call site at line 399 (`notLanded(LEAF.D3_L3)`). Lines 11-14 state the merged truth explicitly and take neither pre-merge side.
- **[C17] COULD-NOT-VERIFY.** The claim is `git diff 390042c...4235a2e -- src` is empty. I have no shell; the snapshot shows `src/routeTree.gen.ts` as a standard TanStack generated file (root + index routes only) with nothing item-specific. Settled by running that exact diff.

## Box 2 — the diff stays inside the declared scope

- **File enumeration: PASS.** All fourteen declared paths exist in the tree: `supabase/config.toml`, `supabase/functions/_shared/memberships.ts`, `supabase/functions/update-organization/index.ts`, both migrations, `tests/at/expected/req-001.json`, and the nine `tests/at/suites/req-001/` files (`_contract`, `_fixture`, `_integration`, `_live`, `_pending`, `_source-scan`, `c-membership-and-acknowledgment.test.ts`, `f-lifecycle-and-audit.test.ts`).
- **"Nothing else in the source-only diff" + "`src/`, `.github`, `package.json`, `bun.lock`, `tsconfig.json` are unchanged": COULD-NOT-VERIFY.** This is the whole substance of the scope box and it is a diff claim. `.github` exists (runner/, workflows/), `package.json`, `bun.lock`, `tsconfig.json` exist; `_bind.ts` and the other suite files (`a-`, `b-`, `d-`, `e-` tests) contain nothing item-specific that I could detect from a snapshot. Settled by `git diff 390042c...4235a2e -- src supabase tests .github package.json bun.lock tsconfig.json` and confirming the changed-file list equals the fourteen.

## Box 3 — every stated fact about the code is true

- **[F1] COULD-NOT-VERIFY.** "Commits no change under `src/`" is the diff claim of C17; "`bun run build` DOES regenerate `src/routeTree.gen.ts` (measured twice, the same ten-line type-only `declare module` block), deliberately NOT committed" and "`bun run typecheck` and the four `at:verify` runs do NOT write that file" are runtime claims. Current state is consistent with the claim (generated-file header lines 1-9, type-only `declare module` block at 42-52). One observation for the executor: the committed block spans lines 42-52, which is 11 lines by my count — if a build regenerates a ten-line block, the regenerated file differs from the committed one, which is exactly what "deliberately NOT committed" predicts; run `bun run build` twice and diff to settle both the count and the regeneration claim.
- **[F2] PASS.** `[functions.update-organization]` with `verify_jwt = true` at `supabase/config.toml:480-481`; the surrounding comment at 470 says "REQ-001's three edge functions", matching the three function blocks present (474-481).
- **[F3] PASS.** `20260811125000…sql:206-207` — revoke from public, grant to `service_role`; a grep across all migrations shows no other `update_organization` execute grant (only `complete_signup`/`create_organization`/`has_platform_acknowledgment` elsewhere), so "service_role alone" holds. Existence check with `23503` and "no such organisation" (152-155) precedes the membership read (160) and role checks (165-177).
- **[F4] PASS.** `20260811125000…sql:95-96` — `create trigger org_memberships_grantee_must_be_ngo before insert or update on public.org_memberships`; the UPDATE half is what a re-point meets.
- **[F5] PASS.** `20260811130000…sql:41-42` — `create unique index org_memberships_one_seat_per_org_idx on public.org_memberships (org_id)` (org_id alone); `:101-104` — `create trigger projects_single_developer_seat before update on public.projects`.
- **[F6] PASS.** `public.projects.assigned_volunteer_id` is ONE nullable column (`20260811130000…sql:61`). No product path creates or assigns a project at either tier: the only writes are the operator methods `createProjectAsOperator`/`assignVolunteerAsOperator` in `_fixture.ts` (state maps) and `_live.ts` (SQL); the remaining hit is `loop/items/AI4DEV-62/verify-first.ts`, a probe in record territory, not a product path.

**Rejected rulings (R1, `platform_admin` half of R5)** — listed in the brief as nothing to grade; I confirm no checklist line asks about them and no code under audit contradicts their residual status.

**Execution evidence.** Per instruction I did not run the suite; the runtime-shaped claims above (C7/C9/C11/C12 measurements, F1 build behaviour) are CI's and the recorded measurement files' subject, and the green `verify` check on `4235a2e` is the citation for them, not this audit.

AUDIT: CLEAN