Commit: `2d88cae402c8a72c9fe2b6835699ff89523c6af2`.

Checks: all seven green at loop. Typecheck, both bijection checks, selftest (33 files, 443 tests), and the three loop verifies matched their expected files (req-004 19 green / 39 red; req-001 33 green / 5 red, unchanged; req-002 20 green / 7 red).

Deviations: switch columns land here as the brief said; the loop reserve twin writes spend as the operator when the actor is verified and inner auth is not; live signs the admin in a second time for a bearer; the three scanner ids call `open()`; the grant-call scanner matches `perform`/`select` only.

Blockers: none at loop. The new route stays 404 until the lead restarts the stack and runs integration.

Report: `loop/items/AI4DEV-132/reports/unit5.md`.