# AUDIT â€” AI4DEV-80 (attribution by spawn tree), FIRST audit

**Seat limitation, stated up front.** I have no shell tool in this launch cage, so `git diff` could not be executed. Everything verifiable by reading and tracing is verified below. The two boxes that depend on enumerating the change-set (`diff stays in declared scope`, and the containment half of G1-1) are graded COULD-NOT-VERIFY, with the exact settling commands. I did corroborate containment in-tree by other means (grep evidence below), but absence of a diff run is absence of the authoritative check.

## Scope box â€” the full-range file list names files ONLY under the declared paths

**COULD-NOT-VERIFY.** Settling command: `git diff ac8a235...<head> --name-only` must list only `loop/work/attribution-report.ps1`, `loop/work/attribution-report.selftest.ps1` and paths under `loop/items/AI4DEV-80/`; plus the additive instrument `git diff ac8a235...<head> -- loop/work` must list exactly the two `loop/work` files. What I verified in-tree instead:
- Both declared code files exist at head and are exactly the subjects described (`loop/work/attribution-report.ps1`, `loop/work/attribution-report.selftest.ps1`).
- The record directory holds only record/evidence files (`loop/items/AI4DEV-80/**` â€” glob enumeration matched the declared set plus `artifacts/`).
- No other `.ps1` in the tree references the report (`attribution-report` grep: 9 hits, all inside the two declared files).
- The new seams (`-ProjectsDir`, `-KimiRoot`, `-CodexSessions`) appear in no file outside the two declared ones (8 hits, all in them).
- The G1-1 clarifying sentence ("a derived SKIP is a floor an orchestrator ruling recorded in PHASE-STATE may tighten to RUN") exists ONLY in `loop/items/AI4DEV-80/` record files â€” it is absent from the process contracts (`.claude/agents/conductor.md`, `.claude/skills/work/WORKFLOW.md`), consistent with "no process contract changed".

## CLAIM CHECKLIST â€” every line, by name

**1. G1-1 â€” filed in words only.** **COULD-NOT-VERIFY** (for the containment half; the filed-in-words half is PASS). The record consistently files the clarification, not builds it: `loop/items/AI4DEV-80/plan.md:32-37`, `gate1-rulings.md:27-36` ("FILED as standalone machinery work, in words, not built here"), `PHASE-STATE.md:92-97` (carried follow-ups item 2). The tree corroborates: the sentence appears nowhere in the process contracts, and no `.ps1` outside the declared pair references the report or its seams. But "the change-set contains NO edit to any file outside the declared scope" is a git-diff fact I could not run; that half is COULD-NOT-VERIFY with the scope-box command above.

**2. G1-2 â€” RED before green.** **PASS.** The selftest at head defines exactly fourteen asserts, A1â€“A14, and nothing else (`Assert-That` grep: 14 hits, `loop/work/attribution-report.selftest.ps1:332,343,351,360,371,379,389,399,408,421,435,447,455,469`). The committed logs name exactly those ids: `loop/items/AI4DEV-80/selftest-red.txt:13-26` shows PASS only on A4, A8, A14, eleven FAIL, "EXIT CODE: 1" (line 29); `selftest-green.txt:7-20` shows all fourteen PASS, "EXIT CODE: 0" (line 24). The RED method claim (report restored from `0c1bbf7`, selftest at `c20d6c9`, head file restored after) is documented at `selftest-red.txt:4-9`; per the brief I cite the logs rather than re-run them.

**3. G1-3 â€” the background file cannot count.** **PASS.** Fixture: `$tasksDir = Join-Path $sessionDir 'tasks'` with `$sessionDir = Join-Path $projects $sessionId` (`selftest.ps1:121-124`), so `bg.output` sits INSIDE the fixture projects root; three transcript-shaped lines written at `selftest.ps1:240-247`. Report scan set: top-level `-Filter '*.jsonl'` non-recursive (`attribution-report.ps1:62`) plus `agent-*.jsonl` recursive under `subagents` only (`:79-82`) â€” `.output` can match neither filter and `tasks/` is not scanned at all, so it can appear in no row. A3's expected totals are computed by measuring every `.jsonl` under `$projects` (`selftest.ps1:292-299`), never hard-coded, with `$mBg.Responses -gt 0` guarding that the fixture planted the lines (`:352`).

**4. G1-4 â€” ambiguity degrades.** **PASS.** `Get-TreeItem` (`attribution-report.ps1:267-275`): `$own.Count -eq 1` â†’ own item; `-eq 0` â†’ parent walk; two or more â†’ `$result` stays `''` â€” the walk returns nothing and the parent branch is unreachable. The counting loop's chain (`:368-373`) then puts M1's branchless lines in `unattributed|none` (branch `''`, tree `''`, stamp guarded by `-not $isAgent`). A9 (`selftest.ps1:407-410`) asserts both halves: unattributed row = M1None+X1, branch rows = C1+M1A under 901 and M1B under 902. The fixture plants the hazard with meta parentAgentId=C1 (`selftest.ps1:226-231`), so a wrongful inheritance would be caught.

**5. G1-5 â€” no depth cap.** **PASS.** `Get-TreeItem` (`attribution-report.ps1:261-279`) recurses on `parentAgentId` with `$visited` passed through the recursion, `$visited.ContainsKey($agentId)` returning `''` on a cycle (`:263`), and marks visited before recursing (`:264`). There is no depth counter anywhere in the function; the visited set is the only guard.

**6. G1-6 â€” inheritance is per spawn call.** **PASS.** Pre-pass (`attribution-report.ps1:218-243`): per line, `$cb`/`$cs` track the session's held branch/stamp; on a `toolu_` id, the key is `$sessId + '|' + $id` (`:232`) and the value is what the session held at THAT line â€” branch first, stamp fallback (`:236-241`), first sighting wins (`:235`). An agent's session is derived from its directory when the meta is read (`:123`). Fixture: sessionA (stamp 901) spawns C1 and U1, then sessionB (stamp 902) spawns U2 (`selftest.ps1:144-160`); U1/U2 metas carry distinct `toolUseId`s (`:203,207`). A7 (`selftest.ps1:387-391`) asserts U1â†’901 and U2â†’902 â€” I traced the keys: `...|toolu_01FixtureUTILONE` is captured under stamp 901, `...|toolu_01FixtureUTILTWO` under stamp 902; latest-wins and file-wide-single both fail these expected values.

**7. G1-7 â€” vendor tables in JSON.** **PASS.** `-Json` emits `codexRows = $vRows` and `kimiRows = $kRows` (`attribution-report.ps1:557-558`). The join key is bare: `$joinKey = $base -replace '^agent-', ''` (`:329`), `$agentItem[$joinKey]` fed at `:384`/`:388`, kimi side extracts the bare id from `wd_agent-<id>_` (`:487-491`). Fixture `wd_agent-O1_ab12cd34\...\wire.jsonl` (`selftest.ps1:260`); A10 asserts exactly one kimi row whose `OutputTok` = 50+70 = `$kimiExpectedOut` (`:417-423`).

**8. G1-8 â€” the unattributed row is exact.** **PASS.** A11 (`selftest.ps1:429-437`) computes `$expUnattResp`/`$expUnattOut` from `$mM1None` + `$mX1` (measured from the fixture's own bytes), asserts the row (`:436`), and recomputes the percentage with the report's own rounding shape `[math]::Round(100.0 * unatt / total, 1)` (`selftest.ps1:431` vs `attribution-report.ps1:516`), comparing both the JSON `unattributedPct` and the regex-extracted printed value (`selftest.ps1:433-434`).

**9. G1-9 â€” the switches are protected.** **PASS.** A12 (`selftest.ps1:442-449`): `-Json -Item AI4DEV-902` rows all carry Item 902, and the sum equals U2 + sessionB + M1B (I traced the 902 rows: branch=M1B, tree=U2, stamp=sessionB â†’ 3 = 1+1+1; the report's `-Item` filter at `attribution-report.ps1:520-523` leaves exactly these). A13: E1's `LastWriteTime` set 40 days back (`selftest.ps1:197`), `-Days 7` filter at `attribution-report.ps1:83`; A13 asserts days7 = total âˆ’ E1 and default = total (`selftest.ps1:454-457`). Meta files: the meta read loop (`attribution-report.ps1:108-127`) runs unconditionally â€” no `-Days` dependency.

**10. G1-10 â€” escaped decoys never resolve.** **PASS.** `$branchRe = [regex]'"gitBranch":"([^"]*)"'` (`attribution-report.ps1:136`), character by character: `"` `gitBranch` `"` `:` `"` capture `[^"]*` `"`. The fixture decoy is `\"gitBranch\":\"nirdrang/ai4dev-999-decoy\"` (`selftest.ps1:56-58`, planted at `:174`); in the raw JSONL the sequence after `"gitBranch"` is `\`, `"`, `:`, `\` â€” the required `":"` is broken by the backslashes, so no match at the decoy; the only successful match in O1's decoy line is the outer unescaped `"gitBranch":""` (empty value â†’ 0 items), and the pre-pass/counting-loop guards (`attribution-report.ps1:211-214, 348-350`) operate on the same raw-line match. A14 (`selftest.ps1:462-471`) checks rows, roleRows, rollRows, codexRows, kimiRows and the printed text for AI4DEV-999; the only occurrence of `AI4DEV-999` in code territory is the `$DECOY` constant itself (`selftest.ps1:133`, grep-confirmed).

**11. D-1 â€” the scan is recursive.** **PASS.** `attribution-report.ps1:81`: `Get-ChildItem $sub -Filter 'agent-*.jsonl' -File -Recurse` â€” recursive, so `subagents\workflows\wf_1\agent-W1.jsonl` is scanned; meta files cannot match (`agent-*.meta.json` ends `.json`, not `.jsonl`) and `tasks\*.output` is outside both filters and roots; top-level `*.jsonl` glob is non-recursive (`:62`), so no file is counted twice. W1 fixture at `selftest.ps1:216-218` with its meta beside it; W1 joins A1's tree expectation (`selftest.ps1:328`) and A3's totals (`:292-299`). The mutation check (flat scan â†’ A1/A3/A13 red, `selftest-green.txt:43-47`) confirms the suite binds on recursion.

**12. G2-1 â€” the stamp fallback is session-only.** **PASS.** The fallback site is `elseif ($curStamp -and (-not $isAgent))` (`attribution-report.ps1:372`); `$curStamp` is still tracked for agent files (`:352`) but can no longer attribute one. M1's fixture carries the 902 stamp line BEFORE its branchless lines (`selftest.ps1:226-230`). The committed mutation check (`selftest-green.txt:32-36`) shows removing the guard turns A1, A2, A9, A11, A12 red â€” a superset of the checklist's A1/A9/A11, so the checklist's claim is consistent.

**13. G2-2 â€” the vendor join degrades on ambiguity.** **PASS.** `$curAmbiguous` computed from `$fileItems[$joinKey]` count > 1 (`attribution-report.ps1:335`); the branch-fed assignment is guarded (`:384`); the tree-fed assignment (`:388`) needs no guard because an ambiguous agent's `Get-TreeItem` returns `''` (verified in G1-4), so `$curTree` is falsy and `:386` is unreachable. Fixture `wd_agent-M1_ef56ab78\...\wire.jsonl` with distinctive 4444/5555 amounts (`selftest.ps1:266-271`); A10 asserts exactly one kimi row with the O1 sum (`:417-423`); mutation check confirms removing the guard turns exactly A10 red (`selftest-green.txt:38-41`).

**14. G2-3 â€” the spawn-context key carries the session (proven by measurement).** **PASS.** Key construction `$sessId + '|' + $m.Groups[1].Value` (`attribution-report.ps1:232`), `$sessId` = session file base name (`:223`), agent session derived from the directory at meta-read time (`session = $d.Name`, `:123`), first-sighting-wins within the key (`:235`) â€” a cross-session copy produces a different key, so it can never answer for another session. The committed probe (`loop/items/AI4DEV-80/artifacts/g2-3-probe.txt`) documents: 580 duplicated `toolu_` ids in exactly one file pair (`:48`), 5 of them resolving different items (`:53-60`), 7/7 subagents directories paired with session files (`:62-66`), and the same 7 root agents resolving under both keys (`:68-72`). Cited, not re-measured.

**15. G2-5 â€” A1 asserts tokens, not only counts.** **PASS.** `$a1TokOk` (`selftest.ps1:331`) asserts `OutputTok` on all four rows against `$expABranchOut = $mC1 + $mM1A` (`:327`), `$expATreeOut = $mO1+$mO2+$mE1+$mU1+$mW1` (`:329`) and the measured segment sums â€” all from `Measure-File`/`Measure-Lines` on the fixture's own bytes, nothing hard-coded.

**16. G2-6 â€” disproven, so nothing changed.** **PASS.** The print site `'COORDINATOR SIGNAL - unattributed share of output tokens: ' + $pct + '%'` (`attribution-report.ps1:599`) carries no culture-forcing; the selftest's decimal assertion is the dot-decimal regex `([0-9]+(?:\.[0-9]+)?)%` (`selftest.ps1:433`), the same shape the gate-2 ruling quotes as pre-existing (`gate2-rulings.md:110`). The committed probe (`artifacts/g2-6-probe.txt`) shows the exact print shape and `ConvertTo-Json` emitting `66.7` under `de-DE` with a live-culture control emitting `66,7` (`:19-22`), plus the real report in-process under `de-DE` printing `67.5` (`:43`). Cited, not re-measured.

**17. Stated code facts.**
- (a) **PASS.** No `.output` scan exists at head â€” the scan set is `*.jsonl`/`agent-*.jsonl` only (`attribution-report.ps1:62,79-82`), and the pairing pass exists only as a comment describing "an earlier version" (`:100-102`). The RED log independently documents the deleted scan in the S3-era code (`selftest-red.txt:41-48`, hard-coded `Temp\claude\...` `$agentRoot`).
- (b) **PASS.** `$curRole = $metaOf[$joinKey].agentType`, else `'unmarked agent'` with a counter (`attribution-report.ps1:330-331`); announced in the floor note (`:605`).
- (c) **PASS.** Resolution order at `attribution-report.ps1:368-373`: branch (1 item) â†’ `unresolved` (2+ items on one branch) â†’ tree â†’ stamp (`-not $isAgent`) â†’ unattributed.
- (d) **PASS.** Parameters at `attribution-report.ps1:13-22` default to the real-machine roots (projects key, attr cache, repo `loop\items`, `.codex\sessions`, `.kimi-code\sessions`); the RED capture demonstrates the S3-era report already accepted these overrides with identical names (`selftest-red.txt:6-9`), and the only un-overridable seam in the old code was the deleted Temp `.output` path (`selftest-red.txt:41-48`). `-Json` emits rows/roleRows/rollRows/codexRows/kimiRows/totals (`:552-571`).

## Findings

**None.** No checklist line is false against the code; every traced mechanism matches its claim, and the committed evidence (RED/GREEN logs, both probes, before/after captures â€” the latter corroborating the delta note's 479â†’919 files, 26352â†’49071 responses, 70.6%â†’67.6% with both denominators, and codex/kimi tables present in both) is consistent with what the code at head would produce. Two boxes are COULD-NOT-VERIFY rather than PASS â€” the scope box and G1-1's containment half â€” both solely because this seat cannot run `git diff`; the settling commands are given above, and every in-tree corroboration I could gather points the same way.

Outside findings, one pre-existing exposure noted once: the record itself already files, in words, the pre-existing session-file stamp-corruption exposure (quoted stamp in a tool result corrupting `$curStamp`) as belonging to another item (`gate2-rulings.md:212-224`, `PHASE-STATE.md:98-100`); I saw no code evidence contradicting that it predates this branch, and it is not this item's defect.

AUDIT: 0 FINDINGS
