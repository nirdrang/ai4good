AI4DEV-19 (planted markers, forced failures) — the GREEN half of the red-then-green proof.

CAPTURED AGAINST COMMIT 6c8fba8 ("AI4DEV-19: point the widened oracle at its own falsification, the way
the tightening already does"), the last code commit of the Gate 2 fix round, with `git diff` against that
commit empty. Same test, same tier, same commands as proof-red.txt. The ONLY difference is that the
transition and the event write commit as one rollback unit again: a crash at
notifications.between_transition_and_event_write puts the transition back, so nothing commits.

WHAT THIS ROUND CHANGED ABOUT THIS FILE. Gate 2 (terra, finding 4) found that its previous version
said "the gate is the --expect run, which is in the report" while containing no --expect transcript
at all — a file claiming content it did not have, which is worse than saying nothing, because a
reader who finds the claim stops looking. The --expect run is now section 2 below, in this file,
rather than referred to. The previous version is not preserved underneath: unlike proof-red.txt's
superseded sections, everything it contained is still exactly reproducible from the current tree and
is reproduced in section 1, so nothing is lost by replacing it — what was removed is the sentence
that was false.

10 green / 2 red is the design's final partition (AT-016.01 the pending static provider scan,
AT-016.11 the pending email provider simulator).

HOW THESE TRANSCRIPTS WERE CAPTURED. stdout and stderr were redirected to separate files rather than
merged: PowerShell wraps a native command's stderr in ErrorRecords when the two are merged, which
injects its own "NativeCommandError" text into the middle of a transcript. Both streams are
reproduced below under their own headings.

=================================================================================================
SECTION 1 — the plain command. exit 1, and that is expected: without --expect the runner has no
declaration to compare against, so it treats any red id as a failure and says so. "FAILURE: 2 ids
red" is this view's opinion, not the gate's.
=================================================================================================
    bun run at:verify req-016 --tier loop

--- stdout ---
JSON report written to C:/Users/nirdr/AppData/Local/Temp/at-verify-GboGty/vitest-report.json

at:verify req-016 --tier loop
  AT-016.01    red      CapabilityPending: CAPABILITY PENDING — H3 static provider scan, H5 email provider simulator
  AT-016.02    green    registered events equal the taxonomy exactly, are immutable, and carry no CR/scope-change event
  AT-016.03    green    every taxonomy row delivers to exactly its recipients on exactly its channels, with the named payloads
  AT-016.04    green    sensitive negatives: no candidacy to the NGO, no vetting outcome to the volunteer, no donation event
  AT-016.05    green    every critical class goes out by email; the low-tone event is in-app only
  AT-016.06    green    a documented delivery default exists for every taxonomy row
  AT-016.07    green    one logical event per committed event, one delivery per recipient-channel pair, across a restart
  AT-016.08    green    a comment burst delivers the count the pinned anti-spam configuration prescribes, on two different configurations
  AT-016.09    green    every guarded transition writes its notification event atomically under an induced fault
  AT-016.10    green    recipients resolve at event creation: the old holder receives, the new holder is excluded
  AT-016.11    red      CapabilityPending: CAPABILITY PENDING — H5 email provider simulator
  AT-016.12    green    an escalation-tier event notifies both the NGO and the platform admin
  12 P0: 10 green, 2 red, 0 missing
  FAILURE: 2 ids red
  FAILURE: the test process exited 1
--- stderr ---
$ bun tests/at/harness/runner.ts "req-016" --tier loop
error: script "at:verify" exited with code 1

=================================================================================================
SECTION 2 — THE GATE. exit 0.

This is the run that decides, and the one the previous version of this file promised and did not
contain. It compares the observed partition against tests/at/expected/req-016.json id by id: a
capability-pending red is matched by whole-string equality on a line rebuilt from the declaration,
so a stale or reordered capability list fails, and the raw counts are compared as well, so leaving a
green declared red fails on arithmetic alone.
=================================================================================================
    bun run at:verify req-016 --tier loop --expect

--- stdout ---
JSON report written to C:/Users/nirdr/AppData/Local/Temp/at-verify-ar6VTz/vitest-report.json

at:verify req-016 --tier loop
  AT-016.01    red      CapabilityPending: CAPABILITY PENDING — H3 static provider scan, H5 email provider simulator
  AT-016.02    green    registered events equal the taxonomy exactly, are immutable, and carry no CR/scope-change event
  AT-016.03    green    every taxonomy row delivers to exactly its recipients on exactly its channels, with the named payloads
  AT-016.04    green    sensitive negatives: no candidacy to the NGO, no vetting outcome to the volunteer, no donation event
  AT-016.05    green    every critical class goes out by email; the low-tone event is in-app only
  AT-016.06    green    a documented delivery default exists for every taxonomy row
  AT-016.07    green    one logical event per committed event, one delivery per recipient-channel pair, across a restart
  AT-016.08    green    a comment burst delivers the count the pinned anti-spam configuration prescribes, on two different configurations
  AT-016.09    green    every guarded transition writes its notification event atomically under an induced fault
  AT-016.10    green    recipients resolve at event creation: the old holder receives, the new holder is excluded
  AT-016.11    red      CapabilityPending: CAPABILITY PENDING — H5 email provider simulator
  AT-016.12    green    an escalation-tier event notifies both the NGO and the platform admin
  12 P0: 10 green, 2 red, 0 missing
  EXPECTED: the run matches C:\Users\nirdr\Downloads\ai4good\.claude\worktrees\agent-adf5c7b803767a1c3\tests\at\expected\req-016.json exactly (10 declared green, 2 declared red)
--- stderr ---
$ bun tests/at/harness/runner.ts "req-016" --tier loop --expect

=================================================================================================
THE COMPANION PROOFS, so that nobody has to guess which file answers which question.

  proof-red.txt      this pair's red half: the rollback reverted, AT-016.09 failing on atomicity.
  proof-oracle.txt   whether AT-016.09's oracle is strong enough to notice — the fault point moved
                     after all the writes, the widened oracle failing and the old one passing.
  proof-restart.txt  whether AT-016.07's restart is causal — the delivery path's read of the
                     process identity broken, and the test failing because of it.
=================================================================================================
