# Unit 4 brief: the interrogate fixes (writer: the feature lane)

Worktree: C:\Users\nirdr\Downloads\ai4good\.claude\worktrees\AI4DEV-87, branch nirdrang/ai4dev-87-the-acceptance-harness-shrinks-to-the-per-id-gate-over-the, head 3141206. You write in this worktree and commit on this branch. Nobody else writes while you do. Use PowerShell for every command. Read `loop/items/AI4DEV-87/artifacts/interrogate/rulings.md` first: its "Act on" list, items 1 to 9, is your contract. The reviews it rules on are beside it (`review-fable.md`, `review-grok.md`, `review-opus.md`); read the findings the items cite for their exact locations and reasoning. Then read `loop/items/AI4DEV-87/artifacts/design.md`.

## The pin (must not change)
- The three manifests under `tests/at/expected/` are not edited. `bun run at:verify req-001 --tier loop --expect`, `req-016 --tier loop --expect`, and `req-001 --tier integration --expect` (16 green, 21 red) match exactly.
- `bun run typecheck` clean, now over three projects. `bun run at:selftest` green.
- The drive `bun .claude/skills/verify-ai4good/scripts/drive-ngo-signup.ts loop/items/AI4DEV-87/artifacts/units/unit-4-drive` passes 13 of 13 (the eleven checks as before plus a2 and a3 in the Doctor step) and exits 0.

## Tests first, then the change, item by item
1. Item 1. In `tests/at/harness/conformance.selftest.ts` add the four assertions rulings item 1 names before you touch `index.ts` or `registry.ts`; watch them fail; then implement. `openWorld` throws the refusal before `createHarness`. `createHarness` above loop with no live adapter throws. `live` leaves `AtHarness` and `TierHarness`. `aboveLoopStandInRefusal(tier, live, sutKey)` keeps its signature; the boolean now comes from `liveAdapterExists`.
2. Item 2. Add `stackFromLocalStatus(repoRoot)` to `local-stack.ts` and one selftest that feeds it a status through the existing `runSupabaseCli` seam the way `local-stack.selftest.ts` already stubs CLI results (read how `identityVerdict` is tested there and follow it; if the seam cannot be stubbed without a new parameter, test the pure mapping from `StackStatus` to `Stack` and say so in the report). Delete `stackFromStatus`, `parseStatusJson`, `stackFromStatusJson` and their selftest blocks. The drive imports `stackFromLocalStatus` from `../../../../tests/at/harness/local-stack.ts`.
3. Item 3. `STACK_ENV` in `live-stack.ts`; `childCoordinates` writes through it; `stackFromEnv` refuses naming the missing variable for the four mandatory coordinates; one selftest for the refusal and one asserting `childCoordinates` and `stackFromEnv` round-trip through the same names.
4. Item 4. `authPost` and `functionPost` return `{ url, status, json }`; `readJson` exported; the drive's `recordHttp` takes the returned url; delete `const api` and the rebuilt URLs. Doctor checks a2 (mail identification) and a3 (the edge runtime mount, `docker inspect`, compare the Source ending in `supabase/functions` against `repoRoot` after normalising slashes and case; fail with the remedy). Keep the other eleven check ids and titles and the transcript shape.
5. Item 5. `.claude/skills/verify-ai4good/SKILL.md`: the Drive recipe describes what `live-stack.ts` sends (Bearer anon on every Auth post, `GET /api/v1/search?query=to:<address>&limit=50` then `GET /api/v1/message/{ID}/raw`, quoted-printable decoded, `type=` filter, a 20-second poll at 250 ms); the Doctor section says the drive runs a2 and a3 itself and keeps the manual commands for a custom drive.
6. Item 6. `.claude/skills/verify-ai4good/scripts/tsconfig.json` (extends `../../../../tests/at/tsconfig.json`, `include: ["*.ts"]`, `exclude: []`); a third row in `PROJECTS` in `tests/at/typecheck.ts` labelled `verify drive`; replace `import.meta.dir` in the drive with `fileURLToPath(new URL('.', import.meta.url))`. `bun run typecheck` must run all three and stay clean. Update the comment in `typecheck.ts` that says "both configs".
7. Item 7. The prose sweep, every site rulings item 7 lists, with the exact replacement it names. No other prose changes.
8. Item 8. The three nits: the preflight describe back to `runner.selftest.ts`; the two selftests import from `local-stack.ts` and the re-export goes; `tests/at/harness/pending.ts` holds `PendingPhase`, `AtPending`, `CapabilityPending` verbatim, `registry.ts` re-exports them (`export { AtPending, CapabilityPending } from './pending.ts'; export type { PendingPhase } from './pending.ts';`) and imports what it uses; `index.ts` and `_live.ts` import from `./pending.ts`.
9. Item 9. `tests/at/harness/stack-lock.ts` and `stack-lock.selftest.ts`, verbatim moves of the lock section and its fifteen tests; `stackLockPath` takes `{ projectId, apiPort }`; `local-stack.ts` imports what it needs from `stack-lock.ts`; no import goes back.

## Files you may touch
`tests/at/harness/`: `index.ts`, `registry.ts`, `contracts.ts`, `pending.ts` (new), `live-stack.ts`, `live-stack.selftest.ts`, `local-stack.ts`, `local-stack.selftest.ts`, `stack-lock.ts` (new), `stack-lock.selftest.ts` (new), `runner.ts`, `runner.selftest.ts`, `runner-blackbox.selftest.ts`, `runner-expect.selftest.ts`, `conformance.selftest.ts`, `suite-adapters.ts`; `tests/at/typecheck.ts`; `tests/at/README.md` (one sentence for the third project and one for the lock module); `tests/at/suites/req-001/_live.ts`, `_integration.ts` (line 1241 prose only), `_fixture.ts` (line 7 prose only); `tests/at/suites/req-016/_fixture.ts` (line 25 prose only); `supabase/functions/_shared/accounts.ts` (line 13 prose only); `.claude/skills/verify-ai4good/scripts/drive-ngo-signup.ts`, `scripts/tsconfig.json` (new), `SKILL.md`; `loop/parked/v1/README.md` (the live-email sentence); `loop/items/AI4DEV-87/artifacts/design.md` (section 1, the corrected sentence). Nothing else. Not `tests/at/expected/`, not any `*.test.ts`, not CI.

## Order of work
Items 1, 8, 9, 2, 3 (harness), then `bun run typecheck` and `bun run at:selftest` green; both loop `--expect` match; then items 4, 5, 6 (the drive and the skill), `bun run typecheck` over three projects; then item 7; then `bun run at:verify req-001 --tier integration --expect` (16 green, 21 red; if the lock is held wait two minutes and try once more; on an infrastructure refusal stop and report, do not restart the stack); then the drive, 13 of 13.

Commit ONCE with `git commit -F <file>`. First line exactly: `AI4DEV-87: the interrogate fixes: liveness before construction, one CLI seam, one name map, the drive checked and typed`. Body: the nine items by number, one line each. Trailers `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01SikdZcn3PmB9SrZ4dL1ziT`. Do not push. Stage nothing under `loop/items/`; the lead commits the item record, including your edit to `design.md`.

## STOP rule
If an item cannot end green, or needs a file outside the touch list, stop, write the report, and say which item and why. Do not edit a manifest.

## Report
Write `loop/items/AI4DEV-87/artifacts/units/unit-4-report.md`: the commit SHA; the nine items with one line each on what landed; every file created, moved, edited; the pasted last 15 lines of typecheck, at:selftest, the three at:verify runs, and the drive (redact JWT-shaped strings and `sb_` keys); line counts of `local-stack.ts`, `stack-lock.ts`, `live-stack.ts` after; every deviation and why. Then reply with exactly five lines: the commit SHA; the selftest count; the three at:verify results and the drive result; deviations (a number, and "none" when zero); the report path.
