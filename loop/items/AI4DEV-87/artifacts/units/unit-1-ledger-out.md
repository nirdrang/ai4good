# Unit 1 brief: the provenance ledger, the brand, the attestation, and the type probes leave (writer: the feature lane)

Worktree: C:\Users\nirdr\Downloads\ai4good\.claude\worktrees\AI4DEV-87, branch nirdrang/ai4dev-87-the-acceptance-harness-shrinks-to-the-per-id-gate-over-the. You write in this worktree and commit on this branch. Nobody else writes while you do. Use PowerShell for every command. `bun install` has run; the pinned tsc, vitest, and supabase CLI are under node_modules.

## The design you implement
Read `loop/items/AI4DEV-87/artifacts/design.md` in full. Section 1 is your contract; section 2 and 3 are later units and you do not start them. Read `loop/items/AI4DEV-86/artifacts/how/explanation.md` sections 6 to 9 for how the ledger threads through the harness today. Then read every file in the touch list below before you edit any of them.

## The behavior that must not change (the pin)
- `bun run at:verify req-001 --tier loop --expect` and `bun run at:verify req-016 --tier loop --expect` match their manifests exactly. The manifests under `tests/at/expected/` are not edited.
- `bun run at:verify req-001 --tier integration --expect` matches: 16 green, 21 red, with the same red texts as today (`CapabilityPending: CAPABILITY PENDING — sut.accounts.registerWithGithub` and the like, and `AtPending: <id> PENDING [sut-missing] — ...`).
- `bun run typecheck` clean. `bun run at:selftest` green.

## Tests first
Before any structural edit, add to `tests/at/harness/conformance.selftest.ts`, inside the describe `the five false-green reproductions`, one test named `refuses to grade a stand-in above the loop tier, by name`: it imports `aboveLoopStandInRefusal` from `./registry.ts` and asserts that `aboveLoopStandInRefusal('integration', false, 'notifications')` is a `CapabilityPending` whose `capabilities` equal `['fixtures.worlds', 'sut.notifications']` and whose `message` equals `CAPABILITY PENDING — fixtures.worlds, sut.notifications` (the dash is U+2014, copy it from the existing class), that `aboveLoopStandInRefusal('integration', true, 'accounts')` is null, and that `aboveLoopStandInRefusal('loop', false, 'accounts')` is null. Run `bun run at:selftest` and watch it fail on the missing export. Then build.

## What you park (git mv, byte-identical, into loop/parked/v1/ at the same relative path)
- `tests/at/harness/capabilities.ts`
- `tests/at/harness/attestation.ts`
- `tests/at/harness/live-ledger.selftest.ts`
- `tests/at/harness/type-invention.selftest.ts`
- `tests/at/typeprobes/` (the whole directory: three probe files and three tsconfig files)

## What you edit
1. `tests/at/harness/registry.ts`: add `export class CapabilityPending extends Error` beside `AtPending`, copied byte-for-byte from `capabilities.ts` lines 488 to 493 (the message text is what `expected.ts` rebuilds). Replace `aboveLoopStubbedRefusal(tier, stubbed)` with `aboveLoopStandInRefusal(tier, live, sutKey)` as `design.md` section 1 writes it, and call it in `openWorld` with `h.live` in place of the `stubbedCapabilities()` await. Remove the import from `./capabilities.ts`. Rewrite the comment above the call so it says what the code now does: above loop, a harness that did not load a live adapter is refused before the body runs, naming the two members the loop fixture supplies. Trim the `harness-missing` detail text to name the files that exist.
2. `tests/at/harness/index.ts`: rewrite. No `Capability<T>`, no witness, no ledger type, no attestation, no `LiveAttestation`. Keep: `FixtureAdapter`, `FixtureAdapterModule`, `adapterUrl`, `loadAdapter` with its requirement check, `LiveAdapterModule` (without `backedSutMethods`), `LiveSlotCoordinates`, `liveAdapterUrl`, `loadLiveAdapterModule` (without the `backedSutMethods` check), `liveCoordinatesFromEnv`, `createHarness`. Move `pendingCapability` here as `refusing<T extends object>(...names: string[]): T`, same Proxy, same `CapabilityPending` (imported from `./registry.ts`). `createHarness` at loop builds the same members as today as plain values and sets `live: false`. Above loop: read the coordinates; if the live module loads, call `createLiveAdapter({ slot, vendors: await createLiveEmail({ catcherUrl }), config, worlds })`, clock `new RealClock()`, `live: true`; if it does not exist, load the loop fixture exactly as the loop path does, clock `new RealClock()`, vendors from the sim, and `live: false`. `static` is `refusing<StaticScan>('H3 static provider scan')` at every tier. Delete every comment that explains the ledger; keep the comments that explain the loader's requirement check and the SUT prefix.
3. `tests/at/harness/contracts.ts`: in `AtHarness`, replace `stubbedCapabilities(): Promise<string[]>` with `live: boolean` and a three-line comment: true only above loop when the suite's `_live.ts` loaded; `registry.ts` refuses every `open()` above loop while it is false; nothing else reads it. Delete the sentences in this file's doc blocks that name `capabilities.ts`, the witness, the ledger, or `pendingCapability` (keep the type-alias-not-interface rule and its reason; drop the sentence that names `tests/at/typeprobes/` and `type-invention.selftest.ts`).
4. `tests/at/harness/clock.ts`: `AttestedRealClock` becomes `export class RealClock` with `now()` only; delete `createAttestedRealClock` and the import from `./capabilities.ts`; rewrite the class comment to two sentences (the integration tier's clock; a different class so the per-tier context type has no `advance` or `freezeAt` to reach). Update the `RealClock` import in `contracts.ts` if the name there differs.
5. `tests/at/harness/live-email.ts`: `createLiveEmail({ catcherUrl })` takes no attestation and stamps nothing; keep the Mailpit identification probe (`/api/v1/info`) and every refusal about it; delete the import from `./capabilities.ts` and the brand refusal; rewrite the header so it no longer claims a grant on a brand.
6. `tests/at/harness/runner.ts`: delete the import from `./attestation.ts`, the nonce mint and write in `prepareLocalStack`, the `nonce` member of `PreparedStack`, and the `ATTESTATION_ENV` entry in `childCoordinates`. The header's steps 2 and 4 and the `prepareLocalStack` doc block no longer mention the attestation write or the nonce; the `StackIdentityRead` doc no longer names `attestation.ts` as a taker. Everything else in the runner stays as it is; unit 3 moves it.
7. `tests/at/harness/runner.selftest.ts`: delete the import from `./attestation.ts` and the assertions on `ATTESTATION_ENV` (around lines 360 to 370); the five `AT_SUPABASE_*` names remain asserted. Fix comments that name the attestation write (around lines 235 and 284).
8. `tests/at/harness/conformance.selftest.ts`: delete the test `derives stand-in provenance from the constructor, not capability self-report`, the describe `provenance is computed from the value or the loader, and refuses what it cannot classify`, and the describe `the harness reports its own provenance honestly`; fix the imports (no `capabilities.ts`, no `buildCapabilityLedger`). Every other test stays.
9. `tests/at/harness/vendors.selftest.ts`: import `CapabilityPending` from `./registry.ts`; delete the `stubbedCapabilities()` assertion around line 271 and its comment; keep the `h.static` refusal assertion.
10. `tests/at/harness/runner-expect.selftest.ts`: the generated probe suite imports `CapabilityPending` from the registry's file URL (there is already a registry URL in that file; reuse it) instead of `CAPABILITIES_URL`; delete `CAPABILITIES_URL`.
11. `tests/at/harness/expected.ts` and `expected.selftest.ts`: prose only. Where they say the error classes live in `capabilities.ts`, say `registry.ts`.
12. `tests/at/suites/req-001/_integration.ts`: import `CapabilityPending` from `../../harness/registry.ts`. Nothing else in this file changes.
13. `tests/at/suites/req-001/_live.ts`: keep `createLiveAdapter`'s signature as it is in this unit (unit 2 reshapes it). Delete `backedSutMethods` and its header paragraph. Add the six unbacked methods as explicit members of the `accounts` object, each one line, each throwing `new CapabilityPending(['sut.accounts.<method>'])` with the method's own name: `registerWithProvider`, `registerWithGithub`, `signInWithProvider`, `sendDiscoveryMessage`, `discoveryMessagesBy`, `publicSignupAccountTypes`. Keep the header's four-family explanation of why they are not backed; drop the sentences about the enumeration and the proxy. If `AccountsSut` names other methods that the object does not implement, typecheck tells you; add each as the same one-line refusal and list them in your report.
14. `tests/at/tsconfig.json`: remove `typeprobes` from `exclude` and the comment about it.
15. `tests/at/README.md`: delete the sentences that name `tests/at/typeprobes/` or `type-invention.selftest.ts`; keep the rest.
16. `loop/parked/v1/README.md`: append one section, `## The provenance ledger (parked 2026-09-02)`, that lists the five parked paths, says they are dead text under version control (not compiled, not run, not imported), that the red kinds and the manifests did not move, that one boolean `live` on the harness now drives the above-loop refusal, and what that costs (the loop tier can no longer tell a real member from a stand-in; above loop a suite with no live adapter is refused by a boolean, not a computed verdict).

Nothing else. Not `tests/at/expected/`, not `check.ts`, not `fixtures.ts`, `sentinels.ts`, `faults.ts`, `guards.ts`, `vendors.ts`, `config.ts`, `atconfig.ts`, not any `_fixture.ts`, not the drive skill, not CI.

## Order of work (each step ends green before the next)
1. The failing test (above). `bun run at:selftest` shows the one failure.
2. The moves. Then `bun run typecheck`; the error list is your edit list. Work through items 1 to 15.
3. `bun run typecheck` clean; `bun run at:selftest` green (expect 9 files); `bun run at:verify req-001 --tier loop --expect` and `bun run at:verify req-016 --tier loop --expect` match.
4. `bun run at:verify req-001 --tier integration --expect` (about five minutes; AT-001.12 and AT-001.13 wait out a real token). It must print the evidence line and end with the EXPECTED match, 16 green, 21 red. If the lock is held by another run, wait two minutes and try once more. If the run refuses with an infrastructure message, stop and report it; do not restart the stack yourself.
5. Item 16, the README section.
6. Commit everything as ONE commit on this branch. Write the message to a file and commit with `git commit -F <file>`. First line exactly: `AI4DEV-87: the provenance ledger, the brand, the attestation, and the type probes are parked`. Body: what moved, what each edited module lost, the boolean that replaced the ledger, the cost stated. End with the two trailer lines `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01SikdZcn3PmB9SrZ4dL1ziT`. Do not push. Do not stage anything under `loop/items/AI4DEV-87/artifacts/`.

## STOP rule
If a step cannot end green, or the design cannot be followed without a change outside the touch list, stop, write the report, and say exactly what blocked you. Do not widen the touch list on your own. Do not edit a manifest.

## Report
Write `loop/items/AI4DEV-87/artifacts/units/unit-1-report.md`: the commit SHA; every file moved, edited, or deleted with one line each; the pasted last 15 lines of each of the six commands in step 3 and 4 (redact any JWT-shaped string); every place you deviated from this brief and why; the selftest count before and after. Then reply with exactly five lines: the commit SHA; the selftest count; the three at:verify results; deviations (a number, and "none" when zero); the report path.
