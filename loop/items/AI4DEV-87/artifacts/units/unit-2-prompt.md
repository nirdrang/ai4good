You are the writer for one unit of work in the repository at your working directory (a git worktree on Windows; use PowerShell for every command). Read and follow, in full and in order, loop/items/AI4DEV-87/artifacts/units/unit-2-live-stack.md. It names the design document to read first, every file you may touch, the selftest to write first, the checks, the commit message, and the STOP rule. Use `git mv` for moves. Commit once as it says; do not push. Write the report file it asks for, then reply with exactly the five lines its Report section asks for and nothing else.
