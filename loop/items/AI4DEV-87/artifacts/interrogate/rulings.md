# Interrogate rulings on head 3141206 (lead, 2026-09-03)

Reviewers: A fable@high (0 critical, 5 warnings, 7 nits), C grok@xhigh (5 warnings), D opus@xhigh (1 critical, 11 warnings, 2 nits). B sol@max pending at the time of ruling; folded in below when it arrives.

## Act on (unit 4, one commit, then a cross-family re-read)

1. **Liveness is decided before anything is built, and `live` leaves the harness type** (A1, C2, D2, D3, D9: consensus). `HarnessModule` gains `liveAdapterExists(requirement): boolean` (the `existsSync` already in `index.ts`). `openWorld` calls it and throws `aboveLoopStandInRefusal(TIER, exists, sutKey)` before `createHarness`. `createHarness` loses its third branch: above loop with no live adapter it throws (`no live adapter for <req>; the registry refuses this tier before construction`), so a flipped check cannot load a fixture. `live` leaves `AtHarness`, `contracts.ts`, and `index.ts`. Selftests, runnable without a stack: `liveAdapterExists('req-016')` is false and `('req-001')` is true; `createHarness({ requirement: 'req-016', tier: 'integration' })` rejects naming the missing live adapter; `createHarness({ requirement: 'req-001', tier: 'integration' })` rejects on the mail reader (the live branch is reached; no coordinates in the environment); the loop harness for req-016 builds. `design.md` section 1 is corrected: the loop fixture was loaded above loop and the boolean was the whole gate; now nothing is built above loop without a live adapter.
2. **The drive's CLI call goes through the one seam** (A2, C4, D7). `local-stack.ts` exports `stackFromLocalStatus(repoRoot): Stack`: `readLocalConfig(repoRoot)`, `runSupabaseCli({ workdir: repoRoot, projectId }, ['status', '-o', 'json'])`, `parseStackStatus`, then the `Stack` (refuse when the status names no mail catcher). `stackFromStatus`, `parseStatusJson`, `stackFromStatusJson` and their selftest blocks are deleted from `live-stack.ts`. The drive imports the one function from `local-stack.ts`. The seam's "nothing under tests/ assembles a CLI call elsewhere" sentence becomes true again.
3. **One name map for the five coordinates** (D6). `live-stack.ts` exports `STACK_ENV` (field to variable name); `childCoordinates` in `local-stack.ts` writes through it; `stackFromEnv` refuses naming the missing variable for the four mandatory coordinates and leaves `mailUrl` empty when absent (the runner omits it when the status reports no catcher; `mailIdentification` already refuses an empty one).
4. **The drive records what it sent and checks what it depends on** (A10, A11, D8, D12). `authPost`, `functionPost` return `url` beside `status` and `json`; `recordHttp` takes that url; the drive's `const api` and the five rebuilt URLs go. `readJson` is exported and the Doctor step uses it. The Doctor step gains two checks: (a2) `mailIdentification(stack)` recorded as the catcher's own identification; (a3) the edge runtime mount: `docker inspect supabase_edge_runtime_<projectId> --format "{{json .Mounts}}"`, the Source that ends in `supabase/functions` must start with `repoRoot`, else fail with the remedy (`bun run db:stop` then `bun run db:start` from this checkout). The drive now has 13 checks; the Done contract's "11 of 11" becomes "13 of 13" and the pull request says why.
5. **The verify skill's recipe describes the protocol the shared module sends** (A4, C1, D "what I verified"): Bearer anon key on every Auth post, Mailpit `search` then `raw`, quoted-printable decoding, `type=` filter, the 20-second poll; the Doctor section says the drive runs its checks itself. The pull request's Tradeoffs names the three request changes in the drive.
6. **The drive is type-checked** (D1, the one critical). `.claude/skills/verify-ai4good/scripts/tsconfig.json` extends `tests/at/tsconfig.json` with `include: ["*.ts"]` and an empty `exclude`; `tests/at/typecheck.ts` gains a third `PROJECTS` row; `import.meta.dir` in the drive becomes `fileURLToPath(new URL('.', import.meta.url))` so the config keeps `types: ["node"]`. The check fails when the drive and `live-stack.ts` disagree.
7. **Prose sweep** (A5, C3, D4, D5, D14, A8, D13): the nine stale sites (`local-stack.ts` 11-20 renumbered, 321, 496, 1013; `registry.ts` 262, 297; `suite-adapters.ts` 10; `runner-blackbox.selftest.ts` 281; `contracts.ts` 174; `_integration.ts` 1241; `req-001/_fixture.ts` 7; `req-016/_fixture.ts` 25; `supabase/functions/_shared/accounts.ts` 13); the two probe sentences say the attack is recorded under `loop/parked/v1/tests/at/typeprobes/` and nothing executes it; `_live.ts` gets two sentences saying why the six refusals are written out (the manifest names them; `AccountsSut` makes an omission a compile error); the parked README says `live-email.ts` was edited in this branch (attestation stripped) before it moved; `live-stack.ts` messages say "the live mail reader" and "the stack"; one cast in `index.ts` with a two-sentence note on what it takes on trust.
8. **Small structural nits** (A6, A7, D11, A12): the lifetime-pin preflight describe returns to `runner.selftest.ts` (it spawns the runner); the two selftests import `bunExecutable` and `childEnv` from `local-stack.ts` and the re-export goes; `AtPending`, `CapabilityPending`, `PendingPhase` move to a leaf `pending.ts`, re-exported from `registry.ts` so no suite import changes.
9. **The lock leaves `local-stack.ts`** (D10; A3 and C5 asked for a split). `stack-lock.ts` takes `StackLock`, `Holder`, `stackLockPath({ projectId, apiPort })`, `acquireStackLock`, `processIsAlive`, `holderIsLive`, `heldByAnotherRun`, `clearStrandedGate`, `GATE_STALE_MINUTES`, verbatim, with its fifteen tests in `stack-lock.selftest.ts`. `local-stack.ts` lands near the thousand-line bar. The four-way split stays a filing candidate.

## Consider (filed under Not done here)
- The four-way split of `local-stack.ts` (A3, C5, D10 beyond the lock): child environment and redaction, CLI and status, identity and reset. After item 9 the file is about one thing, the stack; a later item can cut it when a second consumer appears.
- Joining the two Bun SQL lookups and the two redactions (A9). Both pre-date this branch in different places; noted in the pull request.

## Noted
- A `_live.ts` that returns the loop fixture would go green above loop (C2). Writing a live adapter is an author's decision, the deliberate act the tree's threat model excludes; the refusal-before-construction in item 1 makes the honest mistake (no live adapter, flag flipped) impossible.
- The drive's mail reads changed from the old list-and-render path to the adapter's search-and-raw path (A4, C1, D). Deliberate; the adapter's path decodes quoted-printable, which the old drive did not; stated in the pull request.

## Dismissed
- None. Every finding was either a real defect, a real prose error, or a sizing question answered in Consider.

## Sol (arrived after unit 4 launched; 6 warnings)
- Already in unit 4: the moved `CapabilityPending` cycle (sol 4 = A12, item 8); the prose sites (sol 6 = item 7); the drive protocol change and the stale recipe (sol 1 = item 5); the god module (sol 5 = item 9 and Consider).
- Act on, unit 5, after unit 4 lands:
  10. The mail poll has one deadline (sol 2). `verifyLinksFor` stops reading messages once a matching link is found, passes the remaining time into each fetch, and returns at the deadline even mid-iteration. One selftest with a stubbed fetch pins the bound.
  11. The above-loop refusal is proven through the real path (sol 3, and the half of A1 and D9 that item 1's factory tests do not cover): a black-box selftest spawns the pinned vitest on the real `suites/req-016/` with `AT_TIER=integration` and no coordinates in the environment, and asserts that all twelve ids fail with exactly `CapabilityPending: CAPABILITY PENDING — fixtures.worlds, sut.notifications`. With item 1 the refusal precedes any construction, so the test needs no stack and runs in CI. It replaces the deleted attestation test as the guard for the tier CI never runs.
  12. The request shape is pinned (sol 1's second half): one selftest stubs `globalThis.fetch` and asserts the headers, method, and URL `authPost` and `functionPost` send, including the `x-forwarded-for` rule.
- Noted: `local-stack.selftest.ts` imports `lifetimeProblem` from the req-001 live adapter (sol 5). A harness selftest is the only home for a test with no acceptance id; the function is the pin's live half and belongs with the pin's tests. Left.
- Dismissed: keep caller-specific request construction in the drive (sol 1's suggestion). One protocol is the point of the shared module; the difference is stated in the pull request and the recipe now describes the one protocol.
