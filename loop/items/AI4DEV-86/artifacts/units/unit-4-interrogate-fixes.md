# Unit 4 brief: the interrogate fixes (writer: fable, hardest tasks)

Worktree: C:\Users\nirdr\Downloads\ai4good\.claude\worktrees\AI4DEV-86, current branch, head ea73436. One writer at a time; you are it. Read loop/items/AI4DEV-86/artifacts/interrogate/rulings.md first: its "Act on" list, items 1 to 10, is your contract. The reviews it rules on are beside it (review-fable.md, review-opus.md, review-grok.md); read the findings the items cite for their exact locations and reasoning.

## Rules
- Tests first where a behaviour changes (the lock policy, the AT_REPO_ROOT refusal, the lifetime comparison, the message split, the three new selftests): write them, watch them fail, then make them pass.
- Files you may touch: tests/at/harness/runner.ts, runner.selftest.ts, attestation.ts, live-ledger.selftest.ts (only where the null-proof tests go), capabilities.ts, conformance.selftest.ts and live-ledger.selftest.ts (only the evidence-parameter call sites, if any), check.ts (the one sentence), contracts.ts (line 70), live-email.ts (line 14), .env.example, supabase/config.toml (lines 11-18 and the jwt_expiry comment), tests/at/suites/req-001/_fixture.ts, _integration.ts, _live.ts, b-verification-and-sessions.test.ts, .claude/agents/mechanical.md (line 3), .claude/skills/verify-ai4good/SKILL.md (one sentence), CLAUDE.md (one sentence in section 5), eslint.config.js (one ignore entry), loop/parked/v1/README.md, and the moves in item 9 (git mv to loop/parked/v1/loop/work/ and loop/parked/v1/.claude/skills/find-batch/). Nothing else. Not tests/at/expected/, not index.ts, not registry.ts, not statusline.ps1.
- The brand for the proof: a module-private `const PROVEN: unique symbol` in runner.ts and a `readonly [PROVEN]: true` member on StackIdentityRead that only identityVerdict sets; export the type, never the symbol. attestation.ts imports the type only.
- The lifetime comparison in _live.ts: decode the first access token the adapter obtains (it already decodes payloads for sessionIdOf), compute exp minus iat, and throw a plain error naming both numbers and the two restart commands when it differs from the pinned lifetime by more than 5 seconds.
- The AT_REPO_ROOT refusal: in main's integration branch, before the lock, if REPO_ROOT !== INSTALL_ROOT return infra("the integration tier runs only from the real checkout: AT_REPO_ROOT redirects the data root, and a data root must not choose which database is reset"). One selftest through the runner's black-box seam if one exists cheaply; otherwise a unit test of the predicate.
- Keep every check green after each step: `bun run typecheck`, `bun run at:selftest`, `bun run at:verify req-001 --tier loop --expect`, `bun run at:verify req-016 --tier loop --expect`. At the end, once: `bun run at:verify req-001 --tier integration --expect` (the stack is up and already restarted on the pinned lifetime; do not restart it).
- Commit ONCE, first line `AI4DEV-86: the interrogate fixes on the destructive path and the leftovers`, body listing the ten items by number with one line each, trailers `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01SikdZcn3PmB9SrZ4dL1ziT`. Do not push. Do not stage loop/items/AI4DEV-86/artifacts/.

## Report
The commit hash; the five command outputs (last 8 lines each, with timestamps); the selftest file and test counts after; each of the ten items with one line saying what changed and where; any item you could not do as written, classed as "the ruling was wrong", "a requirement was missed", or "I overreached", with the exact reason. BLOCKED with the error if you cannot finish.