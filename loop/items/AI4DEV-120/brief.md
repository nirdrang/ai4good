# Brief for AI4DEV-120 (intake form and draft autosave)

Chain: AI4PM-25 (project need creation, free-text intake, REQ-003) > AI4DEV-119 (project need
intake dev root) > AI4DEV-120 (intake form and draft autosave, D1)
Branch: nirdrang/ai4dev-120-intake-form-and-draft-autosave-d1
PRD slice: `loop/out/pure-s3-req-001-006.md`, lines 38 to 46, the REQ-003 section, verbatim:

> #### REQ-003: Project Need Creation (free-text intake)
>
> An NGO admin starts a "Project Need" with a free-text problem description, which begins Discovery.
>
> - Intake captures a title, problem description, and urgency — no cause field exists at intake. Cause labels are machine-generated by Discovery, not entered here (REQ-004); a draft shown before Discovery runs legitimately carries none. The NGO may remove a wrongly generated label from its project; it may never type or invent one — correction is deletion-only, invention stays machine-owned.
> - An optional reference-file upload (screenshot, spreadsheet, blank form, mockup, requirements doc) is available for Discovery and the volunteer, shown with the data-responsibility disclosure, which is hardened once Tier-2 sensitivity becomes known (REQ-032).
> - Drafts persist automatically without an explicit save; on submission Discovery begins and the raw intake is retained for audit.
>
> Dependencies: REQ-001, REQ-002, REQ-032.

The manifest is `loop/decomp/req-003.md`, revision `6fe2d7f`. Its cross-contracts line: the
NGO profile requirement is not exercised here, any NGO drafts, and vetting gates publishing,
which lives in the lifecycle requirement; the Tier-2 classification event that one id asserts is
produced by the Discovery agent requirement and is a stubbed fixture until then; the need
attachments requirement owns the file-policy contract whose disclosure copy is consumed here as
a fixture; the draft to discovery_in_progress transition belongs to the lifecycle requirement's
engine; the zero-cause-label assertion is a negative claim about the Discovery agent's
generation timing, not a producer relationship.

Item text: the parent's description, verbatim:

> Deliverable D1 of the project need creation requirement: the intake form captures title,
> description and urgency, only the NGO's admin can start a need, the description gate blocks
> submission on its own, drafts save themselves, and a draft carries no cause labels before
> Discovery. A container — it folds when its leaves close.
>
> Covers acceptance ids AT-003.01, .02, .03, .04, .05 and .17.

Acceptance tests: no suite exists for this requirement. This run creates
`tests/at/suites/req-003/` and `tests/at/expected/req-003.json`, in the shape of the NGO
profile suite under `tests/at/suites/req-002/`, and registers every id below through `atTest`.
The specification is `.taskmaster/docs/acceptance/at-req-003.md`: section A (intake capture),
B (draft autosave), C (reference-file upload at intake), D (submission to Discovery). Thirteen
P0 ids in the requirement, thirteen in this run. The `at:check` bijection requires every P0 id
of the specification to have exactly one call site. Retired ids 06, 08, 13 and 15 are excluded
by the specification. An id whose full proof waits on a surface owned elsewhere is declared red
in the manifest by id with a stated shape, as the NGO profile run did.

## Units

Units 1 to 3 are the parent's own leaves. Units 4 to 7 are extra units from the same
requirement root (founder 2026-09-13, one run, local). The order follows the manifest's
blocked-by edges: capture first, then the gates and autosave, then the upload and its
disclosures, then submission and the audit snapshot. The wiring leaf of D3 is not in this run;
it waits on the founder's sign-off of design batch 2. The pull request names each unit by its
short label in words, never by its id.

### Unit 1: AI4DEV-123 (capture title, description and urgency; only the NGO admin starts a need)
Item text, verbatim:

> An NGO admin of any tier, vetted or not, starts a Project Need with a title and a free-text
> problem description, and both persist on a draft. Urgency persists and renders on the draft.
> There is no cause field at intake; the founder decision on the self-generated cause taxonomy
> removed it, and Discovery generates the labels later. A member without the admin role, a
> volunteer, or a visitor is rejected when they try to start a need for the NGO. The gate is
> the admin role, not the NGO; cross-organisation isolation belongs to the auth requirement.
>
> **Verify:** AT-003.01, AT-003.02, AT-003.04
> **Blocked by:** nothing in this tree; the admin role comes from the auth requirement, which is built
> **Manifest:** `loop/decomp/req-003.md`, deliverable D1 leaf L1, revision `6fe2d7f`

Acceptance tests: AT-003.01, .02, .04, section A. The `projects` table from the auth run (see
facts below) holds the org and the single developer seat and says in its own comment that
product project creation is not landed there. Decide in the design whether the need is a row
on that table or its own table joined to it, and say why.

### Unit 2: AI4DEV-124 (the missing-description block on its own, and autosave without an explicit save)
Item text, verbatim:

> A draft by an email-verified NGO admin with Discovery capacity, complete except for the
> problem description, is blocked on submission. The test isolates this gate so that no other
> gate, verification or capacity, can be the cause of the block. Separately, intake content
> typed by an NGO admin persists with no explicit save action: leaving and returning shows the
> draft intact.
>
> **Verify:** AT-003.03, AT-003.05
> **Blocked by:** D1.L1
> **Manifest:** `loop/decomp/req-003.md`, deliverable D1 leaf L2, revision `6fe2d7f`

Acceptance tests: AT-003.03, section A; AT-003.05, section B. Autosave is a data contract
here: each change persists through the write route with no explicit save call, and a later
read returns it. The screen that types is the wiring leaf's.

### Unit 3: AI4DEV-125 (a pre-Discovery draft carries zero cause labels)
Item text, verbatim:

> A freshly created draft, read through any supported UI or API, shows no cause labels. A
> draft legitimately carries zero labels until Discovery runs and generates them. This is a
> negative claim about the label producer's timing, not a producer relationship.
>
> The producer is the cause-taxonomy leaf of the Discovery agent requirement, not
> materialised. Stub it until it lands.
>
> **Verify:** AT-003.17
> **Blocked by:** D1.L1, plus the cause-taxonomy producer in the Discovery agent requirement
> **Manifest:** `loop/decomp/req-003.md`, deliverable D1 leaf L3, revision `6fe2d7f`

Acceptance tests: AT-003.17, section A.

### Unit 4: AI4DEV-126 (optional reference upload attaches to the draft, with the base disclosure)
Item text, verbatim:

> An NGO uploads a reference file at intake and the file attaches to the draft. Whenever the
> upload surface renders, the data-responsibility disclosure is shown: redacted or sample data
> only, and ai4good and the volunteer will see the files. The format matrix, size caps and
> metadata listing belong to the need attachments requirement, not here.
>
> **Verify:** AT-003.07, AT-003.09
> **Blocked by:** D1.L1
> **Manifest:** `loop/decomp/req-003.md`, deliverable D2 leaf L1, revision `6fe2d7f`

Acceptance tests: AT-003.07, .09, section C. No screen renders the disclosure in this run.
The backend pass proves the disclosure the upload route serves, as copy or as a flag the
screen must render; the wired re-run later proves the rendering. Say in the design which.

### Unit 5: AI4DEV-127 (the Tier-2 hardened disclosure is present before any further upload)
Item text, verbatim:

> Once a project is classified Tier-2, the next render of the upload surface already carries
> the hardened disclosure, a hard acknowledgment restating fixtures-only, before any further
> upload. Hardening triggers on classification, not on the next upload.
>
> The classification event is produced by the Discovery agent requirement, not materialised;
> stub it until it lands. The disclosure copy comes from the file-policy contract of the need
> attachments requirement.
>
> **Verify:** AT-003.10
> **Blocked by:** D2.L1, plus the classification event in the Discovery agent requirement
> **Manifest:** `loop/decomp/req-003.md`, deliverable D2 leaf L2, revision `6fe2d7f`

Acceptance tests: AT-003.10, section C.

### Unit 6: AI4DEV-128 (submission by a verified admin with capacity starts Discovery, file or no file)
Item text, verbatim:

> A complete draft submitted by an email-verified NGO admin with Discovery capacity, free
> credits or funded fuel, starts Discovery and moves the project from draft to
> discovery_in_progress. The same holds with no reference file: the upload is optional. The
> verification and capacity rejection with its remedies belongs to the lifecycle state-table
> requirement, not here.
>
> The draft to discovery_in_progress transition belongs to the transition engine of the
> lifecycle state-table requirement, not materialised. Decide in the design how the transition
> is proven until the engine exists.
>
> **Verify:** AT-003.11, AT-003.12
> **Blocked by:** D1.L2, plus the transition engine in the lifecycle state-table requirement
> **Manifest:** `loop/decomp/req-003.md`, deliverable D3 leaf L1, revision `6fe2d7f`

Acceptance tests: AT-003.11, section C; AT-003.12, section D. Discovery capacity is the daily
allowance the NGO profile run landed (see facts below); read it, do not rebuild it.

### Unit 7: AI4DEV-129 (raw-intake audit snapshot at submission, unchanged under later edits)
Item text, verbatim:

> When submission completes, a retained audit snapshot equal to the submitted raw intake is
> created at that moment. When Discovery or NGO edits later change the working content, the
> snapshot stays unmodified and retrievable by the platform.
>
> **Verify:** AT-003.14, AT-003.16
> **Blocked by:** D3.L1
> **Manifest:** `loop/decomp/req-003.md`, deliverable D3 leaf L2, revision `6fe2d7f`

Acceptance tests: AT-003.14, .16, section D.

## Facts from the repository

The board does not say these. The lead needs them.

1. `main` is at `afb2c85`, the squash of the NGO profile and vetting run. The branch was cut
   from it, and the only commit on the branch before you is this brief.
2. What the auth requirement landed and this run builds on: the three account types and the
   platform admin role; the `organizations`, `org_memberships`, `acknowledgments` and
   `projects` tables; the email-verification gate on unverified writing; the tenant read
   posture, one rule in SQL, reads as the caller, with a static catalog scan over the
   migrations in CI (`tests/at/suites/req-001/_policy-scan.ts`) and a live catalog check at
   integration; the append-only audit table; and one mandatory write-route inventory in
   `supabase/functions/_shared/write-routes.ts` that every write route registers through, with
   a conformance check that fails an unregistered route. Every table this run adds joins the
   posture, and every write route registers with the inventory, or CI fails.
3. What the NGO profile and vetting run landed: the organisation profile route
   (`set-organization-profile`), the vetting aggregate and its route, and the daily Discovery
   allowance, a per-day ledger with ten credits unverified and thirty vetted, its read and
   debit route (`discovery-allowance`), and the block at zero naming three remedies. The
   grants sit in the at-config registry. Unit 6's "with Discovery capacity" precondition is a
   read of that ledger.
4. What the notifications run landed: one shared emitter as the sole writer, the static
   taxonomy table, the outbox, and the delivery defaults, under `supabase/functions/_shared/`.
   No id in this run emits a notification. If the design finds one is owed, it is a "Not done
   here" line, not a new taxonomy row.
5. Two edge routes named `project-workspace` and `public-project` exist from the auth run's
   single-developer and tenant-visibility leaves. Read them before adding a project write
   route, so the need does not get a second shape.
6. Read the NGO profile suite's `_contract.ts`, `_fixture.ts`, `_live.ts`, `_pending.ts` and
   the `_source-*.ts` files for the integration style the per-id gate expects, including how
   a red id is declared with a stated shape and how a static arm pins a fact against the
   shipped tree.
7. The verify commands: `bun run typecheck`, `bun run at:check req-003`,
   `bun run at:selftest`, `bun run at:verify req-003 --tier loop --expect`,
   `bun run at:verify req-003 --tier integration --expect`. Run the auth, NGO profile and
   notifications suites at both tiers too: the write-route conformance check and the
   emitter's sole-writer scan both change their result when this run adds routes.
8. The verify skill under `.claude/skills/verify-ai4good/` drives the real stack: auth, edge
   functions, the database, and the mail catcher on the 44321 block. The mechanical agent
   drives it and captures the evidence under the item folder.
9. The pull request body must not name any id but the parent's own. The seven units are
   named in words.

## The ask
Run this item in poteto-mode, end to end, and open one pull request from this branch.
If the brief has Units, design once for the whole subtree, then build and verify the units
in order, one commit group per unit, each unit green before the next starts. The pull
request body names each unit by its short label in words, never by its id.
Ground it with /how in critique mode first: explorers, explainer, then the critics, on
every item.
In the design arena, give every runner a distinct structural direction, so the candidates
do not converge on one design. The runner lanes are the sheet's four; add none.
Tool-heavy work that needs an executor goes to the mechanical agent with exact instructions:
the rebase into ordered commits, driving the verify skill on the real surface, and the closing
commands. You decide and you judge the evidence; it types; you check each result once.
The acceptance suite is not lane work. Run its commands yourself as background shell commands
that write their output to a file, and read back only the exit code and the green and red
counts. A lane costs about eighty thousand tokens whatever it runs; a background command costs
a few hundred. Verify once per unit boundary and once on the merge head, and not in between
unless code changed. Run the integration tier twice only where a procedure waits on real
elapsed time.
Every delegated lane writes its full report to a file under the item folder and replies
with five lines and the path. Read the file only when the summary names a deviation, a
blocker, or a red.
A unit goes to the hardest-tasks lane only when the writer must still design something. A
unit that applies a fixed contract goes to the feature lane. Say which in the decision trail.
The comment audit before review runs on the mechanical model with the comment-sicko prompt,
never on your own model.
Do not name any other item's id in the pull request title or body.
The pull request body carries Why, Scope, Tradeoffs, Blast Radius, and Verification.
Then close the item as the Closing section says. You close it, nobody else.

## Closing (the git part is yours, the board is not)
1. Wait for CI to be green on the exact head of the pull request, and for the founder to
   say "merge". Both, never one.
2. Hand the git mechanics to the `mechanical` agent with exact commands. You decide, it
   types: `gh pr merge <n> --squash`. The merge closes the item on the board through the
   pull request link. Never touch the board yourself. Delete no branch and no worktree: the
   founder keeps merged branches for reflection, and deletes by name when they choose
   (founder 2026-09-06).
3. Leave the worktree with `ExitWorktree(action: "keep")`.
4. Invoke `/controller done <item>`. That skill does the board steering. Do not do it
   yourself.

## Mechanics never spend your calls
Fable calls are scarce. Tool-heavy work without judgment, the station 7 rebase, the merge
and cleanup commands, goes to the `mechanical` agent (sonnet, inherits the worktree,
executes exact instructions, rules on nothing). Write the exact plan, let it run, check the
result with one read. Do not use a fork for this: a fork runs on your own model.
The acceptance suite's commands are not mechanical work. A background shell command runs them
for a few hundred tokens where a lane spends about eighty thousand; on the notifications item
ten verification lanes cost 746k tokens to execute about thirty minutes of commands.
A writer that dies after finishing its work is recovered by running the pin and committing
the finished tree, not by rerunning the writer.

## The evidence bar
- The verify suite for the acceptance tests above passes on the final head, run as background
  shell commands. Name each check, its exit code, its counts and its timestamp in the
  Verification section.
- CI is green on the final head.
- Discovered work goes in a "Not done here" list in the pull request body, never in the diff.

## Environment facts
- One database, the stack `supabase/config.toml` describes, local and cloud alike. Start it
  with `bun run db:start`; every integration run resets it.
- codex needs `codex login --device-auth` once per fresh VM. The session banner says when.
