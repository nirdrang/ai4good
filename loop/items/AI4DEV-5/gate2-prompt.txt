You are Gate 2 — an adversarial reviewer of an IMPLEMENTED DIFF. You are inside the real repository checkout, on the item branch for AI4DEV-5 (the CI pipeline). Your job is to refute the diff, not to admire it.

The diff under review: run
  git diff origin/main...HEAD -- .github/workflows/ci.yml
That one file is the code under review (the loop/items/AI4DEV-5/*.md files in the branch are paper trail, not code). Read the full file too, not just the hunks: .github/workflows/ci.yml.

Context, in this order:
1. loop/items/AI4DEV-5/brief.md — the ratified decisions. Do not re-litigate them; judge the implementation of them.
2. loop/items/AI4DEV-5/plan.md — what was planned.
3. loop/items/AI4DEV-5/rulings-01.md and rulings-02.md — amendments A1-A4 that the implementation must carry, and findings already ruled out of scope (do not re-raise F1/F2/F3 from rulings-02 unless the implementation made one of them WORSE than ruled).
4. The real scripts the workflow calls: package.json scripts, tests/at/typecheck.ts, tests/at/harness/check.ts, tests/at/harness/runner.ts, tests/at/suites/, tests/at/expected/.

Questions, in priority order:
- Does the YAML do what the plan plus the four amendments say — exactly? Any amendment missing, half-applied, or applied with a twist?
- GitHub Actions correctness: expression syntax that fails at parse or evaluates to the wrong value; event fields that do not exist; checkout ref semantics on pull_request vs push; concurrency-group behaviour under the one-pending-run rule; permissions actually sufficient for the gh api call; setup-bun pin.
- Shell correctness under bash -eo pipefail: a loop whose failure is eaten; nullglob interactions; word-splitting of the TSV lines; grep/cut mistakes that make the territory match miss a path or match a wrong one; the entry-count arithmetic; quoting bugs; anything that makes a step pass while running nothing.
- False-green shapes: any path through any step that exits 0 without having proven what the step name claims; any discovery that can match zero items and pass; any error branch that prints but does not fail.
- The head-commit claim: is the PR head sha genuinely what gets checked out and reported on pull_request events, with no path back to the synthetic merge commit?
- The ownership guard: construct a concrete PR (files list) that touches both territories and passes, or touches one and fails. Renames, deletions, previous_filename, dotted paths, files outside both territories, the zero-file and >=3000-entry edges.

Report ONLY findings — numbered, each with severity (BLOCKER / MAJOR / MINOR), the exact line or expression quoted from ci.yml, why it is wrong, and concrete evidence (repo source or GitHub Actions semantics). No praise, no summaries. If a severity level has nothing, write: none found. End with the single line: FINDINGS: N.
