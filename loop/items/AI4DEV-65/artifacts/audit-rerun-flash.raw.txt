# AUDIT RE-RUN — AI4DEV-65 at head `9728a82f9361e5138f4f65ac51c637d3bf148551` (fix delta from `6ee87419b88aa210b1d08003536469666b65fec0`)

Cage note up front: this launch cage denies the parent repository's `.git` (the gitdir `C:/Users/nirdr/Downloads/ai4good/.git/worktrees/...` sits outside the allowed tree, same denial the first audit's flash seat recorded) and has no shell, so the delta's byte-level composition cannot be read here. Boxes that need the delta's bytes are graded COULD-NOT-VERIFY with settling commands named; everything readable in the tree was read at head.

## Rebuilt checklist (the two adopted rulings)

- **R9 — PASS.** `supabase/migrations/20260811120000_acknowledgment_signer_identity.sql:82-94`. The rewritten paragraph explicitly retracts the old claim (old text quoted in `loop/items/AI4DEV-65/audit-rulings.md:15-24`): line 83-84 "did bridge a mixed-plane window, but NOT because the columns behind them are nullable — all four `volunteer_profiles` columns are `not null` too". The real mechanism is stated: defaults let an old five-named-argument call resolve (85-86); an NGO completion writes NO `volunteer_profiles` row (86 — function body confirms: the profile insert is inside `if v_account_type = 'volunteer'`, lines 306-317); volunteer completion was fail-closed in the window (87 — matches the previous migration's own text, `20260809090000...sql:161-163`). The conclusion is kept: "No caller class avoids the three new columns: EVERY completion writes the acknowledgment row… The defaults exist for call-signature tolerance only" (88-91), consistent with the unconditional ack insert (336-347).
- **R10 — PASS.** `tests/at/suites/req-001/_contract.ts:91-103`. The comment now scopes the claim: "THROUGH THE DEPLOYED PATH exactly one value can appear here. That scope is the whole claim" (96-97); names the residual: "a `service_role` caller that bypasses the edge function can store a different nonblank statement, because the database floors presence and nonblank only. That is the accepted residual" (97-99); points at the migration's "WHERE THIS FILE'S AUTHORITY ENDS" section (99-100; the section exists at migration lines 43-52). The facts the comment asserts trace true: the statement constant at `supabase/functions/_shared/acknowledgment-copy.ts:37-40`, the exact trimmed match at `supabase/functions/_shared/accounts.ts:285-290`, and the judged value travelling at `supabase/functions/complete-signup/index.ts:141-143`.

## Rebuilt checklist (the new fact claims)

- **F11 — COULD-NOT-VERIFY** (the exhaustive half). Verified part: the two change sites the record quotes from the old head (`audit-rulings.md:15-24`, migration lines 82-84; `:44-45`, `_contract.ts` lines 91-98) are comment text, and the corresponding head text is comment/JSDoc (migration 82-94; `_contract.ts` 91-103, one JSDoc block). Head content of both files is otherwise consistent with every SQL/TS fact the first audit graded. Unverifiable part: "every changed line… zero SQL statements and zero TypeScript declarations change" is a claim about the delta's bytes, which this cage cannot read. Settling command: `git diff 6ee87419...9728a82f -- src supabase tests .github package.json bun.lockb tsconfig.json vitest.config.ts` — expect hunks only in the migration's comment block and the `_contract.ts` `authorityAttestation` JSDoc block.
- **F12 — PASS.** `supabase/migrations/20260809090000_volunteer_github_link_and_imported_profile.sql:122-125`: `github_handle text not null`, `top_languages text[] not null`, `repository_count integer not null`, `contribution_summary text not null` — all four content columns `not null`.
- **F13 — PASS.** Same file, lines 152-168: the bridge comment states the mechanism the rewritten text cites — "THE FOUR NEW PARAMETERS ARE `default null`… a call carrying ONLY the original five names still resolves" (153-156); "THE NEW EDGE FUNCTION OMITS the four github keys from the rpc body when the judged handle is null, which is every NGO completion" (157-159); "THE HONEST RESIDUAL: during a mixed-plane window VOLUNTEER completion is unavailable… fail-closed" (161-163).

## Declared-scope box (re-checks in full)

- base `ea4f3453` → earlier head `6ee87419`: **PASS**, settled first-hand by the audit sitting (`audit-rulings.md:73-77`: `git diff --name-only ea4f3453...6ee87419 -- <code territory>` returned exactly the thirteen paths, `src/routeTree.gen.ts` absent). Carried on that recorded first-hand run.
- fix delta half: **COULD-NOT-VERIFY** without git. Both record-named fix files (the migration, `_contract.ts`) are inside the 13-path list, and no fourteenth path surfaced in any read, but "no fourteenth path" is a claim about the delta's file list. Settling commands: `git diff --name-only 6ee87419...9728a82f -- src supabase tests .github package.json bun.lockb tsconfig.json vitest.config.ts` and the full-range variant against `ea4f3453` — each must return exactly the thirteen paths.

## Carried-forward checklist (R1–R8, F1–F10)

All carried lines were re-graded fresh at head where their subject sits in a file I read, or carried on the independence that the record-declared fix delta (two files: the migration and `_contract.ts`) cannot reach them. Caveat, stated plainly: the carry-forwards rest on that two-file delta claim, which I could not enumerate byte-level (F11 CNV); if the actual delta contains other files, the orchestrator should re-check the carried lines against it.

- **R1 — PASS** (re-graded; the delta rewrote its comment subject). Migration 82-94 claims no bridge; 131-133 `default null`; 62-65 three `not null` columns.
- **R2 — PASS** (re-graded fresh). `accounts.ts:37` imports the copy; the fourth check at 285-290 refuses a trimmed value not exactly `authorityStatement`, refusal names the mismatch (288); graph `complete-signup/index.ts:31 → accounts.ts:37 → acknowledgment-copy.ts`; wrong-content variant `'I am not authorized'` at `c-membership-and-acknowledgment.test.ts:165-170`, refused with no writes (the no-partial-state block runs for all seven variants, 209-225); AT-001.20 asserts by pattern at 270-286 (`/shared credential/i`, `/prohibit/i`, `/organi[sz]ation email/i`, `/recommend/i`).
- **R3 — PASS** (fresh). Migration 368-369 revoke-from-public / grant-to-`service_role`, then `notify pgrst, 'reload schema';` at 375.
- **R4 — PASS** (fresh). Loop: all seven variants, after each — account null, acknowledgments `[]`, `hasPlatformAcknowledgment` false, `organizationsNamed` empty, `membershipsOf` empty (`c-membership...test.ts:209-225`). Integration: the three omission variants with the same five assertions (`_integration.ts:704-746`).
- **R5 — PASS** (fresh). Migration 63-65: all three constraints `col !~ '^\s*$'`, no one-argument `btrim`.
- **R6 — PASS** (fresh). Migration 43-52 states the boundary; the only SQL checks are `!~ '^\s*$'` — no exact-statement pin exists, which is the ruled state.
- **R7 — PASS** (fresh). Migration 54-61 ("THE TWO BLANK FLOORS ARE NOT THE SAME WIDTH", POSIX `[[:space:]]` vs ECMAScript `trim()`, U+FEFF measured, pointer to `loop/items/AI4DEV-65/artifacts/verify-first-feff.txt`); constraints unchanged.
- **R8 — PASS** (fresh). The function body trims only `v_github_handle`/`v_contribution_summary` (157-158); `p_signer_*` reach the ack insert untrimmed (344-346) — the absence is the ruled state.
- **F1 — PASS** (fresh). All twelve refusal strings in the recreated body (`20260811120000...sql:162-289`) byte-match the previous definition (`20260809090000...sql:230-357`).
- **F2 — PASS** (fresh). Drop at 98 names `(uuid, text, text, text, inet, text, text[], integer, text)`; create at 131-133 appends exactly `p_signer_name`, `p_signer_title`, `p_authority_attestation`, each `text default null`.
- **F3 — PASS** (fresh). 368-369, identical posture to the previous migration.
- **F4 — PASS** (fresh). `complete-signup/index.ts:141-143` passes the destructured `decision.value` fields (86-94), never `body.value`.
- **F5 — PASS** (fresh). `accounts.ts:214-304`: pre-existing checks (parseAccountType 218-219, org-name 225-236, GitHub gate 240-247, text-version 249-254) all precede the four new checks in order name → title → attestation-present → attestation-match (258-290), each refusal naming its field; earlier refusals return first.
- **F6 — PASS** (fresh). Ack insert 336-347 carries the three columns and is the last write before `return` (349).
- **F7 — PASS** (state fresh, diff carried). `tests/at/expected/req-001.json:19-21` (loop) and `:57-59` (integration) green for AT-001.19/.39/.20; the "nothing else changed" half was settled byte-level by the audit sitting (`audit-rulings.md:78-82`); a grep over `tests/at/expected/` finds none of the three ids in `req-016.json`.
- **F8 — PASS** (fresh). `_pending.ts:2` "There are 21 of them"; D4.L1 removed from the LEAF map with the landed-leaves note (43-53).
- **F9 — PASS** (fresh). `at00119` (`_integration.ts:657-688`) drives `registerConfirmAndSignIn` only (email path, NGO); the comment at 651-655 states the GitHub-path narrowing.
- **F10 — PASS** (fresh). `a-signup-and-signin.test.ts:299-304` builds one `request` object; both calls at 306-307 pass that same reference — byte-identical by construction.

## Findings

[1] severity: S3 — a false statement about code in a comment, no behavioral effect (the same class and severity as the two accepted first-audit findings)    supabase/functions/complete-signup/index.ts:137-138
    claim: the comment says the four github rpc keys are omitted "because the columns behind them are nullable and the omission is a real deployment bridge" — but all four `volunteer_profiles` columns those keys feed are `not null` (`supabase/migrations/20260809090000_volunteer_github_link_and_imported_profile.sql:122-125`), and this item's own corrected migration paragraph states the opposite: "but NOT because the columns behind them are nullable — all four `volunteer_profiles` columns are `not null` too. That bridge worked for two other reasons" (`20260811120000_acknowledgment_signer_identity.sql:83-85`).
    why it matters: the tree now carries two contradictory statements of the same deployment mechanism inside one item — the corrected migration text vs this edge-function comment — so a future mixed-plane analysis reads a false reason here, the exact misdirection the first audit's finding 1 said a false bridge comment produces; the same sentence's contrast ("these three back `not null` columns") is also false in both directions, since the github keys' columns are not null too — the real contrast (every completion writes the acknowledgment row; an NGO completion never writes a `volunteer_profiles` row) is what the migration text now states. The sentence sits in this item's own added comment block (step 7's judged-values block) and in the declared 13-path territory; the fix delta's subject (the bridge mechanism) reaches it. Fix would be comment-only, matching the ruling class.
    unverified-runtime-claim: no (static fact, readable from the two migration files)

The F11/scope COULD-NOT-VERIFY boxes are evidence gaps, not defects — the settling commands above are what the executor runs once with git access. No other concern is withheld: everything else in the rebuilt and carried checklist read true at head.

AUDIT: 1 FINDINGS
