All checklist lines are now graded. Here is the audit.

---

# AUDIT â€” AI4DEV-65 (who signed: name, title, authority)

**Method and limits.** I have read-only tools only â€” no shell â€” and the launch cage denies even the `.git` directory of the parent repo, so anything that depends on the base commit's bytes cannot be settled here. Those boxes are marked COULD-NOT-VERIFY with the exact command that settles them, as verify-first rulings for the executor. Everything else was traced character by character in the head tree.

## Section 1 â€” adopted rulings

- **R1 â€” PASS.** The three new parameters `p_signer_name`, `p_signer_title`, `p_authority_attestation` each carry `text default null` (`supabase/migrations/20260811120000_acknowledgment_signer_identity.sql:126-128`), and the migration comment says explicitly "THAT IS NOT A ROLLING-DEPLOY BRIDGE" with the reasoning (lines 82-89, repeated at 120-122). The three columns are `text not null` (lines 62-65).
- **R2 â€” PASS.** `accounts.ts:37` imports `ACKNOWLEDGMENT_IDENTITY_COPY`; the fourth identity check compares the *trimmed* attestation exactly against `authorityStatement` and refuses with "the authority attestation does not match the shipped authority statement" (`accounts.ts:285-290`). Import chain for the deployed graph is present as claimed: `complete-signup/index.ts:31` â†’ `accounts.ts:37` â†’ `acknowledgment-copy.ts`. AT-001.39's loop tier has the wrong-content variant with the literal `'I am not authorized'` (`c-membership-and-acknowledgment.test.ts:167`), refused through the same no-writes assertion block. AT-001.20 asserts by pattern â€” `/shared credential/i` + `/prohibit/i`, `/organi[sz]ation email/i` + `/recommend/i` â€” plus a non-blank `authorityStatement` (`c-membership-and-acknowledgment.test.ts:270-293`).
- **R3 â€” PASS.** The migration ends `revoke execute â€¦ from public` (line 363), `grant execute â€¦ to service_role` (line 364), then `notify pgrst, 'reload schema';` as the file's last statement (line 370).
- **R4 â€” PASS.** Loop tier, all seven variants inside the same loop: `sut.account(...)` null, `acknowledgments(...)` `[]`, `hasPlatformAcknowledgment` false, `organizationsNamed(<attempted NGO name>)` `[]`, `membershipsOf` `[]` (`c-membership-and-acknowledgment.test.ts:209-225`). Integration tier (`_integration.ts:729-745`): the three omission variants with the same five assertions.
- **R5 â€” PASS.** All three constraints are `check (col !~ '^\s*$')` â€” never one-argument `btrim` (migration lines 63-65).
- **R6 â€” PASS.** The `authority_attestation` comment block opens "WHERE THIS FILE'S AUTHORITY ENDS" (line 43), states the constraints floor presence and nonblank only, names `validateCompleteSignup` as the content pin's home, and records the service-role residual as accepted (lines 43-52). The absence is the ruled state and it holds: the migration contains no statement-content literal â€” the only exact-statement code anywhere is the TS check.
- **R7 â€” PASS.** The comment states "THE TWO BLANK FLOORS ARE NOT THE SAME WIDTH", POSIX `[[:space:]]` vs ECMAScript `trim()`, U+FEFF measured on slot 1, and points at `loop/items/AI4DEV-65/artifacts/verify-first-feff.txt` (lines 54-61). The file exists in the tree (glob-confirmed; content unread â€” artifacts territory, and not needed for the code claim). No constraint change was made â€” the constraints are the plain `!~ '^\s*$'` shape.
- **R8 â€” PASS.** The SQL function body `btrim`s only `v_github_handle` and `v_contribution_summary` (lines 152-153); the three signer parameters are inserted as received (lines 331-342). The trim guarantee lives in `validateCompleteSignup` (`accounts.ts:296-298`). Absence confirmed.

## Section 2 â€” declared code territory

**COULD-NOT-VERIFY** for the exact file-set equality, with strong negative evidence: all thirteen claimed paths exist at head and I read all of them; a sweep for the item's distinctive identifiers (`signer*`, `authority*`, `ACKNOWLEDGMENT_IDENTITY_COPY`, `acknowledgment-copy`) finds matches in **no** file outside the thirteen â€” zero in `src/` (so `src/routeTree.gen.ts` carries none of them), zero in `supabase/` outside the four claimed files (the two older migrations and `config.toml` are clean), zero in `tests/at` outside the seven claimed suite files, and nothing in the configs. A touched file without a new identifier is theoretically possible, which is why this stays COULD-NOT-VERIFY. What settles it: run the pinned `git diff ea4f3453ed59081a3e24c035e6d321d1f2ebaa45...6ee87419b88aa210b1d08003536469666b65fec0 -- src supabase tests .github package.json bun.lockb tsconfig.json vitest.config.ts` and confirm the file list equals the thirteen exactly.

## Section 3 â€” concrete facts

- **F1 â€” PASS.** I compared the recreated function body against `20260809090000_volunteer_github_link_and_imported_profile.sql` refusal by refusal: platform_admin guard, unknown-type refusal, both NGO-name rules, the volunteer link refusal, the `auth.identities` backstop, all three import checks plus the summary check and the NGO GitHub-params mirror, and the already-completed refusal â€” each message byte-identical (new migration lines 157-285 vs old lines 230-358). The declare block and every insert except the acknowledgment one are unchanged too.
- **F2 â€” PASS.** Drop at line 93 names exactly `(uuid, text, text, text, inet, text, text[], integer, text)` â€” the previous migration's 9-argument signature (old file line 172 / 187-200). The recreate appends exactly `p_signer_name text default null`, `p_signer_title text default null`, `p_authority_attestation text default null` at the end of the parameter list (lines 126-128).
- **F3 â€” PASS.** Lines 363-364: revoke execute from public, grant execute to `service_role` only â€” the same posture as the previous migration's tail (old lines 421-422).
- **F4 â€” PASS.** `complete-signup/index.ts` destructures the three from `decision.value` (lines 86-94) and passes `p_signer_name: signerName` etc. (lines 141-143); the comment at 133-140 says "THE JUDGED VALUES, NEVER THE RAW BODY ONES". Raw body values appear only as inputs to validation (lines 78-80).
- **F5 â€” PASS.** Order in `validateCompleteSignup`: `parseAccountType` (218-219) â†’ organisation-name rules (225-236) â†’ GitHub gate (243-247) â†’ text version (249-254) â†’ name (258-263) â†’ title (267-272) â†’ attestation-present (275-280) â†’ attestation-match (285-290). Every refusal names its field. Earlier checks `return refuse(...)` before the new four, so AT-001.01's no-acknowledgment request (`a-signup-and-signin.test.ts:166-170`), AT-001.04's unlinked-volunteer requests (374-382) and AT-001.07's `platform_admin` request (669-673) â€” all of which deliberately omit SIGNER â€” never reach them; their pinned reasons (`/acknowledgment/i`, `/github/i`+`/link/i`, `/platform_admin/`) stay true.
- **F6 â€” PASS.** The insert lists `signer_name, signer_title, authority_attestation` (lines 331-342) and is the last write before the return (344-348).
- **F7 â€” PASS for the first half, COULD-NOT-VERIFY for the second.** At head, `tests/at/expected/req-001.json` lists AT-001.19, AT-001.39 and AT-001.20 green in **both** tiers (loop lines 19-21, integration lines 57-59) and nothing else moved to a visibly different state (the same 21 sut-missing reds and 5 capability-pending reds remain). "Changes nothing else" and "`req-016.json` is untouched" are byte-level claims against the base I cannot make; current `req-016.json` contains only AT-016 ids and none of this item's fingerprints, which is consistent but not proof. Settle with the pinned diff on those two files.
- **F8 â€” PASS.** `_pending.ts`'s `LEAF` map contains no `D4_L1` key (only an explanatory comment at lines 52-53); the header states "There are 21 of them â€¦ there were 24 before it" (lines 2-4). The three ids are registered exactly once each (`c-membership-and-acknowledgment.test.ts:62, 129, 256`).
- **F9 â€” PASS.** `at00119` (`_integration.ts:657-688`) drives `registerConfirmAndSignIn` â€” the email path only â€” and the comment at lines 651-655 states the GitHub-path narrowing ("THE GITHUB-ESTABLISHED PATH IS NOT DRIVEN AT THIS TIER").
- **F10 â€” PASS.** AT-001.03's `request` const (`a-signup-and-signin.test.ts:299-304`) is the same object passed to both `completeSignup` calls (lines 306-307), so byte-identical between them by construction.

**Other observations.** No defect in code this branch never touched came to my attention (the only direct `complete_signup` callers outside the changed files are prior items' proof scripts under `loop/items/`, which are record territory and not shipped code). The loop-tier fixture, live adapter, and contract all carry the three fields consistently with the plan's steps 8-10, and `_live.ts`'s `completeSignup` forwards the request verbatim as the plan's step 9 claimed it already did.

**Verify-first rulings handed to the executor (not findings):**
1. Section 2's box â€” run the pinned source-only diff and compare the file list to the thirteen.
2. F7's second half â€” diff `tests/at/expected/req-001.json` and `tests/at/expected/req-016.json` against base.

AUDIT: CLEAN
