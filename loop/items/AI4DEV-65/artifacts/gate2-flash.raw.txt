I have now read the full diff: both migrations (base and new), the copy module, the shared validation module, the edge function, all four test files, both adapters, the contract, the pending machinery, the expected JSON, and the item ledger. Here is the review.

---

# CODE REVIEW — AI4DEV-65 (who signed: name, title, authority) at `0c389c6`

## What I verified against the plan, direction by direction

**1. Migration drop-and-recreate fidelity (risk direction 1).** `supabase/migrations/20260811120000_acknowledgment_signer_identity.sql` drops `public.complete_signup(uuid, text, text, text, inet, text, text[], integer, text)` — the exact signature `20260809090000` created (its line 172 drop is the mirror image). I compared the recreated body against the previous one statement by statement: the `platform_admin` guard, the unknown-type refusal, the NGO-name rule, the volunteer-org-name mirror, the GitHub-link requirement, the identity backstop against `auth.identities`, both language-import checks, the repository-count check, the contribution-summary check, the NGO branch's GitHub-parameters refusal, the account insert with its `unique_violation` re-raise, the NGO org/membership insert, the volunteer profile insert, and the `jsonb_build_object` return are all reproduced word for word. The only changes are the three appended parameters (`default null`, with the deployment-bridge disclaimers), the acknowledgment insert gaining `signer_name, signer_title, authority_attestation`, the 12-arg signature in the comment/revoke/grant, and the `notify pgrst, 'reload schema';` tail — all exactly as plan decisions A and E demand. The column constraints are `text not null check (col !~ '^\s*$')`, the POSIX-whitespace shape, not the `btrim` shape. The new file sorts last among the three migrations.

**2. Validation ordering (risk direction 2).** In `supabase/functions/_shared/accounts.ts`, the four new checks (name, title, attestation-present, attestation-match, lines 258–290) sit after `parseAccountType` (platform_admin refusal), the organisation-name rules, the GitHub gate, and the text-version check — the exact order decision B demands. I traced every refusal-pinned request in the suite: AT-001.01's no-acknowledgment request (`a-signup-and-signin.test.ts` line 168, `_integration.ts` line 243) omits the signer fields and still refuses on `/acknowledgment/i` at the text-version check; AT-001.07's `platform_admin` request (line 671, `_integration.ts` line 342) refuses at `parseAccountType`; AT-001.04's unlinked-volunteer request (line 382) refuses at the GitHub gate. All fire before the new checks; no pinned reason changes.

**3. Whitespace definitions (risk direction 3).** The three layers are JS `trim()` (validation gate), `!~ '^\s*$'` (column constraints), and the explicit `btrim` sets (DB function, but only for the *pre-existing* github-handle/summary values — the new parameters deliberately have no DB-side trim). The divergence direction that exists is JS-strict, DB-lax: JS `trim()` removes a superset of the ASCII whitespace `\s` matches in the C locale (NBSP, LS/PS, BOM…), so anything JS considers blank is refused at validation with the field named before the DB is ever asked. The reverse direction — a value passing JS validation but refused as all-whitespace by the DB — would require a locale where `[[:space:]]` matches a character JS keeps; no concrete such character exists, and even then the failure mode is a bare constraint error for a direct caller, which is the documented floor, not a plan claim. No plan claim breaks. (One honest note: a *direct* service-role caller can store `'  John  '` untrimmed, because the DB function does not `btrim` the new parameters and the constraint only refuses all-whitespace. The plan's "verbatim and trimmed" is delivered by the validation layer and the migration comment says so explicitly; no claim is violated. Ruled out, not a finding.)

**4. Exact-match declarations (risk direction 4).** `tests/at/expected/req-001.json` moves exactly AT-001.19, .39, .20 into the green list at both tiers; every other entry (green and red) is unchanged. `_pending.ts` has removed the `D4_L1` label from the `LEAF` map (the file's own rule — a label with nothing pointing at it claims pending work), the header count reads 21 (was 24), the written-ids paragraph totals 16, and 16 + 21 = 37 = the acceptance file's P0 count. The only remaining `D4.L1` mentions are comments. The `loop/items/AI4DEV-65/pending-ledger.txt` follows the AI4DEV-60 format, states the three removed lines, the D4 closure, and the two explicit non-claims (integration GitHub-path narrowing; copy-as-content-not-display).

**5. Ripple over existing completions (risk direction 5).** Every completion that must succeed carries all three fields: all of AT-001.01–.07 loop bodies, all of b-verification's four completions (AT-001.09/.10/.12/.13/.14), and every `_integration.ts` completion including the at00109 conditional (both branches) and at00106's volunteer. AT-001.03's shared `request` object (a-signup line 299) is built once with `...SIGNER` and passed to both calls by reference — byte-identical by construction. The three refusal-pinned omissions are exactly the ones the plan allows.

**6. Copy module (risk direction 6).** `supabase/functions/_shared/acknowledgment-copy.ts` matches plan decision D's text exactly, including the em dash and the concatenation boundaries. `validateCompleteSignup` imports it (line 37) and compares the *trimmed* attestation by exact equality (line 286) — the constant has no surrounding whitespace, so trimmed-submission ≡ constant, and the stored value is the trimmed statement. The AT-001.20 body asserts meaning (subject + verb regexes, both of which match the shipped text), not mere existence, and imports the module directly rather than through a sut method.

**Test meaning.** A green AT-001.19 proves the fields land on both account-type routes at loop and on the real row at integration (read back as the operator over SQL, with the GitHub-path narrowing stated in the body, the ledger, and the plan). A green AT-001.39 proves refusal *and* absence of every write the completion could make (account, acknowledgments, predicate, organisation-by-attempted-name, memberships) plus a same-shaped control that succeeds — the seven loop variants include the three blanks and the wrong-content attestation, and the presence-vs-content check is pinned by the negative `/does not match/` assertion. A green AT-001.20 proves content only, which is exactly what the plan claims and nothing more. The attestation-match check gives `authorityStatement` behavioural force at runtime, so the copy is not test-only.

**Not found:** no divergence in the recreated function body, no missed completion literal, no stale `D4_L1` reference that typechecks, no changed id outside the three, no scope creep into `has_platform_acknowledgment` or `create-organization`.

**Left for the executor (not defects, just runtime-shaped):** the em-dash statement surviving the JSON→Postgres round trip, the `\s` class behaving in the slot's locale as it does in the C locale, and the harness's exact-match and bijection machinery accepting the new declarations — all settled by the plan's own verification commands, which I did not run by contract.

**Findings:** none.

**CODE REVIEW: CLEAN**