I have now read the full diff, the plan (including all eight dictations), the acceptance text, the slice-1 migration, the schema migrations it builds on, the fixture and live adapters, both selftests, and every assertion in the new bodies. Summary of what I verified before reporting:

- **Migration vs schema**: `account_type` enum exists with `'platform_admin'`; `projects.assigned_volunteer_id` is nullable; no policy-name collisions with slice 1; no `drop policy` anywhere; `to authenticated` on all five policies; the helper is `sql/stable/security definer/search_path=''` with every name schema-qualified; revoke-then-grant posture exact; `null = auth.uid()` genuinely fails closed; `notify pgrst` present. The platform-admin branches widen only for admins and the volunteer branch only for the assigned developer — AT-001.21/22's denials survive (their callers are neither).
- **Fixture mirror**: clause-by-clause match with the SQL for all four tables, all three branches, OR semantics, and the null-session 401; it does not delegate to `tenantReadAllowed` (that module is not imported there), and the SQL/module difference the comment claims is real (`viewer_is_org_member` admits any seated account; the module requires `ngo`).
- **Assertion teeth**: every arm fails on a concrete defect; every narrowing `return` in the new bodies is preceded by the S2-E assertion pair; the raw-text byte comparison in `at00123` is guarded by a `typeof body === 'string'` check.
- **Conformance arm**: string handling checked character-by-character (word-boundary regex, `true` detection across `true`/`(true)`/` TRUE `, `public`-in-roles handling, `qual: null`); its disclosed limitation (`org_id = org_id`) is honestly stated in its own header and in the item's does-not-prove list.
- **Route arm**: the real tree (`__root.tsx`, `README.md`, `index.tsx`) is clean under the rule; `hasOwnProperty` guards prototype names; the empty-directory throw is real.
- **Declaration**: loop 26 green / 11 red, integration 20 green / 17 red — matches the plan's arithmetic; `ui.logged-out-surface-rendering` is written identically in body and manifest; `_pending.ts` counts and the "EIGHT LABELS" enumeration are internally consistent.
- **Corrected comments**: all six step-18 sites now carry history-marked prose that matches the tree; `shipped-visibility.selftest.ts` exists and does read the basis, so the `visibility.ts` corrections name a real referent.

One observation survives my full pass. It is comment-level and sits in a file this diff does not touch, so I report it with the scope caveat and honest doubt rather than silently dropping it or inflating it.

```
[1] severity: low (comment-level; cannot produce a wrong test result, can mislead a reader)    tests/at/harness/shipped-visibility.selftest.ts:52-53
    claim: The selftest comment "The basis is what makes AT-001.40's reach attributable — an ok: true alone could not tell the admin's reach from an ordinary read of the caller's own organisation" states the exact implication that this diff's own corrected comment in supabase/functions/_shared/visibility.ts:107-115 identifies as the misleading one it fixed ("this paragraph used to imply the consumer was AT-001.40. IT IS NOT"), and that dictation S2-F ruled against — the acceptance surface deliberately does not carry the basis, and AT-001.40's reach is made attributable by the non-admin control, not by the basis.
    why it matters: A reader of the named consumer of `TenantReadBasis` walks away believing the basis is what proves AT-001.40's acceptance claim — a claim the acceptance surface cannot see. The diff corrected this confusion in the shipped module and left the same confusion standing one file away, in the very consumer the corrected comment names. The sentence is defensible if read strictly as describing the unit oracle (where the basis does distinguish the grant), which is why my confidence is moderate rather than high; but under the diff's own step-18 standard — a statement the item makes false should not survive — this instance was missed.
    unverified-runtime-claim: no (settled by reading; the only question is whether the sentence misleads)
```

Everything else the item claims — the OR'd branches replacing nothing, the argument-free helper, the fail-closed free seat, the two-tier attribution of AT-001.40's reach, the shipped-decision reading of AT-001.24, the mirror's statement-by-statement faithfulness, the self-correcting conformance tripwire, and the honesty of the does-not-prove list — I could not break after reading the diff, the plan, and the surrounding tree against each other. The migration remains unparsed by any database and every integration body unexercised; the item says so plainly, and I found no defect that a run would have caught but that reading missed.

CODE REVIEW: 1 FINDING