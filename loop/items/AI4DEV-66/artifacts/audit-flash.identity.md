# Identity extract — audit-flash (opencode reviewer-flash)

Session: ses_0075ef7ebffe6dgXKYXKSYTjOe (slug shiny-lagoon)

Extracted via `opencode export ses_0075ef7ebffe6dgXKYXKSYTjOe`.

Every assistant message in the session: providerID=opencode-go, modelID=deepseek-v4-flash,
agent=reviewer-flash. Zero mismatches against the pin
`opencode-go/deepseek-v4-flash --variant max` and the agent name `reviewer-flash`.

Tool-call cage: all 64 tool events across the session are `read`, `glob`, `grep`, or `gitdiff`
(a read-only diff tool). No `write`, `edit`, `patch`, `bash`, `task`, or `webfetch` event
occurred. See `audit-flash.toolcalls.md` for the full per-event list.
