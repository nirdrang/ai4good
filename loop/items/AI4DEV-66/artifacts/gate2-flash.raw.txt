I have completed a full trace of the diff, the plan, the schema migrations, and both adapters. Here is the review.

---

## CODE REVIEW — AI4DEV-66 slice 1 (cross-org denial, no existence oracle)

**Severity scale:** low = a real seam that does not weaken a criterion under normal operation but can pass vacuously under a specific regression, or narrows what a green proves; medium = flips a criterion or breaks a stated claim; high = the isolation/no-existence-oracle property itself fails.

**What I cleared by reading** (so the gate is visible as checked): the refusal is identical on every path in all three handlers — status, body, CORS/content-type headers, and the thrown-error path through `edgeHandler` are identifier-independent (a thrown fetch error can only echo the caller's own input, never the row's existence); the target row is genuinely the last read in `organization-dashboard` (reads 1-3 precede it, nothing after) and in `project-workspace` (target is read 2 of 2, and is itself a decision input, which the plan allows); the policy helper keys on `auth.uid()` against `org_memberships.account_id`/`acknowledgments.account_id`, both of which resolve to `auth.users.id` through `public.accounts.id` in this schema (first migration lines 38-42, 57-63, 72-79); the definer posture is `language sql`, `stable`, `security definer`, `set search_path = ''`, fully schema-qualified, with EXECUTE revoked from public and granted to `authenticated` + `service_role`; the four grants expose no column a foreign caller can reach (every table has a row-level policy; `anon` receives nothing); the live adapter is faithful (`callFunctionRaw` keeps bytes, `dataApiGet` carries the caller's own token plus `apikey`, `tenantRead` strips only the `ok` envelope); the loop-tier equality assertions have teeth (deep equality of the whole outcome including status, with `rows: null` refused separately from `[]`); the fault arm's 502-equality assertions are armed one-shot per store and fail on the natural short-circuiting re-order.

### [1] severity: low    `tests/at/suites/req-001/d-tenant-isolation.test.ts:94,103` and `tests/at/suites/req-001/_integration.ts:1273,1282,1393`
    claim: `expect(a).toMatchObject({ok:true}); if (!a.ok || a.organizationId === null) return;` — a completion that answers `ok:true` with `organizationId === null` passes the expect, trips the `return`, and the whole criterion goes green with every arm skipped.
    why it matters: the `expect` throws for the common failure (`ok:false`), so the return is only reachable on `ok:true` + null org id — but that is exactly the shape a regression in the NGO completion could take (the deployed `complete-signup` returning a null `organization_id` for an NGO), and the tests' stated purpose is that a green requires the Given to exist. At loop tier the fixture always sets the id, so the seam is integration-tier, but it is the one vacuous-pass seam in the new bodies at the most load-bearing place. It is the suite's pre-existing idiom (other bodies use it), which is why I rate it low — the new bodies inherit it rather than invent it.
    unverified-runtime-claim: no (structural).

### [2] severity: low    `tests/at/suites/req-001/_fixture.ts:1485-1531` (`dataApiRead`), `tests/at/expected/req-001.json:65-66`
    claim: the loop-tier green over the Data API arms grades the fixture's hand-written mirror of the policy set, not shipped code — so item claim 1's "a green grades shipped code rather than a copy of it" holds for the edge-surface arms and not for the probe arms; a divergence between the mirror and the migration keeps every run this change can make green.
    why it matters: `dataApiRead` re-implements the isolation rule (which rows a caller sees per table) as a prediction of `20260812120000_tenant_isolation_policy_set.sql` — the code labels it a prediction itself, and it cannot be anything else since a policy set is SQL with no TS module to import. But the migration is the one artifact in this diff with zero evidence behind it: the integration tier cannot run at this head, and the fixture's filter is the only in-change evidence for the probe arms. A policy written on the wrong column, or a predicate inverted, would ship undetected by any run this change can produce; the declaration's integration-green entries are expectations, not measurements (the item's own artifact says so in those words). The honesty list names the catalog arm and the fault arm but not this mirror, and the item's scoping sentence ("loop green is evidence about the shipped decision module and the fixture") states the scope without naming the divergence mechanism. Verify-first ruling: the goal-step integration run is the only thing that settles the mirror-vs-migration agreement; until it passes, the policy set's behavior is unverified.
    unverified-runtime-claim: no (this is about what the evidence can claim, not about runtime).

**Observations — stated because I hold them, not rated as findings:** (a) the migration's runtime behavior — policy evaluation under RLS, the EXECUTE posture on the slot, the anon privilege answers, the gateway's `verify_jwt` refusals, and the deployed functions' raw-text refusal pair — is entirely unexercised at this head; correct by reading, and a run settles it (the item records the machine fault; I do not repeat it as a finding). (b) The plan's step-8 done-criterion ("the fault arm fails if the target read is moved before any other read") is stronger than the mechanism: the arm fails only a re-ordering that also short-circuits before the later reads; a pure re-order with unconditional reads passes it — and is harmless, since unconditional reads answer the same 502 either way. The property the criterion guards is structurally present in both the fixture and the deployed handlers, so I do not rate this a disagreement that matters. (c) `publicSchemaCatalog` (`_live.ts:992`) is backed but consumed by no test in this change; a defect there would go unnoticed until slice 2's conformance arm (item honesty list #5 covers it). (d) Malformed identifiers: the deployed functions answer 502 for a non-uuid id (PostgREST 400 → `readFailed`) while the fixture answers 404 (store miss); no test probes a malformed id ("well-formed is load-bearing", stated at both tiers), and a malformed string is not an identifier in the criterion's sense, so no oracle exists between two well-formed identifiers. (e) The `if (!x.ok) return;` lines after `expect(x.ok).toBe(...)` are dead code (the expect throws first) — cosmetic only.

Out of scope, mentioned once: I found nothing defective in pre-existing code this diff touches; the `edge.ts` additions, `config.toml` block, `_pending.ts` edits and the declaration move are consistent with the plan.

CODE REVIEW: 2 FINDINGS