# PHASE-STATE — AI4DEV-81 (per-item integration verification), batch with AI4DEV-45 (CI timeout counts queue time)

**Phase: MERGE RULED — the ruling is written and rides in this head. What remains is
mechanical: the required check green on this exact head, then a mechanical publishes the
ruling comment and executes the squash merge.** Written by the merge sitting, orchestrator on
**opus @ max**, 2026-08-11. This sitting runs on opus BY DESIGN to spare fable (founder
2026-08-11); it is not a credit fallback. Chain, derived from the branch:
`AI4DEV-3 (AT harness, bring-up root)` > `AI4DEV-81 (per-item integration verification)`.
The batch partner hangs elsewhere: `AI4DEV-4 (the work skill)` >
`AI4DEV-45 (CI timeout counts queue time)`.

## What happened this sitting

1. **Both audit re-run seats came back CLEAN, and both verdicts are recorded as
   dispositions** in `merge-ruling.md` section 2 — seat one `AUDIT: CLEAN` (39 PASS, 3
   could-not-verify on runtime lines C6/C7/C9), seat two `AUDIT: CLEAN` (all 42 lines PASS,
   20 re-read on the fix delta, 22 carried forward on stated independence). A clean panel is
   evidence, not a step that silently did not happen.
2. **CI was green on the exact head `1efee7b`, confirmed by this sitting independently** of
   the conductor's observation: check run `93810797267`, workflow run `31501041580`,
   conclusion `success`, and the check run's OWN `head_sha` field reads
   `1efee7b00e7b721a85fa4758258876b5145bc266`. No classification was needed and no bypass was
   used.
3. **The verified code head is `7525e32`.** `git diff 7525e32..1efee7b -- ':(exclude)loop/items/AI4DEV-81'`
   is empty, so the code the four exact-match runs graded is the code being merged.
4. **The merge ruling records BOTH tiers**, per the contract text this very branch adds to
   `.claude/agents/orchestrator*.md`. The sitting adopted the amended contract from the tree
   rather than the copy frozen in its own system prompt: the amendment is a tightening, and a
   loop-only ruling for THIS item would be the exact defect the item removes.
5. **Two handed facts were corrected by measurement** (see `merge-ruling.md` section 6). The
   spawn prompt placed `timeout-minutes: 30` at `ci.yml` line 49 with zero change from this
   item. The value is at **line 58**, and this branch DOES change `ci.yml` — comment-only, in
   commit `a13f846`. That comment edit is part of the partner's remedy, because the partner
   item forbids justifying the raise by electing one of the two undistinguished failure
   stories, and the old comment elected one.
6. **The twin rule was verified by this sitting itself**, since this branch edits both
   orchestrator twins and a silent fork would be invisible: `twin-check: SYNCED - 251 body
   lines identical apart from the declared differences`, and the two diff hunks are the same
   text.

## What completes the next phase

The MERGE, executed by the mechanical this sitting spawns — the merge tail has exactly one
executor, and no other actor may publish the body, post the ruling, or merge.

1. The required check green on the head that carries THIS commit (the ruling rides in it, so
   the green must be re-confirmed after the push — the green must pin the exact head that
   merges).
2. The mechanical posts `merge-ruling.md` as a pull-request COMMENT, as handed. The body is
   NOT touched: it already carries the one sanctioned closes-line, and the CI reference guard
   reads title and body only (`ci.yml:326`), so quoted claims belong in a comment.
3. The mechanical executes the squash merge of pull request #53.
4. This sitting then verifies the merged state and both board items:
   `AI4DEV-81 (per-item integration verification)` closes by the pull request's own link, and
   `AI4DEV-45 (CI timeout counts queue time)` closes by the sanctioned `Closes AI4DEV-45`
   line. A dropped webhook is repaired and recorded AS a repair, never as a normal closure.

## Facts the next sitting needs

- Heads: base `466880d`; verified code head `7525e32`; the pre-ruling head `1efee7b` carries
  the audit re-run evidence and CI green. Everything after `7525e32` is record-only.
- The record is complete in `loop/items/AI4DEV-81/`: `plan.md`, five rulings files
  (`rulings-gate1.md`, `rulings-draft.md`, `rulings-gate2.md`, `rulings-fix.md`,
  `rulings-audit.md`), `merge-ruling.md`, `goal-evidence.md`, `verify-first.md`, the four gate
  and audit brief files, and `artifacts/` with every reader's raw output and distillate — both
  audit re-run seats included.
- Rejected rulings that must not be quietly reopened: S1-5 (bare bodies run at every tier),
  the any-name half of S1-3, S2-6 and F4 (the closes-line and in-file id citations). All four
  are quoted verbatim in the merge ruling.
- The recorded residual from AU-1 (a revoked handle still passes `tokensOf` after S2-2, while
  the simulation refuses a dead session at link) is a WRITTEN observation, ruled and not acted
  on — not an open defect.
- One deviation from the board's "done means" is stated openly in `merge-ruling.md` section 4:
  the GitHub sign-in checks did NOT migrate to integration green. They are declared red with
  exact kinds, because no local environment can perform a real provider handshake.
- Slot 1 stays reserved to this item until the coordinator's sweep.

## Open questions

None for the founder. Nothing contradicts ratified text and there is no scope growth. The
"done means" deviation in section 4 is environmental, was ruled at R-D1, and is recorded
rather than escalated: the ids migrated into declared reds with exact kinds, which is the
honest result the item's own doctrine requires.
