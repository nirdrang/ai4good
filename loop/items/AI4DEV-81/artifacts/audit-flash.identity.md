# Identity extract — audit-flash (AI4DEV-81)

Source: `opencode export ses_00f09f207ffeaL7ZOaPT9GdQhA`

Session info block:
- `agent`: `reviewer-flash`
- `model.providerID`: `opencode-go`
- `model.id` (modelID): `deepseek-v4-flash`
- `model.variant`: `max`

Pin required (verbatim from conductor): `opencode-go/deepseek-v4-flash`, `--variant max`, agent `reviewer-flash`.

Match: providerID + "/" + modelID = `opencode-go/deepseek-v4-flash` = required pin. variant = `max` = required. agent = `reviewer-flash` = required.

Verdict: IDENTITY MATCH — confirmed on the session info block. (Full raw export omitted from committed evidence — 3.3MB event dump embedding full tool-result contents including file reads; not suitable to commit. This summary carries the load-bearing fields.)
