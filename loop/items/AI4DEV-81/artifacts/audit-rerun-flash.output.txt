I have everything I need. All 42 checklist lines are now graded, the re-graded set from direct reading, the carried set on stated independence.

---

# AUDIT RE-RUN — AI4DEV-81 (flash seat), pinned head, fix delta `1fef027...HEAD`

**Scope note first.** I cannot execute `git diff` (no shell in this cage). Every diff-shape claim below is therefore graded on the strongest available evidence: the tree's own content, the first audit's committed citations (whose pre-fix line numbers must survive my reading), and the executor's recorded post-fix verification in `rulings-audit.md` (written with first-hand git access, and itself part of the audited record). The settle-instrument for any diff-shape dispute is the exact command in the brief.

## The fix delta, established

The pre-fix tree, as the first audit's distillate cites it (`artifacts/audit-luna.distilled.md:34`): `linkGithubIdentity` at `_live.ts:369–381` bypassed `tokensOf`; registration at `:315–322`; `tokensOf` call sites at `:221, :401, :412`. The current tree: the method is still at `:369`, its body now begins with the five-line AU-1 comment (`:370–374`) plus the guard `tokensOf(sessions, session, 'link a GitHub identity')` (`:375`); the `tokensOf` call sites below the insert sit at `:407` and `:418` — exactly `+6` from the pre-fix `:401`/`:412`; `tokensOf` itself is still at `:151–162`, byte-consistent with the gate-2 citation. A single 6-line insertion inside `linkGithubIdentity` explains every line-number movement in the file; nothing else moved. This independently corroborates the brief's "one file, six inserted lines, no deletions" for `_live.ts`.

## A. Adopted rulings

- **A1 — PASS.** `attestation.ts:100–208`: `writeAttestation(target, read, nonce)` deletes/writes the nonce (minted by `prepare()` per the header, written after the reset per `:88–99`); `attestSlot` reads back through the supplied coordinates and refuses on absent URL/nonce (`:159–165`), read failure (`:180–188`), row-count ≠ 1 (`:190–195`), nonce mismatch (`:196–201`).
- **A2 — PASS.** `capabilities.ts:492–554`: `real` only per exact enumerated name with the admission checks (name shape `:499–505`, no witness overlap `:506–512`, non-empty `:513–519`, no duplicates `:520–526`, every name exists on the surface `:527–535`, attestation brand `:536–543`); `index.ts:410–422` drives it key-by-key. No prefix grant.
- **A3 — PASS.** `index.ts:337–447`: `buildLiveLedger` builds the live email from the slot coordinates' `mailUrl` via `createLiveEmail` (probe inside), attestation first (`:342–354`); the sim branch (`createEmailProviderSim`, `:369`) is used only on the no-live-adapter fallback — untouched.
- **A4 — PASS.** `clock.ts:18–51`: `AttestedRealClock` exposes only `now()` — no `freezeAt`/`advance`, no seam; `capabilities.ts:220–237` witness branch grants on `attestationOf(value, 'clock.controlled')` and refuses a seamless object without the attestation; per-tier context types in `registry.ts` (used as `HarnessAtContext<…, 'integration'>` at `_integration.ts:33`).
- **A5 — PASS.** `capabilities.ts:629–664`: `pendingMethodProxy`'s `get` throws `CapabilityPending([capabilityName.property])` (`:643`) — `sut.accounts.<method>` on use.
- **A6 — PASS.** `db-pool.ts:338` `SLOT_JWT_EXPIRY_SECONDS = 120`; `generateSlotConfig` applies it as a standing transform (`:355–368`), fail-closed on an absent `[auth] jwt_expiry`; `db-pool.selftest.ts:388–465` asserts it is the only new difference (`:404–423`) and refuses absent settings. No transient override, no restore, anywhere in the function.
- **A7 — PASS (re-verified by direct read).** `_integration.ts:99–110` (`autoRefreshToken: true`), body `:497–566` — no refresh call of any kind.
- **A8 — PASS (re-verified).** `_integration.ts:358` — `for (const kind of ['ngo', 'volunteer'] as const)`.
- **A9 — PASS (re-verified).** `_integration.ts:156–236` — NGO type `:175`, org row `:176`, admin membership `:177–181`, acknowledgment fields incl. text version/IP/readable timestamp `:183–192`, pre-project gate `:161–164` and post-completion `:194–197`, later sign-in `:199–202`, refusal arm `:227–236`.
- **A10 — PASS.** `plan.md:237–251` settled table names exactly the manifest's eight greens (.01, .06, .07, .09, .12, .13, .14, .38) and .04's refusal as `sut.accounts.registerWithProvider`; `req-001.json:48–63` and `req-016.json:25–41` match it. (The "authored from, not the other way round" direction is a process claim carried from the previous audit; the matching claim is verified by my read.)
- **A11 — PASS.** `ci.yml:45–58`: the comment states both no-runner explanations as undistinguished and retracts the old queueing assertion; `timeout-minutes: 30` at `:58`. (Comment-only shape: gate-2 F5 settled it with git at the earlier head; the fix delta cannot reach `.github/`.)
- **A12 — PASS (RE-GRADED).** `_live.ts:151–162` refuses a sessionless handle by name; every session-taking operation routes through it — `callFunction :221`, `linkGithubIdentity :375` (first statement; the returned `LiveSession` is deliberately unbound), `signOut :407`, `refreshSession :418`. The S2-2 exception is about token-*keeping*, not refusal: `signOut` still calls `tokensOf`. Contract promise at `_contract.ts:310–313`; sim's uniform validation at `_fixture.ts:727–741`.
- **A13 — PASS.** `package.json:79` — `@supabase/supabase-js` in devDependencies. (One-line shape carried.)
- **A14 — PASS.** `req-001.json:61` — `.04` declares `capability-pending: ["sut.accounts.registerWithProvider"]`.
- **A15 — PASS.** `attestation.ts:100–127`: proof travels in (`ProvenSlotRead`), refusals for wrong/absent project and missing stack report before any connection; selftest `live-ledger.selftest.ts:292–304` drives the unproven paths.
- **A16 — PASS.** `clock.ts:37–51` and `live-email.ts:93–107` — `attestationOf(attestation, SLOT_ATTESTATION_BRAND)` with named refusals for an unbranded object.
- **A17 — PASS.** `capabilities.ts:456–465` walks the chain and stops at `Object.prototype`; selftest `live-ledger.selftest.ts:375–389` refuses a `toString`-naming enumeration; the `sut.*` any-name admission is unchanged (`:499–505`) — per the ruling, not reported as missing.
- **A18 — PASS.** `capabilities.ts:659–662`: `has` returns `true` for string properties, `Reflect.has` for symbols; selftests at `live-ledger.selftest.ts:94–174`.
- **A19 — PASS.** `registry.ts:874–885` (comment block above `AtTestFn` at `:886`): single-body form runs at every tier, typed at the richest; per-tier form is the opt-in protection.
- **A20 — PASS.** `runner.ts:784–788` — `MAIL_URL` checked against the slot config's mail port via `checkUrl` (loopback + port; selftests `runner.selftest.ts:135–153`); `live-email.ts:142–158` — probe requires parseable JSON with a non-empty string `Version`, refusing any other 200.
- **A21 — PASS.** `registry.ts:96–100` `tierTimeout` (pure, no fallback), applied at `:952`; `_integration.ts:65` `INTEGRATION_TIMEOUT_MS = 240_000`; `vitest.config.ts:18` still `testTimeout: 30_000`; selftest `live-ledger.selftest.ts:550–560`.
- **A22 — PASS (RE-GRADED).** `_live.ts:406–415`: `signOut` fetches logout and returns; no `sessions.delete` exists in code anywhere — the only occurrence is inside the S2-2 explanatory comment at `:393`. The post-logout write (`createOrganization` with the cached token, `_integration.ts:450`) therefore reaches the live stack.
- **A23 — PASS (re-verified).** `_integration.ts:204–230`: negative arm sends no acknowledgment, asserts the refusal + zero rows; the comment claims exactly what it proves and explicitly records "no fault-injection seam was added to shipped code".
- **A24 — PASS (re-verified).** `_integration.ts:358–405`: each iteration completes signup as its type — NGO with org + acknowledgment, volunteer with linked GitHub identity + acknowledgment — and asserts the account row's global type (`:401–404`).
- **A25 — PASS (re-verified).** `_integration.ts:526–562`: `signedInAs` captured at sign-in; post-rotation `getUser()` compared to it (`:556–559`); non-null account row with the same id (`:560–562`); the tautology is gone (comment `:551–555`).
- **A26 — PASS (carried).** Subject is `pr-body.md` (record territory, outside my diff); the ruling was REJECTED — presence of the citations is correct. Nothing in the code territory bears on it.
- **A27 — PASS.** `registry.ts:462` `captureFailure` is pure; selftests `live-ledger.selftest.ts:245–272` show the declarable shapes passing through unchanged and ordinary errors wrapped.
- **A28 — PASS (RE-GRADED).** `_live.ts:301–308`: bounded poll — 20 s deadline, 250 ms interval, returns the empty result at the bound; the comment at `:285–300` records the measured 4 ms delivery and the reason for the bound. Present by ruling RF-4.
- **A29 — PASS (NEW, RE-GRADED).** `_live.ts:375` is the first statement of the method body, above the operator insert (`:377–387`); the comment (`:370–374`) says verbatim what AU-1 requires — "the VALIDATION is the point and the returned tokens are deliberately unused" — and the call is exactly `tokensOf(sessions, session, 'link a GitHub identity')`. Nothing else in the method changed: the pre-fix method (`:369–381`, per the first audit) is the same signature + identityId + insert; the doc comments above (`:340–354`, `:355–368`) were pre-existing.

## B. Declared scope (re-checked in full)

- **B1 — PASS.** The fix delta's file (`_live.ts`) is already inside the previous audit's 22-file list; the brief's assertion that the list is unchanged is consistent with every line-number and citation check I ran against the other 21 files (all match the pre-fix audit's references). The full-range enumeration itself is the git instrument's; the settle-command is `git diff 466880d...HEAD -- <the brief's path list>`.
- **B2 — PASS.** Nothing under `src/` or `supabase/`: the fix delta touches only `_live.ts`; the shipped files I read (`supabase/functions/_shared/accounts.ts`, `supabase/functions/_shared/verification.ts`, both migrations) are the unchanged objects the checklist's claims cite; `rulings-fix.md` RF-6 records that the regenerated `src/routeTree.gen.ts` was restored, not committed.
- **B3 — PASS.** `bun.lock` exists at the repo root (glob). That it changed paired with the `package.json` line is a diff-shape claim carried from the previous audit; the fix delta cannot touch it.

## C. Stated facts about the code

- **C1 — PASS (re-verified).** `db-pool.ts:338` (120 s) and `_integration.ts:47` (`SLOT_JWT_EXPIRY_MS = 120_000`); the transform emits `jwt_expiry = 120` (`db-pool.ts:365–368`).
- **C2 — PASS (carried).** Gate-2 F5 settled the insertions-only shape with git (43 and 17 added lines, zero deletions, `rulings-gate2.md:264–268`); the fix delta cannot reach the manifests; the previous audit graded it PASS.
- **C3 — PASS (re-verified).** `req-001.json:48–57` — exactly eight integration greens (.01, .06, .07, .09, .12, .13, .14, .38); every other id red with an exact declarable kind (`capability-pending` with named capabilities at `:59–63`, `pending/sut-missing` for the rest). `req-016.json:25–41` — every id red.
- **C4 — PASS (RE-GRADED).** `_live.ts:271–274`: soft-line-break removal first, then `=XX` hex decode (plus `&amp;`), before any link matching — the claimed quoted-printable decode, with the load-bearing order documented at `:256–270`.
- **C5 — PASS (RE-GRADED, the AU-2 correction).** `_live.ts:520` sends `{ name: organizationName }` to the deployed function; `:549–550` reads `org_id as organization_id` with the seam comment at `:543–546` ("THE COLUMN IS `org_id`, not `organization_id`"); `:383` casts `identity_data` through `::text::jsonb`; `:486` hands the shipped extractor `new Date(confirmedAt).toISOString()` — a string, never a `Date` (`:475–481` documents the Date-object failure it fixed).
- **C6 — PASS (re-verified by full read).** `_integration.ts:422–476`: scoped logout (`:443`; `?scope=local` in `_live.ts:410`) with sibling-session control (`:436–446`); post-logout write refused by the live stack (`:450–452`); re-authentication (`:455–459`); expiry arm waits `SLOT_JWT_EXPIRY_MS + 15_000` with the margin on the far side (`:461–467`); refresh restores access (`:471–475`).
- **C7 — PASS (re-verified by full read).** `_integration.ts:497–566`: the only waits are `wait(5_000)` polls of `client.auth.getSession()` (`:538–542`); no refresh call of any kind.
- **C8 — PASS (re-verified).** `supabase/functions/_shared/accounts.ts:215` refuses `rawVersion.trim() === ''` before the transaction; migration `20260808120000:78` constrains `length(btrim(text_version)) > 0`; the acknowledgment insert is the last write (`20260809090000:399–400`). JS `trim()` strips a superset of `btrim`'s space-only default, so the DB-refused set is a subset of the validator-refused set — empty intersection by construction (`verify-first.md:259–345`). No fault-injection seam in shipped code.
- **C9 — PASS (coverage re-verified; count cited, not re-derived).** The selftest files contain the named coverage — attestation refusals (`live-ledger.selftest.ts:190–231`, `:292–304`), brand validations (`:314–340`), enumeration/proxy rules (`:94–174`, `:375–389`), mail probe refusals (`:332–340`), tier-timeout choice (`:550–560`), capture pass-through (`:245–272`). The 327→344 count is execution evidence: the executor's recorded run at this head ("`at:selftest` 344 passed, count UNCHANGED", `rulings-audit.md:125`) is the CI-side evidence the brief tells me to cite; the delta adds no selftest, so the count cannot have moved.
- **C10 — PASS (NEW, RE-GRADED).** The delta's code-territory content is exactly the six lines at `_live.ts:370–375` (five comment lines + the guard), zero deletions: every pre-fix citation of the file survives my reading with a single localized +6 shift (`:401→:407`, `:412→:418`) and `tokensOf` at `:151–162` untouched, which is consistent only with that one insert. "No other code file moved": corroborated by the same check plus the record's post-fix statement; the exact enumeration is the git instrument's (`git diff 1fef027...HEAD -- <path list>`).

## Carry-forward independence, stated

- **Re-graded (delta's file):** A12, A22, A28, A29, C4, C5, C10.
- **Re-verified by direct full read despite non-reach:** A7, A8, A9, A23, A24, A25, C6, C7 — the delta's only behavioral addition is the sessionless-handle refusal in `linkGithubIdentity`; the only integration call sites (`_integration.ts:272`, `:396`) pass signed-in sessions (`registerConfirmAndSignIn` returns `signInWithEmailPassword`'s session, `:118–138`), so the guard cannot fire on any exercised path and no declared outcome can shift.
- **Carried on non-reach:** A1–A6, A10, A11, A13–A21, A26, A27, B1–B3, C1, C2, C3, C8, C9 — the delta touches no harness file, no manifest, no workflow, no package file; `_live.ts`'s only harness import is the type-only `LiveVendors`. The brief's caution applies here: the harness-machinery lines rest on the previous audit's single landed gate reading (flash's gate-2 slice-1 seat failed to land), and I carry them on exactly that independence, with my spot-checks (attestation, capabilities, clock, live-email, registry, db-pool, runner, index, selftests) corroborating each.

## Findings

None. One out-of-scope observation, mentioned once: `loop/items/AI4DEV-81/artifacts/audit-rerun-flash.events.jsonl` is a committed tool-transcript file in the record directory — record territory, outside my source-only diff; not graded.

**unverified-runtime-claim:** none of the runtime claims here were re-derived by me; execution evidence (selftest count, exact-match runs, typecheck) is cited from the executor's recorded verification at this head, as the brief directs. Diff-shape claims are tree-corroborated and record-corroborated; the settle-instruments are the two `git diff` commands named above.

AUDIT: CLEAN