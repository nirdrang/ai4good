I have completed the review. All attack directions have been traced — the acceptance criteria against each integration body, the declaration manifests against both code and plan, the live adapter's evidence rules, the sessionless-handle design, the dependency seam, the process text, and the verify-first claims against the code. The verification produced five findings.

---

**CODE REVIEW: 5 FINDINGS**

Severity scale: **high** = the item's core claim (a declared green) cannot hold as written; **medium** = a real but bounded correctness gap; **low** = an assertion/claim that does not do what its comment says, or a rule stated absolutely that the diff contradicts; **verify-first** = a claim I cannot settle with read-only tools, with the exact command that settles it.

---

**[1] high    tests/at/suites/req-001/_integration.ts:385  (with _live.ts:151-162, 329-339)**
**claim:** AT-001.12's revocation arm always throws client-side, so the declared green for AT-001.12 is unachievable and the revocation clause is never measured against the live stack.
**why it matters:** `signOut` deletes the session from the adapter's own token store (`sessions.delete(session.sessionId)` at `_live.ts:338`); the very next call, `createOrganization(session, ...)` at `_integration.ts:385`, goes through `callFunction` → `tokensOf`, which throws `Error("refusing to call the deployed create-organization: ... holds NO session ...")` (`_live.ts:151-162`). The throw happens before any request leaves the process, so `expect(revokedWrite.ok, 'a revoked session performed a write').toBe(false)` at line 386 never runs and the body fails with an undeclarable `Error` (fits neither declarable red kind). Every path fails: if logout succeeds the store deletion kills the call; if logout answers ≥400, `signOut` itself throws. The integration declaration (`req-001.json` lines 52) says AT-001.12 is green, so `at:verify req-001 --tier integration --expect` cannot exit 0, and plan step 7's done-criterion fails. Additionally, even if the throw were converted to a refusal, the write never reaches GoTrue, so "access ends when the session ends (revocation)" would be proved by the adapter's own bookkeeping, not by the live stack — the server-side 403 for a dead session (measured and recorded in `_shared/edge.ts:200-201`) is what the arm should be driving.
**unverified-runtime-claim: no** — fully determined by the code (the only runtime question is which error text appears).

---

**[2] high    tests/at/suites/req-001/_integration.ts:399, 452-458  (with tests/at/vitest.config.ts:18, tests/at/harness/runner.ts:1352-1353)**
**claim:** The two declared-green time-based bodies wait far past the 30-second vitest `testTimeout`, so both AT-001.12 and AT-001.13 time out and report red, contradicting their declared green.
**why it matters:** `at00112` executes `await wait(SLOT_JWT_EXPIRY_MS + 15_000)` = a 135-second sleep inside the test (`_integration.ts:399`); `at00113` polls `while (Date.now() < deadline)` with `deadline = SLOT_JWT_EXPIRY_MS + 30_000` = 150 s (`_integration.ts:452-458`), and the executor's own verify-first (d) measured rotation "about 30 seconds into a 120-second token" — so at00113 needs roughly 35-40 s of test time including the preceding register/confirm/sign-in round trips. `tests/at/vitest.config.ts:18` sets `testTimeout: 30_000`, and the runner spawns vitest with that config and no timeout override (`runner.ts:1352-1353`, 1344-1363). Vitest kills each test at 30 s with a timeout failure — red, in an undeclarable shape, against a green declaration. This also means the expiry arm, the refresh arm, and the auto-refresh observation never complete.
**unverified-runtime-claim: yes** — the at00112 half (135 s vs 30 s) is statically certain; the at00113 half depends on the observed rotation timing (verify-first (d): ~30 s). Settle by running `bun run at:verify req-001 --tier integration --expect` on slot 1, or by checking vitest's reported failure detail for "timed out".

---

**[3] low    tests/at/suites/req-001/_integration.ts:469-470**
**claim:** AT-001.13's "same account" assertion is vacuous — it always passes — and its comment claims a fact the code does not establish.
**why it matters:** The body registers, confirms and signs in but never completes signup, so no `public.accounts` row exists for this user; `sut.account(...)` therefore always returns `null`, and `expect(account === null || account.id.length > 0).toBe(true)` passes on the null branch in every run. The comment ("AND THE ACCOUNT IS THE SAME ONE, so the rotation extended this user's work rather than silently starting somebody else's") is not supported: the user id from `getUser()` is never compared with the sign-in's identity, and the assertion cannot fail. The criterion's own text (automatic rotation, continued access) is still proved by the token arms, so this does not invalidate the green — but the body asserts something that proves nothing and claims something it does not measure.
**unverified-runtime-claim: no** — `sut.account` reads `public.accounts` (`_live.ts:440-445`) and the row is only created by `complete_signup`, which this body never calls.

---

**[4] low (ambiguity stated)    tests/at/suites/req-001/_live.ts:35, 61, 249, 332, 361, 369, 391, 411; _integration.ts:482; loop/items/AI4DEV-81/plan.md; loop/items/AI4DEV-81/verify-first.md**
**claim:** The board-item hygiene rule — "no board item id other than AI4DEV-81 may appear in any file a pull request displays" — is violated by foreign ids in files this branch authored and the PR displays.
**why it matters:** `_live.ts` cites AI4DEV-58/59/60 eight times, `_integration.ts` cites AI4DEV-60 once, and the item record (part of the branch diff) cites AI4DEV-45 (the partner) and AI4DEV-58/59/60. Under the rule as stated in the review brief, these are violations. The mitigating context, stated so the ruling is fair: the whole repository's convention cites prior item ids in comments (pre-existing files like `_fixture.ts`, `_contract.ts`, `_pending.ts` and the other suite files do it pervasively), and the CI reference guard reads only the PR title and body, so no mechanical harm follows from ids in committed files. If the rule intends to cover only text that can move items (title/body/comments), these references are fine; if it is literal, the new files and record need rewording to "the email-verification item's proof transcript" style. I report the literal result and leave the ruling to the orchestrator.
**unverified-runtime-claim: no**

---

**[5] verify-first    .github/workflows/ci.yml; tests/at/expected/req-001.json; tests/at/expected/req-016.json**
**claim:** The two diff-level constraints — ci.yml "comment-only, zero behaviour change" and "loop tier declarations and loop body meaning unchanged" — cannot be confirmed with my read-only tools; the content checks that I *can* do all pass.
**why it matters:** What I verified directly: the timeout comment at `ci.yml:45-57` is the D10 neutralization — it states both candidate explanations (hosted-capacity queueing; a dead runner lane) as undistinguished ("Two explanations fit the evidence equally well"), elects neither, records that "An earlier version of this comment asserted the queueing one; that assertion had nothing behind it", and keeps `timeout-minutes: 30` — exactly what the amended plan and gate-1 ruling 11 require. The loop sections of both manifests are internally consistent with the registered ids (13 green + 24 red = 37 for req-001; the integration additions are additive), and the two modified suite files preserve every loop body verbatim under the new `default:` key — the only registration-form change is D5's per-tier map. What I cannot establish: that the branch's ci.yml diff contains *nothing but* the comment (the file also carries the twin guard, the prose fast lane, the ownership guard, and the reference guard with the batch-partner exception — all plausibly pre-existing on main, and PHASE-STATE.md asserts "this branch's only ci.yml change is the comment neutralization"), and that the loop sections of the manifests are byte-identical to main. Settle with: `git diff 466880d...HEAD -- .github/workflows/ci.yml tests/at/expected/req-001.json tests/at/expected/req-016.json tests/at/suites/req-001/` — read-only, no execution needed.
**unverified-runtime-claim: yes**

---

**Checks that came back clean, so the empty parts of the gate are visible:** each green body against its criterion's full text — AT-001.01 (account type NGO, org row, admin membership, acknowledgment timestamp/IP/text version, pre-project gate via the shipped SQL predicate, later sign-in, atomicity as negative arm), AT-001.06 (NGO control first, refusal with no rows, reason matches the shipped `ngoOnlyActionAllowed` text), AT-001.07 (provisioned admin sign-in, escalation refused with no row, reason matches the shipped `parseAccountType` text), AT-001.09 (parameterized over both `ngo` and `volunteer`, unverified-before/link-flips/verified-after plus the early sign-in refusal), AT-001.14 (new password works, old refused). The declaration manifests match the code's actual refusal names and kinds on every red (AT-001.02 `registerWithGithub`, .03/.04 `registerWithProvider` — confirmed as the true first refusals on each body's path, matching draft ruling R-D5 — .05 `vendors.github-public-statistics` against the shipped stub whose own text says "no GitHub API was called", .10 `sendDiscoveryMessage`, the 24 `pending`/`sut-missing` ids whose `notLanded` bodies throw before `open()`, and req-016's 12 ids whose `capability-pending` names match the sorted ledger stand-ins `fixtures.worlds, sut.notifications` fired by the registry gate before any body runs). The live adapter fabricates nothing: sessions are stored only from real password grants, refresh grants and the admin user API; the sessionless registration handle can only refuse (always red); `linkGithubIdentity` is a documented operator Given; `provisionPlatformAdmin` is the criterion's own provisioning recipe. The mail-catcher URL is read from the slot's own `supabase status` report (`runner.ts:708-717`) and passed through unmodified to the child and the adapter — never recomputed from the port offset — and `SLOT_JWT_EXPIRY_SECONDS = 120` (`db-pool.ts:338`) equals the bodies' `SLOT_JWT_EXPIRY_MS = 120_000` (`_integration.ts:47`). The supabase-js load seam fails red on absent module, missing `createClient`, or shape drift, and its declared client surface is honest against the real library's. The process text (WORKFLOW.md goal step 41c and merge ruling 62, executor.md goal definition, both orchestrator merge-ruling paragraphs) states the two-tier goal step, the slot-evidence merge line, and the live-proof demotion coherently with the plan; the orchestrator twins are identical outside the frontmatter and the two declared fallback paragraphs, exactly as `loop/work/twin-check.ps1` sanctions.

CODE REVIEW: 5 FINDINGS