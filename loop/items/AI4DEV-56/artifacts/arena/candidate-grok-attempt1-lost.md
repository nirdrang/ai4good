loop/items/AI4DEV-56/artifacts/arena/candidate-grok.md
7942 words
One edge function dispatches a closed command vocabulary; each command is inserted as an audit row first and account, seat, and escalation state are projections of that row.
Least sure: whether `pg_trigger_depth() = 0` still distinguishes a volunteer self-unlink from a vendor user-delete cascade once the refusal is a real migration function rather than the probe.
Graft if this loses: `apply_admin_event` reading the audit row as the only writer of lifecycle and seats, so a reviewer can reconstruct state from `admin_audit` alone.