• Now reading the two depth files — the migration and the config diff.

• Both depth files read. Now the TypeScript side and supporting files for context.

• Now the test suite, fixture, expected declarations, and the proof script.

• Now the remaining suite files and the harness/registration details.

• Now the manifest, the pending ledger, and the config claims.

• One last mechanical check on the `AtPending` shape the stubs rely on.

