AI4DEV-21 (fake Stripe, GitHub, Anthropic) - F4: AT-016.05 falsified against a fixture that routes
an IN-APP row through the email provider.

This is terra's finding 2 (MAJOR): the diff introduced the invariant "in-app never reaches the
provider" and left it with no oracle at all - AT-016.01's provider-side check rejects only attempts
whose event id is unknown, so an in-app row sent through the provider under its own valid event id
passed everything. Ruling F-B. This transcript is that ruling's falsification.

CAPTURED AGAINST COMMIT 360fb23 ("AI4DEV-21: the Gate 2 round - the provider trace becomes the
oracle it claimed to be"), the round's code commit. The one temporary edit below was reverted
immediately afterwards, and `git diff` against that commit is empty.

WHAT WAS MUTATED, one condition in tests/at/suites/req-016/_fixture.ts inside `runDeliveryPass` -
the low-tone row (`pm_item.status_changed`, in-app only by the requirement's own delivery defaults)
stops taking the in-app shortcut and is handed to the email provider instead:

    -  if (delivery.channel !== 'email') {
    +  if (delivery.channel !== 'email' && delivery.type !== 'pm_item.status_changed') {
         markSent(delivery);
         continue;
       }

The delivery record stays honest throughout: the row still says `channel: 'inapp'`, still reaches
`sent`, and still lands on exactly the recipient the taxonomy names.

WHY EVERYTHING ELSE PASSED IT. AT-016.03, AT-016.05's original clauses, AT-016.06 and AT-016.12 all
read the delivery rows the system under test wrote about itself, and those rows are unchanged.
AT-016.01's orphan check filters provider attempts by event-id membership, and this attempt carries
a perfectly valid event id. Before F-B, the run under this mutation was fully green apart from the
declared capability-pending red - which is terra's point exactly: the invariant existed only as a
sentence in a comment.

WHAT THE NEW ORACLE DOES. The taxonomy capture now snapshots the provider trace per row, and
AT-016.05 reconciles it in BOTH directions. Both halves fire on this one mutation: the off-channel
attempt is named, and the accepted-pairs equality reports the pair as unexpected because a row the
taxonomy keeps in-app owes the provider nothing at all.

WHAT IT DOES NOT PROVE. Nothing about the product: `sut.notifications` is a declared stand-in and
the registry bars every stand-in from the tiers that gate. It proves the ORACLE bites.

REPRODUCE: apply the edit above, run the two commands below, revert.

-----------------------------------------------------------------------------------------------
run at 2026-08-04T20:12:52.6159908+03:00, HEAD 360fb23d4e5e3a41504f5f2bb2eac0856c136d70 plus the one uncommitted mutation above

$ bun tests/at/harness/runner.ts "req-016" --tier loop --expect
JSON report written to C:/Users/nirdr/AppData/Local/Temp/at-verify-ogc1ym/vitest-report.json

at:verify req-016 --tier loop
  AT-016.01    red      CapabilityPending: CAPABILITY PENDING — H3 static provider scan
  AT-016.02    green    registered events equal the taxonomy exactly, are immutable, and carry no CR/scope-change event
  AT-016.03    green    every taxonomy row delivers to exactly its recipients on exactly its channels, with the named payloads
  AT-016.04    green    sensitive negatives: no candidacy to the NGO, no vetting outcome to the volunteer, no donation event
  AT-016.05    red      AssertionError: rows whose provider-side trace contradicts the channels the taxonomy gives them: expected [ …(2) ] to deeply equal []
  AT-016.06    green    a documented delivery default exists for every taxonomy row
  AT-016.07    green    one logical event per committed event, one delivery per recipient-channel pair, across a restart
  AT-016.08    green    a comment burst delivers the count the pinned anti-spam configuration prescribes, on two different configurations
  AT-016.09    green    every guarded transition writes its notification event atomically under an induced fault
  AT-016.10    green    recipients resolve at event creation: the old holder receives, the new holder is excluded
  AT-016.11    green    sent only on provider acceptance; unconfirmed sends retry; a lost ack mints no duplicate
  AT-016.12    green    an escalation-tier event notifies both the NGO and the platform admin
  12 P0: 10 green, 2 red, 0 missing
  DEVIATION: AT-016.05 — declared green, reported red: AssertionError: rows whose provider-side trace contradicts the channels the taxonomy gives them: expected [ …(2) ] to deeply equal []
  DEVIATION: the report counts 2 failed tests but the declaration declares 1 red — a failure outside the declared ids (an untagged test, a failing hook) looks exactly like this
  DEVIATION: the report counts 10 passed tests but the declaration declares 11 greens
  EXPECT FAILURE: 3 deviation(s) from the declaration. A red that turned green is a failure too — if reality improved, update the declaration in the same change.
-> exit code 1

AND THE FULL ASSERTION DETAIL, which the runner's one-line report truncates:

$ AT_TIER=loop bunx vitest run --root tests/at --config vitest.config.ts suites/req-016/d-taxonomy-evidence.test.ts

 RUN  v4.1.10 C:/Users/nirdr/Downloads/ai4good/.claude/worktrees/agent-af1e90584864a51d0/tests/at

 ❯ suites/req-016/d-taxonomy-evidence.test.ts (5 tests | 1 failed) 25ms
     × AT-016.05 — every critical class goes out by email; the low-tone event is in-app only 5ms

(the block below is vitest's failure detail, which it writes to stderr — pasted here verbatim from
the same run, because PowerShell's native-command redirection mangles it into ErrorRecords)

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  suites/req-016/d-taxonomy-evidence.test.ts > AT-REQ-016 taxonomy capture and projections > AT-016.05 — every critical class goes out by email; the low-tone event is in-app only
AssertionError: rows whose provider-side trace contradicts the channels the taxonomy gives them: expected [ …(2) ] to deeply equal []

- Expected
+ Received

- []
+ [
+   "pm_item.status_changed: a inapp delivery to actor-ngo arrived at the EMAIL provider",
+   "pm_item.status_changed: at the provider, actor-ngo:inapp: 1 unexpected deliveries",
+ ]

 ❯ suites/req-016/d-taxonomy-evidence.test.ts:273:105
    271|       );
    272|     }
    273|     expect(problems, 'rows whose provider-side trace contradicts the c…
       |                                                                                                         ^
    274|   });
    275|
 ❯ executeRegisteredBody harness/registry.ts:486:3
 ❯ runTrackedTest harness/registry.ts:555:5
 ❯ harness/registry.ts:778:5

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯

 Test Files  1 failed (1)
      Tests  1 failed | 4 passed (5)
   Start at  20:12:53
   Duration  283ms (transform 118ms, setup 0ms, import 136ms, tests 25ms, environment 0ms)

-> exit code 1
