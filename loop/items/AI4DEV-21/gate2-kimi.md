• I'll review the tree directly. Starting with the diff rendering and the intent record.

• Now the remaining evidence: taxonomy rows, conformance oracle, expected/README, proofs, verify transcripts, capabilities/registry internals, spec amendment.

• One last check — the actual diff hunks for the fixture and the test file, to catch anything beyond the claimed changes.

• All files reviewed. Let me independently re-run the two key verification commands rather than trusting the transcripts.

• # Gate 2 review — AI4DEV-21 (email provider stand-in)

  Reviewed against the tree at `9435eb7` (merge-base `fa18fda`), every changed file read in full, all seven attack directions worked, and the verification re-run independently in this worktree (`at:selftest` 162/162 green; `at:verify req-016 --tier loop --expect` 11 green / 1 red, matches the declaration exactly). Findings only.

  ## Findings

  **1. MINOR — the provider-side trace is never reconciled with the event's resolved recipients, so a wrong-recipient send at the seam is green.**
  `tests/at/suites/req-016/c-reliability-guard.test.ts:253-258` and `tests/at/suites/req-016/a-emitter-and-taxonomy.test.ts:70-72`.
  The (c) accepted-side oracle checks only the *internal consistency* of `accepted()` (`acceptedPairs.length === new Set(acceptedPairs).size`), and AT-016.01's orphan check filters attempts by `eventId` membership only. Concrete passing implementation that violates what AT-016.11(c) claims: a fixture whose lost-ack retry calls `vendors.email.deliver({ recipientId: <the ngo actor>, eventId, channel: 'email' })` instead of the original recipient, while reporting the correct delivery rows. Walk it: `accepted()` for the event then holds two *distinct* pairs (`vol:email` from the `ack_lost`, `ngo:email` from the retry) → length 2 === set size 2 → passes; the trace assertion reads outcomes only (`['ack_lost','accepted']`) → passes; delivery pair checks pass; AT-016.01's orphan check passes (the eventId is valid). The provider trace then records the NGO being mailed a revocation the taxonomy restricts to the volunteer, and nothing anywhere fails. The seam's entire purpose is out-of-band contradiction of the SUT's own records, and this is the one axis the out-of-band record is never read on. Fix direction: in (c), assert the accepted email pairs *equal* the expected set (`expectedPairs(w.actors, ['volunteer'], ['email'])`), not merely that they contain no internal duplicate.

  **2. MINOR — the conformance wall never exercises the channel component of the idempotency identity.**
  `tests/at/harness/vendors.ts:51-56` documents the identity as `eventId:recipientId:channel`; all nine cases in `vendors.selftest.ts` (and both harness-level cases) send only `channel: 'email'`. A sim that deduped on `eventId:recipientId` alone passes the entire wall and the entire suite — the false-green construction is `sendIdentity = eventId + ':' + recipientId`, which satisfies every existing assertion, yet would treat a same-pair send on a second channel as a replay and suppress a physically new send. Nothing pins that channel-scoped identities are independent. One added case — `deliver(A on 'email')`, `deliver(A on 'sms')` → both physically accepted, `accepted()` holds both — closes it. Reachable only via future channels today (REQ-016's `inapp` bypasses the port by design), hence MINOR not MAJOR.

  **3. NOTE — `EmailProviderPort` / `ProviderSend` / `EmailProviderStandIn` staying out of `ALIAS_PROTECTED`: the ruling survives the attack.**
  `tests/at/harness/type-invention.selftest.ts:128-150`, `tests/at/harness/index.ts:141,163`. I looked for a route from a test body to these types and found none: `open()` hands the body `h` and `w`; `h.vendors` is `Vendors<Channel> = { email: EmailProviderSim }` (the sim face only, `index.ts:141`); the port travels exclusively through `FixtureAdapterModule.createFixtureAdapter`'s options (`index.ts:33-43`) into the adapter, and the suite-adapters derivation reads sut/world types off the adapter's *return*, not its options parameter. Independently, all three types are aliases (`vendors.ts:30,40,45`), so even the documented known-open hand-reconstruction route gets nothing to merge into. Accept the reasoning; no finding.

  **4. NOTE — `expected/README.md:60` still illustrates exactness with a counterfactual capability name.**
  The schema example was correctly moved to AT-016.01's real red (the accepted ride-along), but line 60's prose — "declaring `H5 email provider` when the real detail says `H5 email provider simulator` fails" — now cites a detail no seam in the tree produces. Illustrative only, but it is the same declared-vs-real drift the item itself hunts. The other surviving mentions are all legitimate: `expected.selftest.ts:207-209` (synthetic `detailMatches` fixture data), `index.ts:157` (historical comment), and item/prose files under `loop/`.

  **5. NOTE — `NotificationEvent.attempts` counts worker passes that touched the event, not send attempts.**
  `tests/at/suites/req-016/_fixture.ts:354,379-385`. The counter increments once per pass per event even when the only attempted delivery was in-app (which never reaches the provider). The AT-016.11(b)/(c) assertions (`attempts >= 2`) are satisfied honestly in both flows — the provider trace is the load-bearing oracle and the suite says so in its own comments — but the field's name overstates what it measures, and a future reader may rely on it as a send count.

  ## Attack directions that produced no finding

  - **AT-016.11 false greens.** I walked every clause against concrete cheating implementations: marking sent without consulting the port (fails (a)'s trace assertion — F1 transcript confirms, `proof-f1.txt:50`); marking sent on `no_ack` (fails the new unacked-delivery assertion at `c-reliability-guard.test.ts:216-220`); retrying inside the first pass (fails the exact `['ack_lost']` trace at :208-211); never retrying (fails the exact `['ack_lost','accepted']` trace at :229-232); a sim that records replays as second acceptances (fails (c)'s pair dedupe — F2 transcript plus maintained wall case 3, `proof-f2.txt:50,72`); a sim that drops replays from `attempts()` (fails the same trace assertion); padding the trace with fabricated eventIds (caught by AT-016.01's orphan check). The idempotency/queue interplay is clean: rejected identities are not protected (`vendors.ts:106-108`), replays consume no forced outcome (`vendors.ts:99-102`), and both are pinned by wall cases 3, 4 and 7.
  - **AT-016.07 stamp survival.** A pre-restart stamp cannot survive while passing: the precondition at `b-delivery-defaults.test.ts:32-35` makes any pre-restart send red, and first-send-owns-the-stamp (`_fixture.ts:338-341`) is independently pinned by the conformance case at `conformance.selftest.ts:550-586`.
  - **AT-016.09 rollback × drain.** The crashed fire rolls back before the event/delivery/ops-item writes (`_fixture.ts:277-321`), so the new drain has no leftover delivery to send; the widened oracle reads deliveries after the drain and would catch one either way.
  - **AT-016.03/.05/.12.** With the sim in default-accept, one quiescent pass sends everything; the capture's observable rows are unchanged, and all four ids verified green in my own run.
  - **Declaration edit.** The static seam throws exactly `CapabilityPending(['H3 static provider scan'])` (`index.ts:162`), AT-016.01 touches `h.static` first (`a-emitter-and-taxonomy.test.ts:28`), and the declaration matches order-sensitively (`req-016.json:21`); confirmed live by the `--expect` run.
  - **Cross-test leakage.** The sim is built per `createHarness()` (`index.ts:126`), `openWorld` builds a fresh harness per `open()` (`registry.ts:608`), teardown is tracked per test, no module-level state exists in `vendors.ts` or the selftest, and `registry.ts:618-620` refuses any stubbed capability above loop tier — the stand-in cannot reach the gating tiers.
  - **Drain termination.** The structural argument holds for every reachable state: each pass attempts every non-`sent` delivery, in-app always completes, and every email send either consumes one of the finitely many forced outcomes or is accepted/replayed-as-accepted; passes are bounded by queue length + 1. Even a hypothetical `failed`-state delivery is re-attempted rather than spun on. The `passes` option matches `_contract.ts:157-163`'s documented promise.
  - **Plan/rulings vs code.** D2–D8 as amended are what the code does; the founder ruling is folded into `loop/bringup/AI4DEV-3-at-harness.md` item 8 and the H5 breakdown with owners AI4DEV-38..42; the conformance-list amendment landed sorted at `conformance.selftest.ts:158`; all six adaptations and the ride-along match the tree. No silent divergence found.

  ## Verdict

  **APPROVE.** Findings 1 and 2 are real oracle gaps but both require a SUT/sim that lies on an axis no current consumer exercises; they are follow-up hardening (an accepted-pairs equality assertion in AT-016.11(c), one cross-channel idempotency case in the wall), not merge blockers. Findings 3–5 are advisory. The green the item claims — AT-016.11 discriminating on a maintained oracle, the declaration edit exact, the stand-in barred from gating tiers — is genuinely forced by the tree as it stands.

