1. **BLOCKER** — `AT-016.11` never binds provider acceptance to the intended recipient/channel pair. At `tests/at/suites/req-016/c-reliability-guard.test.ts:167` and `:252`, traces are filtered only by `eventId`; `:258` checks only nonempty, unique accepted pairs. A fixture that sends every email to `wrong-recipient` while retaining the intended recipient in its delivery record passes clauses (a), (b), and (c), including the lost-ack replay. The 45-row capture also reads those delivery records rather than provider pairs. This green would claim delivery acceptance for a pair the provider never accepted.

2. **MAJOR** — The claimed “in-app never touches the provider” invariant has no oracle. `tests/at/suites/req-016/a-emitter-and-taxonomy.test.ts:69`–`:72` rejects only provider attempts with an unknown event ID. A fixture can route `thread.comment` or `pm_item.status_changed` through the email provider using their valid event IDs and still pass once AT-016.01’s static scan is implemented. This contradicts the invariant implemented at `tests/at/suites/req-016/_fixture.ts:360`.

3. **MINOR** — `drainDeliveries` does not honor or reject invalid `passes` values. At `tests/at/suites/req-016/_fixture.ts:421`, `passes: 1.5` executes two passes, while `passes: NaN` runs to quiescence because `pass >= NaN` is always false. That conflicts with the contract’s “N worker passes” promise at `_contract.ts:157`. Existing coverage uses only `passes: 1`.

4. **MINOR** — The conformance wall does not fully pin claimed forced-queue semantics. `tests/at/harness/vendors.selftest.ts:110` checks invalid armings only on an empty queue, so an implementation that clears an existing queue before rejecting an invalid arming passes. Its mixed FIFO case at `:150` uses only count `1`; a chunk-round-robin implementation would pass it and the single-kind `rejectNext(3)` test, yet return `rejected, ack_lost, rejected, ack_lost` for `rejectNext(2); acceptButLoseAck(2)` instead of FIFO order.

5. **MINOR** — `tests/at/harness/vendors.ts:54` serializes identity with an unescaped `:` delimiter despite all three fields being unrestricted strings. `{ eventId: "e:a", recipientId: "b" }` and `{ eventId: "e", recipientId: "a:b" }` collide, so the second physical send is incorrectly treated as a replay. No conformance case covers this.

CHANGES REQUIRED (findings 1 and 2 gate).