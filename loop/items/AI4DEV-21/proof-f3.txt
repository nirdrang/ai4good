AI4DEV-21 (fake Stripe, GitHub, Anthropic) - F3: AT-016.11 falsified against a fixture that mails
the WRONG RECIPIENT at the seam while keeping honest delivery records.

This is the Gate 2 finding both reviewers found independently (terra 1, BLOCKER; Kimi 1, MINOR):
the provider-side trace was filtered by event id and never reconciled with the recipient/channel
pairs the taxonomy resolved. Ruling F-A. This transcript is that ruling's falsification.

CAPTURED AGAINST COMMIT 360fb23 ("AI4DEV-21: the Gate 2 round - the provider trace becomes the
oracle it claimed to be"), the round's code commit. The one temporary edit below was reverted
immediately afterwards, and `git diff` against that commit is empty.

WHAT WAS MUTATED, in tests/at/suites/req-016/_fixture.ts inside `runDeliveryPass`, on the send to
the provider - the RETRY of the lost-ack row goes to the NGO instead of the volunteer, while the
delivery record it belongs to keeps naming the volunteer:

       const outcome = vendors.email.deliver({
    -    recipientId: delivery.recipientId,
    +    recipientId:
    +      delivery.type === 'access.key_revoked' && delivery.state === 'retrying'
    +        ? (currentWorld?.actors.ngo ?? delivery.recipientId)
    +        : delivery.recipientId,
         eventId: delivery.eventId,
         channel: delivery.channel,
       });

That is exactly the cheating implementation Kimi walked: the seam's whole purpose is to contradict
the system's own records out of band, and the recipient was the one axis the out-of-band record was
never read on.

WHY THE OLD ORACLE PASSED IT, stated so the improvement is measurable rather than asserted. The
clause used to check only that the accepted pairs held no duplicate AMONG THEMSELVES:

    expect(acceptedPairs.length, 'the provider never accepted the send at all').toBeGreaterThan(0);
    expect(acceptedPairs.length, 'the provider accepted the same recipient-channel pair twice')
      .toBe(new Set(acceptedPairs).size);

Under this mutation `accepted()` for the event holds TWO DISTINCT pairs - `volunteer:email` from
the lost-ack acceptance and `ngo:email` from the misdirected retry - so length 2 equals set size 2
and both assertions pass. Every other clause passes too: the outcome trace still reads
['ack_lost','accepted'], the delivery rows still name the volunteer and still reach `sent`, one
logical event remains, and AT-016.01's orphan check sees a valid event id. Nothing anywhere went
red, while the provider's own record said the NGO had been mailed a key revocation the taxonomy
restricts to the volunteer.

WHAT THE NEW ORACLE DOES. It asserts EQUALITY against the pairs the event resolved to, through the
same expectedPairs/countPairs/pairProblems oracle the delivery side already used, and it names the
extra pair by actor id.

WHAT IT DOES NOT PROVE. Nothing about the product: `sut.notifications` is a declared stand-in and
the registry bars every stand-in from the tiers that gate. It proves the ORACLE bites.

REPRODUCE: apply the edit above, run the two commands below, revert.

-----------------------------------------------------------------------------------------------
run at 2026-08-04T20:11:30.3728104+03:00, HEAD 360fb23d4e5e3a41504f5f2bb2eac0856c136d70 plus the one uncommitted mutation above

$ bun tests/at/harness/runner.ts "req-016" --tier loop --expect
JSON report written to C:/Users/nirdr/AppData/Local/Temp/at-verify-HUi4xV/vitest-report.json

at:verify req-016 --tier loop
  AT-016.01    red      CapabilityPending: CAPABILITY PENDING — H3 static provider scan
  AT-016.02    green    registered events equal the taxonomy exactly, are immutable, and carry no CR/scope-change event
  AT-016.03    green    every taxonomy row delivers to exactly its recipients on exactly its channels, with the named payloads
  AT-016.04    green    sensitive negatives: no candidacy to the NGO, no vetting outcome to the volunteer, no donation event
  AT-016.05    green    every critical class goes out by email; the low-tone event is in-app only
  AT-016.06    green    a documented delivery default exists for every taxonomy row
  AT-016.07    green    one logical event per committed event, one delivery per recipient-channel pair, across a restart
  AT-016.08    green    a comment burst delivers the count the pinned anti-spam configuration prescribes, on two different configurations
  AT-016.09    green    every guarded transition writes its notification event atomically under an induced fault
  AT-016.10    green    recipients resolve at event creation: the old holder receives, the new holder is excluded
  AT-016.11    red      AssertionError: the pairs the provider physically accepted are not exactly the pairs this event resolved to: expected [ Array(1) ] to deeply equal []
  AT-016.12    green    an escalation-tier event notifies both the NGO and the platform admin
  12 P0: 10 green, 2 red, 0 missing
  DEVIATION: AT-016.11 — declared green, reported red: AssertionError: the pairs the provider physically accepted are not exactly the pairs this event resolved to: expected [ Array(1) ] to deeply equal []
  DEVIATION: the report counts 2 failed tests but the declaration declares 1 red — a failure outside the declared ids (an untagged test, a failing hook) looks exactly like this
  DEVIATION: the report counts 10 passed tests but the declaration declares 11 greens
  EXPECT FAILURE: 3 deviation(s) from the declaration. A red that turned green is a failure too — if reality improved, update the declaration in the same change.
-> exit code 1

AND THE FULL ASSERTION DETAIL, which the runner's one-line report truncates:

$ AT_TIER=loop bunx vitest run --root tests/at --config vitest.config.ts suites/req-016/c-reliability-guard.test.ts

 RUN  v4.1.10 C:/Users/nirdr/Downloads/ai4good/.claude/worktrees/agent-af1e90584864a51d0/tests/at

 ❯ suites/req-016/c-reliability-guard.test.ts (3 tests | 1 failed) 41ms
     × AT-016.11 — sent only on provider acceptance; unconfirmed sends retry; a lost ack mints no duplicate 7ms

(the block below is vitest's failure detail, which it writes to stderr — pasted here verbatim from
the same run, because PowerShell's native-command redirection mangles it into ErrorRecords)

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 1 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  suites/req-016/c-reliability-guard.test.ts > AT-REQ-016 C — critical-event reliability guard > AT-016.11 — sent only on provider acceptance; unconfirmed sends retry; a lost ack mints no duplicate
AssertionError: the pairs the provider physically accepted are not exactly the pairs this event resolved to: expected [ Array(1) ] to deeply equal []

- Expected
+ Received

- []
+ [
+   "actor-ngo:email: 1 unexpected deliveries",
+ ]

 ❯ suites/req-016/c-reliability-guard.test.ts:286:9
    284|         pairProblems(expectedPairs(w.actors, ['volunteer'], ['email'])…
    285|         'the pairs the provider physically accepted are not exactly th…
    286|       ).toEqual([]);
       |         ^
    287|     },
    288|   );
 ❯ executeRegisteredBody harness/registry.ts:486:3
 ❯ runTrackedTest harness/registry.ts:555:5
 ❯ harness/registry.ts:778:5

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/1]⎯

 Test Files  1 failed (1)
      Tests  1 failed | 2 passed (3)
   Start at  20:11:31
   Duration  305ms (transform 122ms, setup 0ms, import 136ms, tests 41ms, environment 0ms)

-> exit code 1
