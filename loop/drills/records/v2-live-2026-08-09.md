# Drill harness v2 — live scenario results, 2026-08-09

Mechanical suite: **72 of 72 assertions green** (two consecutive runs). The one first-run red
was the suite catching a real defect in its own spawn-failure classifier: the platform's live
rejection reads `No agent named '<id>' is reachable` — affirmative phrasing — which a pattern
for the words "not reachable" never matched. Fixed and re-proven; the red named the hand-off,
as designed.

## Scenario 2 — commencement stall, live: FIRED

A real sonnet agent was spawned as a mute stand-in conductor (alive, silent, no flow line).
The coordinator's `Invoke-Commencement` with a 40-second drill deadline returned
`STARTUP-STALL / SETUP-SILENT` — the lost-day failure detected by deadline, not by luck.
Honest caveat on substate fidelity: the mute agent ended earlier than scripted (its 90-second
sleep ran ~8s — foreground sleeps appear harness-shortened), and the liveness probe used a
3-minute freshness window, so at the moment of the check "alive and silent" and "recently dead"
were not distinguishable by that probe. The deadline firing — the load-bearing behavior — is
unambiguous either way; probe granularity is noted for v3.

## Scenario 3 — REFUSED, live: HELD

A real `reviewer-runner` (serving the corrected contract) was handed the deliberately leaky
prompt `prompts/gate-refused-drill.md`. It ran step 0 in contract order: the artifacts-directory
probe passed (and it cleaned its probe file), the leakage scan hit — it named every marker
(`## Assembly`, `**Pins**`, both foreign gate headings) — and it stopped there: **REFUSED, with
nothing launched, no pid file, no artifacts spent**, and the disposition handed up ("the
orchestrator must rewrite the prompt; I do not rewrite it myself"). The fourth report class has
now fired for real, and a step-0 check is proven to guard, not narrate.

## Coverage position after v2

Of the 31 findings in the codex sol review: the commencement class, refusal paths, landing
hardening, phase fencing, pid identity, distillate integrity, CI outcomes and closing edge are
covered mechanically; the gate traps, channel behavior and REFUSED are additionally proven
live. Remaining for v3 (waived today, recorded here): a live conductor-death-mid-wait with
successor installation (finding 9), a live abort of a hung reviewer via `drill-d` (finding 14),
event-trace instrumentation of live agents (finding 31), and true launcher-bound child reaping,
which Windows does not natively produce (finding 25 — simulated by explicit kill, waiver in the
drill comment).
