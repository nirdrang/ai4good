# DECOMP REQ-034 — Requirement-Level Attribution (telemetry, never gating)
pm-item: AI4PM-47 · wave: W4 Run & money surfaces · surface: mixed
sync-stamp: 78c7d8f · d84
depends-on: REQ-009, REQ-026, REQ-028
cross-contracts: the writers already exist and were built against this manifest's pinned schema fixture — REQ-009 D5.L1 (gateway rows, W1) and the platform backend's Discovery/assistant calls (REQ-004 W2 / REQ-033 in-wave); this wave replaces the fixture with the real log and re-runs their row assertions. The binding lifecycle rides REQ-026's pull/complete (W3) and REQ-028's per-worktree binding (W3). The steering ceiling is d77's layered rule: Skill-local pause only (REQ-028), never platform/gateway gating. The NGO burn-per-requirement view is OWNED HERE (built and wired in D6 — REQ-013 stays the status/fuel dashboard and never grows a burn view). Assistant turns (REQ-033, in-wave) are produced before their rows are asserted — one-way. Money truth stays REQ-006/009/021's — this log is token-denominated with no money fields.
sources: requirements/req-034.md · acceptance/at-req-034.md (17 P0) · architecture-notes#req-034

## Done contract
All 17 P0 of AT-REQ-034 green at integration tier + the wiring leaf's wired re-run green (the NGO burn view) + founder attestation.
Verify commands: `pnpm at:verify req-034 --tier integration`. Harness is a W0 bring-up item; exact command pinned at pull.

## Deliverables

### D1 — Binding resolution & soft degradation
  leaves:
  - L1 resolution: a valid binding attributes the row to its pulled PM requirement; no/unresolvable binding → "unattributed", served either way; a spoofed-but-CURRENTLY-PULLED binding records there (telemetry, not a gate) while a real-but-unpulled PM item resolves to unattributed; dev-item bindings → unattributed + a coordinator signal · verify: AT-034.01,02 · blocked-by: cross-manifest: REQ-028 D3.L1 (the binding), REQ-009 D3.L2 (request riding)
  - L2 taskless sessions get exploration/onboarding offered PROACTIVELY as first-class buckets; attribution failure never blocks a request — the data degrades, never the service · verify: AT-034.03,04 · blocked-by: L1
  - L3 pull-interval validity: only an active pull binds; an unauthorized PM edit is not resolvable; a dev merge leaves the binding intact; verified completion makes it stale → unattributed · verify: AT-034.17 · blocked-by: L1 + cross-manifest: REQ-026 D4.L1/D5.L1 (pull + completion)

### D2 — The one usage log
  leaves:
  - L1 one append-only log, one row per metered request (including non-success), written in-line by the handling component with writer identity; surface/actor, project, request id, provider status, echoed token usage — no second store · verify: AT-034.05 · blocked-by: D1.L1 + cross-manifest: REQ-033 D2.L1 (assistant turns exist before their rows are asserted; Discovery turns are REQ-004's, done)
  - L2 rows carry no bodies and no money-denominated fields (metadata only, token-denominated); consumers are reads-only, no subscribers; mutation attempts rejected with bytes unchanged · verify: AT-034.06,07 · blocked-by: L1

### D3 — Two meters, never mixed
  leaves:
  - L1 fuel is never derived from token math (log rows + constant provider snapshots leave fuel unchanged); reconciliation is same-unit only — token totals vs provider usage, money ledger vs billed cost + Stripe, no cross-conversion path · verify: AT-034.08,09 · blocked-by: D2.L1

### D4 — Both billable surfaces & category reconciliation
  leaves:
  - L1 the direct surface (funded Discovery + assistant turns) writes rows with project + first-class categories, never folded into build burn; every row carries project + category and all attributed rows sum to the project's total · verify: AT-034.10,11 · blocked-by: D2.L1 + cross-manifest: REQ-033 D2.L1 (assistant turns)

### D5 — The steering ceiling (d77)
  leaves:
  - L1 forwarding is never gated for attribution (bound/unbound/spoofed all served); the only permitted pause is the Skill's own workflow; disabling the Skill is immediate and degrades to unattributed; the exploration→implementation nudge is NON-BLOCKING · verify: AT-034.12,16 · blocked-by: D1.L2 + cross-manifest: REQ-028 D3.L2 (the guided pause), REQ-028 D1.L2 (disable/enable), REQ-028 D5.L2 (the disabled-fallback degradation)

### D6 — The NGO burn view, aggregation boundary & baselines
  leaves:
  - L1 the NGO burn view (OWNED HERE): burn per requirement + per category, token-denominated, with exploration/onboarding/unattributed as honest lines reconciling to the total; no celebration; NO dev-item attribution exists anywhere — metered requests bind directly at PM-requirement granularity, never via a dev-to-PM roll-up · verify: AT-034.13 · blocked-by: D4.L1
  - L2 coordinator-side signals only (unattributed-% and binding failures never public); wide variation is data, not anomaly; per-requirement baselines are token-based with no currency anywhere · verify: AT-034.14,15 · blocked-by: L1
  - LW wire the NGO burn view from fixtures to edge functions · verify: wired re-run of ui-tagged P0s (no new AT ids) · blocked-by: L1, L2

## Coverage check
D1: 01,02 / 03,04 / 17 (5) · D2: 05 / 06,07 (3) · D3: 08,09 (2) · D4: 10,11 (2) · D5: 12,16 (2) · D6: 13 / 14,15 (3). Total 17 = suite's 17 P0; each id in exactly one leaf.
