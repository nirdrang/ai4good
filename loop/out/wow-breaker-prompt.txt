You are an adversarial evaluator. TRY TO BREAK this way-of-work plan for driving Linear entirely from Claude Code sessions (no GitHub-to-Linear automation). The plan governs how the ai4good buildout tracks its own work: Linear projects per requirement, issues as AT-derived slices, client-side attribution stamping, evidence-gated Done writes, and reconcile passes.

Read fully:
THE PLAN UNDER ATTACK: loop/out/wow-claude-driven-linear.md
CONTEXT (consult as needed): .taskmaster/docs/prd-mvp.md (REQ-026/028/034 describe the PRODUCT's task-system rules that this plan deliberately mirrors: two status authorities, binding rides every request, telemetry never gates, detect-and-revert); design/design-session.md; loop/decomp/ does not exist yet.

Attack angles (minimum — find more):
1. RACE/CONSISTENCY: two sessions/worktrees concurrently; queue replay after MCP outage (idempotency claims); reconcile cursor correctness; crash mid-/done (Linear written but attachment/comment lost, or vice versa).
2. EVIDENCE GATE HOLES: can Done be written on a commit that references the ID but does not contain the work (empty/revert commit)? v0 local-test evidence trustworthiness; batch-merge edge cases; rebase/squash changing SHAs after evidence cited; force-push history rewrite.
3. ATTRIBUTION: cache staleness beyond renames; worktree/file edge cases (multiple sessions one worktree); stamp injection risks (does stamping every message pollute context or leak into commits/PR text?); hierarchy correctness when an issue moves projects mid-work.
4. HUMAN-FACTOR: hand-edits in Linear UI; forgotten /done accumulating; premature Done revert fights; the founder working from a machine without the repo (mobile Linear edits).
5. INTERNAL CONTRADICTIONS: with the two-authority rule; with the product's own REQ-026/034 principles it claims to mirror; with the decomposition plan (projects per REQ, milestones, sync-stamp discipline).
6. MISSING PIECES: anything the GitHub integration provided that is now silently lost; anything the plan needs but does not define (e.g., who reverts R2 violations, where the reconcile cursor lives, queue file conflicts across machines).

Judge strictly but flag REAL problems only. If the plan survives, say pass.

OUTPUT ONLY this JSON (no prose before/after):
{"round":1,"verdict":"pass|needs-fixes","issues":[{"severity":"critical|major|minor","at":"section ref","detail":"...","fix":"..."}]}
